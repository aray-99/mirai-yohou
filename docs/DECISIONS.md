# DECISIONS — 実装判断の記録

フォーマットは `docs/SPEC.md` §0.5.5 に従う。ID は連番・ゼロ詰め4桁。

## [0001] リモートと CI/PR 運用

- 日付: 2026-07-07
- 論点: §0.5.3 は feature → develop の PR 運用を要求するが、本プロジェクトは自律実行で人間レビューを介さない。リモートの有無・PR の形式が未規定だった。
- 選択肢: (a) ローカルのみで運用し PR を省略 (b) GitHub リモートを作成し gh で PR を作る (c) リモートは作るが PR 相当の情報は `--no-ff` マージコミット body に記載する
- 採用: ユーザーが事前に作成していた既存の空リモート `aray-99/mirai-yohou`(public)をそのまま origin として利用(private 化を試みたところリポジトリが一時 disabled になったため、ユーザー了承のうえ public で運用)。main/develop を push し GitHub Actions CI(§0.5.4)を有効化。PR 必須記載事項(マイルストーン番号・通過テスト名・DECISIONS ID)は develop への `--no-ff` マージコミット body に記す (c)。
- 理由: §0.5.3 は「PR 本文またはマージコミット body」への記載を許容している。自律実行では gh PR を挟むよりマージコミット body の方が確実で、CI ゲートは push 時に機能する。
- 関連: §0.5.2 / §0.5.3 / §0.5.4

## [0002] Julia バージョン(ローカル 1.12 と CI 行列)

- 日付: 2026-07-07
- 論点: 仕様は Julia 1.10+ を要求(§0/§0.5.4)。ローカルは 1.12.6 であり、1.12 で生成した Manifest.toml は 1.10 の CI で julia_version 不一致となる。
- 選択肢: (a) CI を 1.12 のみにする (b) CI 行列 ['1.10', '1'] とし、1.10 側では Manifest を無視して resolve させる (c) juliaup で 1.10 を入れて Manifest を 1.10 で生成
- 採用: (b)。CI 行列は '1.10' と '1'(最新)。Manifest.toml は開発機の 1.12.6 で生成しコミットする。1.10 ジョブでは `Pkg.test()` 前に resolve が走ることを許容する。
- 理由: §0.5.4 の「1.10 を最低ライン」を CI で維持しつつ、開発機の完全再現は Manifest(1.12)で担保。両立の最小コスト解。
- 関連: §0.5.4 / §0.5.7

## [0003] Manifest.toml コミットと .gitignore 方針

- 日付: 2026-07-07
- 論点: §0.5.7 が M0 時点での確定と記録を要求。
- 選択肢: (a) Manifest をコミットしない(ライブラリ流儀) (b) コミットする(アプリ/再現性流儀)
- 採用: (b) Manifest.toml をコミット。`.gitignore` には計算スクラッチ(`scratch/`, `*.log`)、アンサンブル生出力(`experiments/output/`)、カバレッジ(`*.jl.cov` 等)、環境固有ファイル(`.vscode/`, `.DS_Store`)を列挙。`experiments/figures/` の最終図(小サイズ)はコミット可。大容量物が発生したら Git LFS を検討。
- 理由: §0.5.7 の明示規定(依存の完全固定=再現性)に従う。
- 関連: §0.5.7

## [0004] thinning における強度評価点の近似

- 日付: 2026-07-07
- 論点: Ogata thinning で格子点間の候補時刻における λ(X_t) の評価には格子点間の状態が必要(§10)。
- 選択肢: (a) 格子点間を補間 (b) 直前格子点の状態で近似
- 採用: (b)。dt = 0.01 年と最速時定数 1/β = 0.2 年に対し十分小さく、誤差は O(dt)。
- 理由: §10 の注意書きの指示どおり。Phase B で JumpProcesses.jl の VariableRateJump に移行した際に統計的一致を検証する。
- 関連: §10

## [0005] 分岐比 n の定義と v 依存の自己励起

- 日付: 2026-07-07
- 論点: ジャンプ時の λ_e 増分は α0·(v/v0)^κ_alphav(§6.3)であり、v > v0 の領域では実効的な分岐比が名目値 n = α0/β を超え得る。§6.1 の安定条件 assert がどちらを指すか曖昧。
- 選択肢: (a) 実効分岐比(v の上限を仮定)で assert (b) v = v0 基準の名目値 n = α0/β で assert し、λ_bar を安全弁とする
- 採用: (b)。パラメータ構築時に n = α0/β < 1 を assert(仮値では n = 0.4)。
- 理由: §6.1 の定義式が n = alpha0/beta と明記しており、同節が「lam_bar は数値安全弁」と位置づけているため。v 依存の実効爆発は λ_bar = 50/年 のクリップで防がれる。
- 関連: §6.1 / §6.3 / §7

## [0006] 無次元数 De の仮値検証(調整不要)

- 日付: 2026-07-07
- 論点: §8.4 は「安定国 De < 1・変動国 De > 1 を assert、ならなければ eta_g, delta_sig を調整して記録」と定める。仮値で成立するか未確認だった。
- 選択肢: (a) 仮値のまま (b) パラメータ調整
- 採用: (a)。初期状態評価で De_安定 = (0.5·(0.33−0.35)₊ + 0.3·0.30)/0.10 = 0.9 < 1、De_変動 = (1.0·(0.45−0.35)₊ + 0.3·0.50)/0.05 = 5.0 > 1。n = 2.0/5.0 = 0.4 < 1、H = 0.3/1.0 = 0.3。
- 理由: §8.2/8.4 の仮値のままで全制約を満たすため調整不要。
- 関連: §7 / §8.2 / §8.4

## [0007] ジャンプと EM ステップの演算子分割順序

- 日付: 2026-07-07
- 論点: §10 の擬似コードは格子区間内の thinning を規定するが、区間内ジャンプと Euler–Maruyama 更新の適用順序、および区間内での lam_e 減衰・状態変化の扱いが未規定。
- 選択肢: (a) 区間内でドリフト・拡散・ジャンプを厳密に時刻順に交互適用 (b) 区間 [t, t+dt) のジャンプを先に処理(受理のたびに Γ を適用し、以後の候補は更新後の状態で評価)し、その後 dt 全幅の EM ステップを行う
- 採用: (b)。区間内の lam_e 減衰と連続力学の進行は無視する(強度評価は直前状態、DECISIONS #0004 と同じ O(dt) 近似)。
- 理由: dt = 0.01 年は最速時定数 1/beta = 0.2 年より十分小さく、分割誤差は O(dt)。透明性・デバッグ容易性を優先する Phase A の方針(§10)に沿う。Phase B で JumpProcesses.jl の VariableRateJump と統計的一致を検証する。
- 関連: §6 / §10 / DECISIONS #0004

## [0008] regimes テストの「高確率」「ジャンプ ≈ 0」の定量化

- 日付: 2026-07-07
- 論点: §11 regimes テストは「安定国: 50年でジャンプ ≈ 0(高確率)/ 変動国: 少なくとも1回発火(高確率)。各20回反復で判定」と定めるが、「≈ 0」「高確率」が未定量。仮値での実測(シード1001〜1040の40反復)は、安定国: ジャンプ0回が33/40(82.5%)・平均0.45回(稀に自己励起連鎖で最大9回)、変動国: 全40回で1回以上(6〜24回、平均14.3回)。
- 選択肢: (a) 安定国は全20回でジャンプ0を要求(実測分布と矛盾し常に red) (b) 実測分布に余裕を持たせた閾値: 安定国はジャンプ0の反復が14/20以上かつ平均1回未満、変動国は1回以上発火が19/20以上
- 採用: (b)。テストは固定シード(決定的)で実行し、閾値は回帰検知のガードとして機能させる。
- 理由: lam0 = 0.02/年でも50年の基底期待値は約1回あり、文字通りの「全反復で0回」は仮値パラメータと整合しない。実測の二項分布(p≈0.83)に対し 14/20 は十分に緩い下限であり、かつレジーム分離(De<1 vs De>1)の定性は厳密に検証される。
- 関連: §7 / §11 / §12 M3

## [0009] 同化ドライバの実装詳細(src/assimilation.jl)

- 日付: 2026-07-07
- 論点: §9.2/§9.3 のハイブリッド同化ループの置き場所と、仕様が定めない細部: (i) 週次バッチ窓と dt=0.01年 グリッドの不整合(1週間 = 0.0192年)、(ii) 強制ジャンプ後の一時インフレーション 1.05 の適用期間、(iii) 観測時刻(月次 1/12年 等)とグリッドの不整合、(iv) §12 のディレクトリ構成に同化ドライバ用ファイルがない。
- 選択肢: (i) 窓境界でステップを分割 / 2グリッド(0.02年 ≈ 7.3日)で近似 (ii) 固定期間 / 次回解析まで (iii) ステップ分割 / 最近傍グリッドに丸め (iv) enkf.jl に同居 / 新規 src/assimilation.jl
- 採用: (i) 窓 = 0.02年(2グリッド)で近似 (ii) 強制ジャンプ後「最初の解析ステップ」のみ 1.05、以後 1.02(§9.3 の「広げ直す」目的に合致)(iii) 観測・イベント時刻は最近傍グリッドに丸め(誤差 ≤ dt/2 = 1.8日)(iv) 新規 src/assimilation.jl(EnKF 解析カーネルと駆動ループの関心分離。§12 の構成への追加であり既存ファイルの変更ではない)(v) ポアソン log 重みは窓ごとに破棄せず、再抽選まで累積する(標準的な適応リサンプリング粒子フィルタの解釈)。
- 理由: いずれも O(dt) 近似で Phase A の精度と整合。ドライバ分離は enkf_linear テストの純粋性(解析カーネル単体検証)を保つ。(v) は必須と判明: 窓ごとに重みを破棄すると、同化中は内生降伏がオフのため σ_s が暴走したメンバーが淘汰されず(1窓の重み差は微小)、強制ジャンプ時に m = (1-ρ)σ⁻ の巨大衝撃で発散する(E1a 初回ランで σ_s=90 のメンバーが xi_tau を発散させた)。累積重みは高 σ_s メンバー(Λ が観測カウント0と不整合)を数窓で淘汰し、これが §9.3 ハイブリッドの安定化機構そのものである。
- 関連: §9.2 / §9.3 / §10 / §12

## [0010] E1 合格判定の未規定細部の確定(基準値は不変)

- 日付: 2026-07-07
- 論点: §13 の合格基準のうち判定手続きが未規定のもの: (i) ランクヒストグラムのビン数と変数の扱い、(ii) tauA の「大ジャンプ」の定義、(iii) RMSE・相関・被覆率の評価時点、(iv) E1b の状態拡大に theta_p を含めるか。
- 選択肢・採用: (i) 全観測変数の事前ランクをプール(N=100 → ランク1〜101)し、10等分ビンのカイ二乗検定(df=9、有意水準は凍結 TOML の 0.05)。変数別ヒストグラムは診断用に出力のみ(3年毎の tau は15点しかなく変数別検定は検出力不足)。(ii) 真値カタログ中で解放エネルギー m が最大のイベント。(iii) 全グリッド時点(dt=0.01年)で評価。sigma_s の相関・被覆は自然座標で計算。(iv) §13 手順4の「(+theta_p)」は括弧書きのため theta_sig のみを必須とし、theta_p 拡大は E1b 合格後の拡張として保留。
- 理由: いずれも §0.5.8 で凍結した閾値(0.5 / 0.6 / 0.80–0.98 / 0.05 / ±50%)自体は動かさず、判定手続きの曖昧さのみを事前に固定するもの。合格判定の後出し変更を防ぐため、E1 実行前に本エントリで確定する。
- 関連: §13 / §0.5.8 / test/acceptance_thresholds.toml

## [0011] 病的メンバーの重みゼロ化ガード(数値安全弁)

- 日付: 2026-07-07
- 論点: 同化中は内生降伏がオフ(§9.3)のため、EnKF 更新・インフレーションで σ_s が暴走したメンバーが生じ得る。累積ポアソン重みは Λ の不整合でこのメンバーの重みを実質ゼロにするが、ESS = 1/Σw² は単一外れ値ではほぼ低下しない(w_i→0 は ESS を N 付近に保つ)ため再抽選が発火せず、外れメンバーが強制ジャンプ時に m = (1-ρ)σ⁻ の巨大衝撃で Inf/NaN を発生させ、EnKF の解析(Cyy の LU 分解)全体を落とす。E1a 初回ランで実際に発生(σ_s = 90 → xi_tau 発散。ジャンプ直後インフレーション 1.05 が引き金)。
- 選択肢: (a) logit 座標のクランプ(力学を歪める) (b) ESS 閾値を上げる(根本解決にならない) (c) 週次窓の評価時に「病的」メンバー(|logit ξ| > 15 または σ_s > e³ ≈ 20 または非有限)の log 重みを -Inf とし、1体でも該当すれば強制再抽選で置換する
- 採用: (c)。判定閾値は §9.4 の警告水準(|logit ξ| > 10)と降伏応力の正規化(σ_Y = 1)から十分外側に取り、正常なフィルタ動作(実測で σ_s ≤ 10 程度のメンバーは出現し得る)に干渉しない。
- 理由: 病的メンバーの Λ は観測イベントカウント0と大きく不整合であり、重みゼロ化はポアソン尤度の極限として §9.3 の意味論に整合する。状態のクランプと異なり力学・分布を直接歪めず、lam_bar と同種の「数値安全弁」(§6.1)である。
- 関連: §6.1 / §9.3 / §9.4 / DECISIONS #0009

## [0012] E1 不合格の診断(基準・パラメータは不変のまま報告)

- 日付: 2026-07-07
- 論点: §13 の双子実験 E1a/E1b が凍結しきい値(test/acceptance_thresholds.toml)で不合格。§13 の規定に従い、基準を動かさず診断を記録してユーザーに報告する。公式ラン(シード: 真値4501/観測4502/アンサンブル4503/自由4504/E1b 4505、コミットは本エントリ直前)の結果: RMSE比 k=18.2, g=0.69, tau=0.81, p=1.04(基準<0.5)、σ_s相関=0.40(>0.6)、被覆率=0.987(0.80–0.98)、tauA誤差 同化0.356 vs 自由0.040、ランクχ²=358.8(臨界16.9)、E1b theta_sig事後=発散。
- 診断(フィルタ崩壊か、可同定性不足か、実装バグか):
  - **主因はフィルタ崩壊(仕様の同化設定の構造問題)**: §9.2 の乗法インフレーション 1.02 を「解析ステップ毎」に適用すると、多レート観測(月次 v・四半期 y・年次ほか)では45年で約900回の解析があり、偏差が累積 1.02^900 ≈ e^17.8 倍に膨張する。強く観測される方向は解析で収縮するが、log y を通してしか観測されない k, h の直交方向(弱観測部分空間)は力学の収縮(≈6–15%/年)より速く膨張し、アンサンブル平均が漂流(xi_k 平均誤差 +2.2、最大メンバー +23)。感度実験(同一シード): ρ=1.0 で k比0.77/g比0.40/tau比0.40/p比0.91・σ_s相関0.81・被覆0.64、ρ=1.005 で k比0.92・被覆0.67、ρ=1.02 で k比18.2。発散が ρ に単調依存し、enkf_linear テスト(厳密KF一致)が green であることから実装バグは否定的。
  - **副因は可同定性不足**: p(分極)は2年毎 sd0.10 の観測に対し過程ノイズが支配的で、インフレーションなしでも RMSE 比 0.91(基準 0.5 は観測設計上ほぼ達成不能)。σ_s の被覆率はインフレーションなしで 0.64(過小分散)であり、「解析毎 1.02」と「なし」の間に適切な注入率が存在するが、その選定は §9.2 の数値(1.02)の変更を要する。
  - E1b の theta_sig 発散は主因の帰結(拡大パラメータ行が弱観測部分空間の膨張に巻き込まれる)。
- 対応: §13 の規定によりしきい値・パラメータの変更は行わない。想定される是正案(いずれも §9.2 の記述の解釈または改訂が必要): (a) インフレーションを「解析毎」でなく「単位時間あたり」に正規化(例: ρ_eff = 1.02^(Δt/0.25年))、(b) RTPS(事前スプレッドへの緩和)への置換、(c) 弱観測変数への付加的インフレーション、(d) p の基準の観測設計見直し。ユーザーの指示を待つ。
- 関連: §9.2 / §13 / §0.5.8 / DECISIONS #0009 / #0011

## [0013] スプレッド注入方式の改訂: 解析毎乗法インフレーション → RTPS(α=0.7)

- 日付: 2026-07-07
- 論点: #0012 の主因(解析毎 ×1.02 の複利膨張によるフィルタ崩壊)の是正。ユーザー承認のもと SPEC §9.2 の該当記述を改訂する(合格しきい値 §13 は不変)。
- 選択肢: (A) 時間正規化乗法 ρ_eff = 1.02^(Δt/0.25年) (B) RTPS(relaxation to prior spread)α ∈ {0.3, 0.5, 0.7}
- 採用: (B) RTPS α = 0.7(強制ジャンプ直後は α = 0.8)。AssimConfig の inflation_mode 既定値を :rtps とし、:per_analysis(v1.0 原案)・:per_time も比較用に残す。
- 理由: 診断マトリクス(シード4501/7701/8801、公式判定とは別)で、(A) は k 比 0.80–1.08・被覆 0.76–0.89 と不安定かつ複利膨張の程度問題を残すのに対し、RTPS は観測に拘束されない成分への注入が構造的にゼロで崩壊が原理的に再発しない。α=0.7 は3シード全てで σ_s 相関 0.87–0.97・被覆率 0.83–0.87 と、σ_s の両基準(§13)を唯一安定して満たした。E1b の θ_sig 発散も解消(有限値に収束)。
- 関連: §9.2 / §13 / DECISIONS #0012 / コミット(本エントリと同時)

## [0014] RTPS 改訂後の公式 E1 再判定: 依然不合格(残存項目の構造分析)

- 日付: 2026-07-07
- 論点: #0013 の改訂後、公式シード(4501〜4505)・凍結しきい値で E1a/E1b を再判定した結果と、残存 FAIL の診断。
- 結果: PASS = σ_s 相関 0.874(>0.6)/ σ_s 被覆率 0.833(0.80–0.98)/ g 比 0.337(<0.5)。FAIL = k 比 0.942 / tau 比 0.615 / p 比 1.002 / tauA(同化 0.263 vs 自由 0.040)/ ランク χ² 236.1(臨界 16.9)/ E1b θ_sig 事後 4.79(相対誤差 0.598 > 0.5)。#0012 のフィルタ崩壊は解消(発散なし・再抽選2回のみ)。
- 残存 FAIL の診断(3シードの頑健性確認と合わせた判定):
  - **p 比(0.78〜1.07、全シード未達)= 可同定性不足**。p の観測は2年毎 sd 0.10 で、p 自身の過程ノイズ(sigma_p0 × v 増幅)の定常変動を追えない。自由ランのアンサンブル平均は強い平均回帰により既に真値近傍にあり、「自由ランの半分」は現行観測設計ではほぼ達成不能。
  - **ランク χ²(216〜572、全シード棄却)= 検定の検出力過剰**。全観測変数をプールすると標本数 ≈ 950 で、EnKF の僅かな較正誤差(非線形・ジャンプ系では不可避)でも棄却される。§13 の意図する「較正の健全性チェック」としては、標本数に応じた実効的判定(例: ビン最大偏差率)への改訂が必要と思われる。
  - **k 比・tau 比・tauA = シード依存**(別シードでは k 0.43・tau 0.32〜0.37 で合格、tauA も 7701 で合格)。公式シード 4501 は自由ランのベースラインが偶然良い(k の自由ラン RMSE 0.073、tauA 窓誤差 0.040)ハードケース。単一シード判定の統計的限界(§14-7 と同種の問題)。
  - **E1b θ_sig(0.598)= σ_s 水準バイアスとの交絡**。同化中の内生降伏オフによりアンサンブルの σ_s がやや上振れし、λ 整合のため θ_sig が上方修正される。発散(#0012 時点の Inf)は解消しており、収束メカニズム自体は機能。
- 対応: §13 の規定に従いしきい値・観測設計・シードは変更せず、本エントリを記録してユーザーに報告し指示を仰ぐ。M5 は develop へマージしない(ゲート未達、§0.5.3)。
- 関連: §9 / §13 / §14-7 / DECISIONS #0012 / #0013

## [0015] E1 合格プロトコル v1.1(判定手続きの改訂。数値しきい値は不変)

- 日付: 2026-07-07
- 論点: #0014 の残存 FAIL のうち、(i) 単一シード判定が自由ランベースラインの運に支配される(k/tau/tauA は別シードで合格)、(ii) ランク χ² 検定は標本 ≈950 で検出力過剰となり EnKF の不可避な較正誤差でも常に棄却、(iii) p の RMSE 基準は観測設計上ほぼ達成不能(可同定性不足)、の3点はフィルタ性能でなく判定手続きの設計問題である。ユーザーの承認(方針一任)のもと、プロトコルを改訂する。
- 採用(v1.1。**新シードの実行前に本エントリとコミットで凍結**):
  1. 判定は事前宣言した未使用の5truthシード(6001〜6005。手法選定に使った 4501/7701/8801 系とは不交差)で行い、数値指標は**5シードの中央値**、二値指標(tauA)は**多数決(≥3/5)**で判定する。
  2. RMSE 比 < 0.5 の対象は xi_k, xi_g, xi_tau(**p は除外**。理由: 2年毎 sd 0.10 の観測では p の過程ノイズ変動は原理的に追えず、全シードで 0.78〜1.07。可同定性限界として記録)。
  3. ランクヒストグラム χ² 検定は「観測変数の較正」基準に置換: 全観測の事前アンサンブル90%区間への被覆率(プール)が 0.80〜0.98(σ_s と同じ凍結値を再利用)。
  4. σ_s 相関(>0.6)・σ_s 被覆(0.80〜0.98)・tauA(自由ラン比誤差減)・E1b θ_sig(±50%)は不変。
  5. 数値しきい値(0.5 / 0.6 / 0.80–0.98 / 0.05 / ±50%)はいずれも緩めない。変更は判定の集計方法と基準の構成のみ。
- 理由: (1) は §14-7「歴史は1回しか起きない」の精神を双子実験に適用したもの(合成データでは反復標本が作れる)。(2) は効果量ではなく標本数で結論が決まる検定の既知の欠陥の回避で、被覆率基準は「予報円」という本システムの出力思想(§1)に直接対応する。(3) は不合格を隠すのではなく可同定性限界として明示的に文書化する。v1.0 の結果(#0014)は本エントリと SPEC に恒久的に記録され、v1.1 の結果と併記される。
- 関連: §13 / §14-7 / §0.5.8 / DECISIONS #0012 / #0013 / #0014

## [0016] E1 プロトコル v1.1 の公式判定結果: E1b 合格・E1a 部分合格

- 日付: 2026-07-07
- 論点: #0015 で凍結した v1.1 プロトコル(未使用5シード 6001〜6005、中央値/多数決)による公式判定の記録。
- 結果: **E1b = PASS**(θ_sig 相対誤差 中央値 0.116。シード別 1.558/0.283/0.116/0.092/0.025 — 5本中4本が ±50% 以内)。**E1a = FAIL**(8項目中5 PASS): PASS = g 比 0.390 / tau 比 0.356 / σ_s 相関 0.932 / σ_s 被覆 0.947 / (E1b)。FAIL = k 比 0.789(0.399〜1.871 とシード分散大)/ 観測較正被覆 0.708(5シードで 0.701〜0.717 と一貫。基準 0.80)/ tauA 2/5(多数決 3/5 未達)。
- 診断:
  - **観測較正被覆 0.71(全シード一貫)= 事前アンサンブルの観測空間での系統的過小分散**。約10ポイントの不足であり、フィルタ崩壊ではなくスプレッド管理の較正不足。RTPS α や状態拡大ノイズの増強で改善し得るが、公式結果を見た後の手法調整は #0015 の凍結原則に反するため実施しない。
  - **k 比(中央値 0.789)**: 自由ランが強いベースラインであることに加え、シード 6002 の 1.87 が示すとおりジャンプの資本破壊(c_k m)の同化誤差が尾を引く。過小分散の解消が K ゲインの改善を通じて波及する可能性がある。
  - **tauA(2/5)**: 判定がベースラインの運に敏感(自由ラン誤差 0.07〜0.10 vs 同化 0.03〜0.16)。
- 対応: §0.5.6 に従い E1b 合格状態に `experiment/E1b-pass` タグを付す。E1a は不合格のため M5 のゲート(§12)は未達とし、develop へマージしない。第一段階の科学的結論(中核仮説の検証状況)と残課題を明記してユーザーに報告し、(i) 過小分散是正を目的とした手法改善の追加ラウンド(プロトコルは v1.1 のまま再判定)、(ii) 現状を第一段階の結論として確定し実データフェーズ設計へ進む、の判断を仰ぐ。
- 関連: §12 / §13 / §0.5.6 / DECISIONS #0013 / #0014 / #0015

## [0017] 観測較正ランクの定義修正(メンバー側への観測ノイズ付加)

- 日付: 2026-07-07
- 論点: v1.1 公式判定(#0016)で観測較正被覆が全シード一貫して 0.70〜0.72(基準 0.80)だった。原因調査の結果、事前ランクの計算がメンバー予測値 H(x_j) を生のまま観測値と比較しており、観測値に含まれるノイズ(y = H(x_truth) + ε、ε ~ N(0, σ_o²))をメンバー側に付加していなかった。ランクヒストグラム/順位被覆の標準定義(Hamill 2001, MWR)では H(x_j) + ε_j(ε_j は独立な観測ノイズ抽選)と比較する。省略すると、アンサンブルスプレッドが σ_o と同程度以下の高頻度観測変数(月次 log v が観測946点中540点)で見かけの過小分散が生じ、プール被覆を系統的に押し下げる。
- 選択肢: (a) 現定義のまま(標準定義と不一致な指標で判定を続ける) (b) メンバー側に観測ノイズ抽選を加えた標準定義に修正
- 採用: (b)。ノイズ抽選は力学の乱数列と独立の専用ストリーム(rank 用 Xoshiro)で行い、状態推定の再現性に影響させない。
- 理由: これはフィルタや基準の調整ではなく、指標実装を文献標準の定義に一致させるバグ修正である。基準値(被覆 0.80〜0.98)・プロトコル(シード・集計)は不変。修正は公式シードの再実行前にコミットで凍結する。
- 関連: §13 / DECISIONS #0015 / #0016

## [0018] ランク定義修正後の公式 v1.1 再判定: E1a 6/8(残 k・tauA)、E1b PASS 維持

- 日付: 2026-07-07
- 論点: #0017 の指標修正(手法・プロトコル・シードは不変)後の公式判定の記録と、残存2項目の構造分析。
- 結果: 観測較正被覆 0.910(全シード 0.903〜0.915)で PASS に転じた。E1a = FAIL(8項目中6 PASS。残: k 比 中央値 0.789、tauA 2/5)。E1b = PASS 維持(θ_sig 誤差中央値 0.116)。調整用シードでの α 感度確認では α を上げると k が悪化(0.85 で 1.41)するため手法は α=0.7 のまま。
- 残存2項目の構造分析:
  - **k 比 0.789**: 同化の k 絶対 RMSE は log 座標で ≈0.06(約6%)と小さいが、自由ランのベースラインが ≈0.07 とほぼ完璧なため「その半分未満」に届かない。k は log y(四半期 sd 0.01)を通してのみ観測され、k–h の直交方向は力学でしか拘束されない。自由ランは同一初期分布から力学が真値多様体上に保つため、この方向で自由ランを2倍上回ることは観測設計上ほぼ不可能。
  - **tauA(2/5)**: 同種の「ほぼ完璧なベースライン」問題(自由ラン窓誤差 0.07〜0.10 vs 同化 0.03〜0.16)。塑性シフトの大きさがメンバー個別の σ_s に依存するため、同化はイベント時刻を捉える一方でシフト量に分散が乗り、勝敗がシード運に支配される(3シードの調整ランでも 1/3)。
  - いずれもフィルタ品質の欠陥ではなく、「自由ランが強い変数に対して RMSE 半減を要求する」基準構成の問題。E1 の本来の目的である隠れ状態復元(σ_s 相関 0.932)・較正(0.910/0.947)・パラメータ同定(11.6%)は達成されている。
- 対応: 手法調整はこれ以上行わない(α 感度で悪化を確認済み)。第一段階の結論として「E1b PASS・E1a 6/8」を確定し、M5 ゲートの扱い(部分合格での完了認定 or 継続)はユーザーの判断を仰ぐ。
- 関連: §12 / §13 / DECISIONS #0015 / #0016 / #0017

## [0019] 第一段階(M5)の完了認定: E1b PASS + E1a 6/8 を充足とする

- 日付: 2026-07-07
- 論点: §12 の M5 完了条件は「§13 の合格基準」。v1.1 最終判定は E1b PASS・E1a 6/8(残: k 比 0.789、tauA 2/5)であり、残存2項目は #0018 のとおり「ほぼ完璧な自由ランベースラインの半減要求」という基準構成の構造的限界で、手法調整では改善しない(α 感度で悪化を確認)。
- 選択肢: (A) 残存2項目を構造的限界として文書化のうえ第一段階完了と認定 (B) E1a 未達のまま feature ブランチに保留
- 採用: (A)。ユーザーの明示承認による。M5 を develop/main にマージし、vM5 と experiment/E1a-partial タグを付す。
- 理由: E1 の本来の目的 — 隠れ状態 σ_s の復元(相関 0.932・被覆 0.947)、観測較正(0.910)、支配パラメータ θ_sig の同定(誤差 11.6%)、観測可能変数の追跡(g 0.390・tau 0.356)— は達成されており、残存項目は情報理論的に観測設計へ帰着する。k・tauA の改善は実データフェーズの設計課題(観測追加・平滑化 EnKS)として §14 と同様に持ち越す。
- 関連: §12 / §13 / §14 / DECISIONS #0016 / #0017 / #0018

## [0020] マイルストーン M6(Phase B)の定義

- 日付: 2026-07-07
- 論点: §10 Phase B(JumpProcesses.jl VariableRateJump + 適応 SDE ソルバへの移行、Symbolics.jl による疎ヤコビアン自動検出)は「E1 合格後の最適化」と定められているが、§12 の表にマイルストーンとしての完了条件がない。第一段階完了(#0019)を受け、ユーザーの継続指示により Phase B を M6 として定義する。
- 採用: M6 の完了条件を次のとおり事前確定する(タグ vM6):
  1. **phaseb_agreement テスト**: Phase A(固定刻み EM + thinning)と Phase B(SOSRI + VariableRateJump)のアンサンブル統計が一致。事前確定トレランス: 主要変数(xi_g, xi_tau, xi_k, xi_sig)の終端時刻平均の差 ≤ 4×結合標準誤差、分散比 ∈ [0.5, 2]、平均イベント数の差 ≤ 4×√(平均/N)。両レジームで検証。
  2. **sparsity テスト**: Symbolics.jacobian_sparsity によるドリフトヤコビアンの自動検出が §5.1 の見積り(実効密度 ≈ 28%、48/169)と整合(密度 0.20〜0.35 の帯で検証し、検出パターンの主要連成(式11の行が最密)を確認)。
  3. 既存テストすべて green(Phase A の経路は不変)。
- 実装判断: (i) softplus を分岐レス形 `max(z,0) + log1p(exp(-|z|))` に書き換える(数値的に従来と同値。Symbolics のトレースは Bool 分岐を通せないため)。(ii) Phase B は純予測(内生 Hawkes)のみ実装し、同化(ExogenousEvents・EnKF)は Phase A の固定刻み経路を維持する(§10 の位置づけどおり)。(iii) drift!/diffusion! 等の引数型を AbstractVector に緩め、シンボリックトレースと JumpProcesses の拡張配列を受け入れる(数値経路は不変)。
- 理由: §10 の「Phase A と統計的に一致することを確認」を検証可能な事前確定基準に落とし込むもの。トレランスはテスト実行前に本エントリで凍結する。
- 関連: §5.1 / §10 / §12 / DECISIONS #0019

## [0021] Phase B(VariableRateJump)はスレッド並列で発火しない

- 日付: 2026-07-07
- 論点: simulate_sde_phaseb を Threads.@threads でアンサンブル実行すると全メンバーのジャンプ数が 0 になる(逐次実行では 6〜13 回/30年で正常)。JumpProcesses v9 の VariableRateJump(連続コールバックによる発火検出)がスレッド安全でないためと推定。
- 選択肢: (a) 原因を JumpProcesses 内部まで追う (b) Phase B のアンサンブルは逐次実行とし、注意書きを docstring と本エントリに残す
- 採用: (b)。1ラン ≈ 13ms(コンパイル後)で100メンバー逐次でも ≈1.5秒であり、並列化の必要がない。phaseb_agreement テストも逐次で実行する。
- 理由: Phase A(自作 thinning)はメンバー独立でスレッド安全(§10 のアンサンブル並列は Phase A の設計)。Phase B の並列化が必要になった時点で Distributed 並列か JumpProcesses の更新を検討する。
- 関連: §10 / DECISIONS #0020

## [0022] Phase B 残課題の結論: stiff ソルバ・マルチレート化は不要

- 日付: 2026-07-07
- 論点: §10 Phase B の「stiff 対応ソルバとマルチレート化を検討」の定量評価。
- 評価: 変動国の代表軌道(シード4501、45年)に沿った10点でドリフトヤコビアンを有限差分計算し固有値を評価。最速減衰率は全点で |Re λ|max = 5.0/年(= β、Hawkes 減衰。§10 の想定どおり)で、最短時定数 0.2 年は dt = 0.01 年の20倍 — 陽解法で十分解像される。剛性比(1.3e4〜3.6e12)は大きいが、eps_A = 0.02/年 や外生式の準ゼロモードなど「遅い端」に起因し、陽解法のステップ制約にはならない。SOSRI(適応)の実測も特別な縮小を示さない。
- 採用: 現行パラメータ領域では stiff ソルバ・マルチレート化を導入しない。13次元 × N=100 の計算コストは自明であり、複雑化の便益がない。将来 β の大幅増や高速連成の追加でパラメータ領域が変わる場合に再評価する。
- 理由: stiffness が問題になるのは最速モードが時間地平に比して過小なステップを強制する場合で、本モデルは該当しない。これで §10 Phase B の項目はすべて完了。
- 関連: §10 / DECISIONS #0020 / #0021

## [0023] Phase 2 のスコープ決定(ユーザー承認)

- 日付: 2026-07-07
- 論点: PHASE2_DESIGN.md v0.1 の ⚖ 判断ポイント。
- 採用(ユーザー選択): (1) イベント一次ソース = **ACLED**、GDELT は感度分析用(検証期間は ACLED カバレッジに合わせ 2010 年代以降中心)。(2) 変動国第一候補 = **タイ**(安定国は日本)。(3) GDP ヴィンテージ = **段階化**(Phase 2.0: 最新確定系列の疑似リアルタイム、改定影響は R に上乗せ → Phase 2.5: 当時版)。(4) マイルストーン M7(データ基盤+EnKS)→ M8(単国ヒンドキャスト)→ M9(walk-forward 複数国)を承認。
- 残りのデフォルト採用(提案どおり、変更時は本エントリ改訂): ジニ = SWIID(補間済みのため R を膨らませて使用)、T 代理 = WIPO 特許出願。
- 関連: docs/PHASE2_DESIGN.md / DECISIONS #0022

## [0024] M7 の完了条件(着手前凍結)

- 日付: 2026-07-07
- 採用: M7 の完了条件を次のとおり凍結する(タグ vM7):
  1. **EnKS(固定ラグ平滑化)**: assimilation に固定ラグ平滑化を実装し、E1 設定(v1.1 の5シード)で平滑化推定の k・tauA 誤差が**フィルタ推定より悪化しない**(中央値で比較)こと。単体テスト(線形玩具問題で平滑化が事後分散を減らす)green。
  2. **データ基盤**: 日本・タイの観測系列(P, w, y, g, T代理, phi, v, tau, p)を公開 API/データセットから取得・整形・キャッシュするスクリプトと、来歴メタデータ(取得日時・ソース URL・バージョン)の付与。ACLED は API キーが必要なため、キーを環境変数で受けるスタブまで(キー取得はユーザー側)。再実行で同一出力(キャッシュ)になること。
  3. 既存テストすべて green。
- 関連: docs/PHASE2_DESIGN.md §6-7 / DECISIONS #0023

## [0025] EnKS の設計: 疎観測ゲート + 変数局所化

- 日付: 2026-07-07
- 論点: 素朴な固定ラグ EnKS(全観測・全変数を平滑化更新)は E1 設定で k を 10.7% 悪化させた(#0024 基準1に抵触)。原因は、過去状態への実情報を持たない高頻度観測の解析(45年で約900回)のたびに、クロス共分散の標本雑音(N=100)由来の微小更新がスナップショットへ蓄積すること。疎観測(period ≥ 2年)限定でも k は -11%(g/tau/p 観測と過去 k の真の相関が乏しく、雑音のみが乗る)。
- 採用: (i) 平滑化更新は period ≥ smoother_min_period(既定2年)の観測を含む解析に限定、(ii) 更新する状態行を smoother_vars(既定: xi_g, xi_tau, xi_tauA, xi_sig, xi_p の制度ブロック+格差)に限定する変数局所化。E1 設定(v1.1 の5シード、中央値)の結果: lag=5年で k +0.2%(悪化なし)・tauA +1.4%・σ_s +4.9% 改善、lag=2年で k +0.1%・tauA +0.5%・σ_s +7.9%。#0024 基準1を充足。
- 理由: 平滑化の設計目的(遅延・低頻度観測による塑性変数・隠れ応力の事後改善、PHASE2_DESIGN §6)に更新範囲を一致させるもので、スカラー距離に基づく共分散局所化の変数版として標準的。§9.2 の「局所化不要」はフィルタ更新についての規定であり、数百回の逐次平滑化更新には当てはまらない。
- 関連: §9.2 / DECISIONS #0024 / docs/PHASE2_DESIGN.md §6

## [0026] ACLED の認証方式と Research access の制約(フェッチャ実装)

- 日付: 2026-07-08
- 論点: M7 の ACLED スタブ(#0024 基準2)は旧 API キー方式(ACLED_ACCESS_KEY / ACLED_EMAIL)を前提としていたが、現行の ACLED API は OAuth 2.0 password grant に移行しており前提が無効。また、トークン取得成功 = read 権限ではないことが判明(認証と認可が別)。
- 経緯: 外部リポジトリ <https://github.com/aray-99/acled-client> で認証フローを検証。新規 myACLED アカウントは /oauth/token が 200 を返しても /api/acled/read は 403 になる。サポートへの申請(API & data access)で 2026-07-08 に "Research access" へ昇格し、read 成功を確認済み。
- 採用: fetch_data.jl の fetch_acled を実装。(i) 認証は ENV["ACLED_USERNAME"] / ["ACLED_PASSWORD"] による password grant(client_id=acled, scope=authenticated、access_token 24時間)。(ii) /api/acled/read を Bearer トークン+limit/page ページングで取得し、event_id_cnty, event_date, year, disorder_type, event_type, sub_event_type, admin1, fatalities を `<ISO3>_events.csv` にキャッシュ(来歴サイドカー付き、§0.5.6)。admin1・disorder_type は初回取得後に追加(タイの死傷イベントの約85%が深南部4県の慢性反乱であり、国政レベルのジャンプと分離する地理フィルタに必要と判明したため)。(iii) 年範囲は 2010〜現在(タイのカバレッジ開始に合わせる。日本は東アジア拡張以降のみ)。
- 制約(M8 の検証期間設計に影響): Research access は (a) 週次集計データは無制限だが、(b) イベント単位の生データは**直近12ヶ月がエンバーゴ**。ヒンドキャスト(2010年代中心、#0023)には支障なし。リアルタイム運用が必要になった場合はティア変更か週次集計 API の利用を再検討する。
- 理由: #0024 の「キー取得はユーザー側」が完了したため、スタブを実フェッチャに置換するもの。M8(単国ヒンドキャスト)のイベントカタログ・週次カウント生成の前提データを確保する。
- 関連: §0.5.6 / §14-3 / DECISIONS #0023 / #0024 / docs/PHASE2_DESIGN.md §4 / <https://github.com/aray-99/acled-client>

## [0027] p(分極度)系列は V-Dem vdemdata パッケージから自動抽出(手動配置から変更)

- 日付: 2026-07-08
- 論点: M7(#0024)では p を「V-Dem サイトから手動ダウンロード」とし、ユーザー側作業に割り当てていた。V-Dem は公式 R パッケージ vdemdata(GitHub)で全データセット(vdem.RData、約34MB)を認証なしで配布しており、自動取得・来歴固定が可能。
- 採用: experiments/data/extract_vdem_p.jl を追加。(i) vdem.RData を GitHub から取得(Git 管理外の scratch にキャッシュ)、(ii) v2cacamps(political polarization)の original-scale 点推定 v2cacamps_osp(0〜4)を **/4 で 0〜1 に正規化**して `<ISO3>_p.csv`(1990〜)を生成、(iii) 来歴サイドカーに vdemdata のデータファイルコミット SHA(v16 リリース、f4dd269、2026-03-17)と測定不確実性系列(v2cacamps_osp_sd / 4。M8 の観測ノイズ R 設計用)を記録。RData/CodecBzip2/DataFrames を experiments 環境に追加(Manifest 固定)。
- 実測: JPN 36点(1990〜2025、0.13〜0.28)・THA 36点(0.40〜0.98)。安定国/変動国の想定(初期値 p=0.30 / 0.50、§8.4)と整合し、タイは 2013–14・2020年代に上昇。logit 座標でも |ξ| < 4 で §9.4 の警告水準に抵触しない。
- 理由: 手動配置を自動化することで §0.5.6 の来歴要件(どの版のデータか一意に遡れる)を満たし、V-Dem の年次更新にも --force 再抽出で追従できる。正規化 /4 は測定モデルの original scale の値域(0〜4)による自然な線形写像で、PHASE2_DESIGN §2 の「0〜1 に正規化」を具体化するもの。tau(WVS)は登録ダウンロードが必要なため引き続きユーザー側作業。
- 関連: §0.5.6 / DECISIONS #0023 / #0024 / #0026 / docs/PHASE2_DESIGN.md §2

## [0028] phaseb_agreement テストの CI 不安定性の診断(基準・テストは未変更)

- 日付: 2026-07-08
- 論点: develop の CI(Julia '1' = 1.12.6 ジョブ)で phaseb_agreement の「stable: 分散比 ∈ [0.5, 2]」(#0020 で凍結)が fail(実測 2.32、再実行で 2.47 — 再実行間でも値が変わる)。同一コミット・同一依存バージョン・同一 Julia でローカルは pass(1.39)。src/test は vM7 以降無変更で、コード回帰ではない。
- 診断(ローカル追試、stable レジーム・30年):
  1. **連続力学は一致**: ジャンプ未経験メンバーに条件付けた分散比は 1.05〜1.18(3シード対、N=1600)。
  2. **統計量が重い裾**: xi_sig の終端分散は少数のジャンプ経験メンバー(N=1600 中 150〜185)に支配され、全体分散比はシード対によって 1.10〜1.50 と大きく揺れる。N を 400→1600 に増やしても収束しない(推定量の分散でなく実現ジャンプ集合の差が支配的)。
  3. **環境感度**: Phase B(SOSRI 適応刻み)は浮動小数点環境(runner の CPU / SIMD 幅)で軌道が僅かに変わり、降伏閾値近傍のメンバーのジャンプ有無が入れ替わる。CI と ローカルで同一シードでも実現値が異なるのはこのため。境界 2.0 への真の余裕が薄く(真の比 ≈ 1.1〜1.5)、環境によって越える。
  4. **未解決の観察**: ジャンプ経験メンバー数が Phase B で系統的に少なめ(2/3 シード対で約 -13%)だが総イベント数はほぼ一致 — B では自己励起連鎖が少数メンバーに集中している可能性があり、分散比の差の一部はこれかもしれない(Phase A thinning の区間内近似 #0007 と VariableRateJump の連鎖解像の差)。
- 対応: §0.5.8 の精神(基準を後から動かさない)に従い、テスト・しきい値は変更せずに本エントリで診断を記録し、ユーザーに以下の選択肢を提示して指示を待つ:
  - (A) 判定構成の改訂(数値しきい値は不変): 分散比 [0.5, 2] の適用対象を「ジャンプ未経験メンバーの条件付き分散」(連続力学の一致)に変更し、ジャンプ側の一致は既存のイベント数テストで担保する。#0015(v1.1 改訂)と同型の「推定量の構成変更」であり要ユーザー承認。
  - (B) 現状維持(CI の当該ジョブの不安定を既知として記録し、再実行運用)。
  - (C) 観察4(B の連鎖集中)を先に有界調査し、実差の有無を確定してから判断。
- 関連: §10 / DECISIONS #0007 / #0020 / #0021 / test/test_phaseb.jl

## [0029] phaseb_agreement の有界調査の結果と判定構成の改訂(C→A、ユーザー承認)

- 日付: 2026-07-08
- 論点: #0028 の選択肢 C(実差の有界調査)→ A(判定構成の改訂)をユーザーが選択。調査の結論と改訂内容の記録。
- 調査(λ_b を定数に凍結した厳密検定。theta_sig=theta_p=theta_v=kappa_alphav=0 で λ_b = lam0 とし、指数 Hawkes の厳密理論値 — ジャンプ経験率 = 1−exp(−λ0T) = 0.4512(移民過程のみで決まり連鎖に非依存)、平均件数 = λ0T/(1−n) = 1.0 — と比較):
  1. **実装欠陥なし**: N=8000 で Phase A(z=+0.9、平均 1.0046)・Phase B(z=+0.9、平均 0.983)とも理論値と一致。#0028 観察4(B のジャンプ経験メンバー約-13%)は N≤2000 のシードバッチ揺らぎと確定(同一系列を N=8000 に延長すると z=-1.9 → -1.2 に減衰。A 側の逆方向異常 z=+3.2 も同様に +0.9 へ収束)。
  2. **JumpProcesses 単体も厳密**: 定数強度の最小再現(1次元、N=20000)で理論と一致(z=0.8)。ノイズ・適応刻みを加えても不変。ソルバ許容誤差(1e-2→1e-4)はジャンプ統計に影響しない(強度が状態非依存の間はジャンプ抽選が軌道と独立のため)。
  3. **不安定の実体**: stable レジームの終端分散は少数(≈10%)のジャンプ経験メンバーに支配される重い裾で、分散比の実現値はシード対・FP 環境(runner の SIMD 幅等による適応軌道の微差 → 降伏閾値近傍メンバーのジャンプ有無の入替)で 1.10〜2.47 と揺れる。真の比は ≈1.1(ジャンプ未経験条件付きで 1.05〜1.18)であり、境界 2.0 への余裕が構造的にない。
- 採用(判定構成の改訂。数値しきい値 [0.5, 2] は不変): stable レジームの分散比判定を**ジャンプ未経験メンバーの条件付き分散**(連続力学の一致検定。条件付き標本 ≥100 を assert)に変更。ジャンプ側の一致は既存のイベント数テスト(4×√(平均/N)、不変)が担保する。volatile はほぼ全メンバーがジャンプを経験し重い裾問題がないため全体分散のまま。平均差 ≤ 4×結合SE も不変(SE が裾を自己適応するため)。
- 理由: #0015(推定量の構成変更、数値不変)と同型。検定対象を「何の一致を検証したいか」(連続力学 vs ジャンプ機構)に対応する統計量へ分離するもので、しきい値の緩和ではない。実装欠陥がないことを厳密理論との照合で確定させた上での改訂である。
- 関連: §10 / DECISIONS #0015 / #0020 / #0028 / test/test_phaseb.jl

## [0030] M8 設計の凍結(PHASE2_DESIGN §9 のユーザー承認)

- 日付: 2026-07-08
- 論点: PHASE2_DESIGN §9(M8 詳細設計ドラフト)の承認と凍結。ユーザーは §9 提示後に「作業はそのままどんどん進めてください」と指示(2026-07-08)し、提案どおりの採用を承認した。
- 凍結事項(検証結果を見た後は動かさない。§0.5.8 と同手続き):
  1. **観測系列**: y は年次で開始(四半期化は改善オプションとして後置。E1-sparse の感度確認が根拠)。g は SWIID gini_disp(#0023 のデフォルトを Dataverse doi:10.7910/DVN/LM4OWF v9.92 からの自動抽出で実装。補間系列のため R は点別 se を基に膨らませる)。World Bank Gini は感度分析用に併存。tau = WVS E069_11(ユーザー作業、docs/TODO_WVS_TAU.md)。p = V-Dem(#0027)。
  2. **スケール正規化**: 無次元化の基準年 = 1990(両国)。P, y, T代理, v は 1990 年値で除して log(A0 = y_ref = v0 = 1 の §8.1 正規化を 1990 年に固定する解釈)。
  3. **イベントストリーム**(prepare_events.jl): THA は深南部4県(Pattani/Narathiwat/Yala/Songkhla)を除外。対象 = disorder_type に Political violence を含むイベント + Riots(平和的デモは除外)。強制ジャンプカタログは週次規模 s = 死者数 + 0.1×件数の較正期間内分位 q=0.98(下限フロア min_size=5)で閾値を決め、検証期間には凍結適用。JPN は強制ジャンプなし(無イベント尤度のみ)。
  4. **較正/検証分割**: THA 較正 2010-01〜2017-12 / 検証 2018-01〜2024-12。JPN 較正 1995-01〜2015-12 / 検証 2016-01〜2024-12。スピンアップは 1990 年から。
  5. **L2 較正**: 反復 EnKF 方式。較正対象は {eta_g, delta_sig, lam0, mu_gbar, mu_p}(他の L2 は §8.2 仮値を維持)。L3 状態拡大は theta_sig(theta_p の追加は変動国で再評価、#0010)。
  6. **M8 完了条件**(タグ vM8): (i) 検証期間の観測較正被覆(事前90%区間、プール、Hamill 定義 #0017)0.80〜0.98(両国)。(ii) 同化ありの逐次1年先予測 RMSE が同一較正パラメータの自由ラン比で観測変数の過半で改善(国別)。(iii) THA 検証期間の週次カウント予測ポアソン尤度が自由ラン(内生 Hawkes)より改善。(iv) 既存テスト green。
  7. R の具体値・較正の反復回数等の数値設定は、検証期間の実行**前**に追加の DECISIONS エントリで凍結する(E1 の acceptance_thresholds.toml と同じ流儀)。
- 補足: ジャンプカタログの試験出力に検証期間のイベント(2023-12 等)が含まれた状態で設計を凍結しているが、閾値決定は較正期間内の分位のみを用いる手続きであり、検証期間の観測値・判定指標は未参照である。
- 関連: docs/PHASE2_DESIGN.md §9 / DECISIONS #0023 / #0024 / #0026 / #0027 / #0029

## [0031] M8 設計の補完(実装着手時の設計ギャップ3点。較正・検証ラン実行前の凍結)

- 日付: 2026-07-08
- 論点: #0030 の凍結後、実装着手時に3つの設計ギャップが判明した。いずれもまだ較正・検証ランを一切実行していないため、#0030 への補完として実行前に凍結する。
  1. **イベントカウントとモデル強度のスケールリンク**: モデルの λ は「大変動」の強度(lam0 = 0.02〜0.1/年)だが、ACLED 週次カウント(#0030-3 のフィルタ後)はタイで約58件/年と桁が違う。E1 ではモデルジャンプ=観測イベントが1対1だったため存在しなかった問題。
  2. **v の水準整合**: 1990 年基準の正規化(#0030-2)でモバイル契約は log(v/v0) が最大 7.4 まで成長する。式(8)の平衡 xi_v ≈ xi_T + c_v0 に対し c_v0(仮値 0)の較正なしでは水準が構造的に合わない。
  3. **ACLED カバレッジ外の窓**: 日本のイベントデータは 2018 年以降のみ。カバレッジ外の週次窓でカウント0を「観測された0」として尤度に使うと誤情報になる。
- 採用:
  1. 観測週次カウントを N_w 〜 Poisson(ν·Λ)(Λ = ∫λ dt、ν = 報告率)でリンクする。ν は L2 較正対象に追加(較正期間で推定し検証期間に凍結)。run_assimilation に観測カウント列(窓別)とスケール ν を渡せる後方互換な拡張を行う(既定は従来動作 = event_times のカウント・ν=1)。
  2. c_v0 を L2 較正対象に追加する(#0030-5 の {eta_g, delta_sig, lam0, mu_gbar, mu_p} → + c_v0, ν の7個)。
  3. カウント列に「データなし」窓を許し(負値で表現)、その窓はポアソン重み更新をスキップする(強制ジャンプ・EnKF 解析は通常どおり)。
- 理由: (1) は §14-3「イベント規模とマークの対応付けが実データフェーズの主戦場」の一部であり、ν は最も単純な間引き/多重報告モデル。(2) は観測が z ≤ 7.4 に達する事実からの必然で、c_v0 はスケール定数の性格が強く事前 LogNormal の枠に馴染む(exp 変換で正値化)。(3) はデータの存在しない期間に人工的な「静穏」情報を注入しない、の一般原則。3点とも検証結果を見た後の変更ではない。
- 関連: §9.3 / §14-3 / DECISIONS #0030 / PHASE2_DESIGN §9

## [0032] σ_s ステップの数値ガード(tame + floor)

- 日付: 2026-07-08
- 論点: 日本ヒンドキャストのスモークランで全メンバーが数値発散。診断: 式(11)の log 座標 drift は L·e^{−ξ_sig} 項をもち、L(載荷)< 0 の領域では σ_s → 0 に緩和するのが物理的正解だが、ξ_sig ≈ −9 で |drift| ≈ 数百/年に発散し、固定刻み EM(dt=0.01)が破綻して NaN が伝播する。双子実験では L がほぼ常に正(変動国)か弱負(安定国でも σ_s 初期値 0.3 の近傍)でこの領域に入らなかった。実データの安定国(日本)では yhat > 0・低 g・低 p により L < 0 が恒常化し、必ず踏む。
- 採用: integrator/assimilation の EM ステップに (i) tame: |f_sig| ≤ 50/年(dt=0.01 で最大 |Δξ|=0.5)、(ii) floor: ξ_sig ≥ −12(σ_s ≈ 6e-6。L 符号反転時の復帰時間を有界に保つ)。双子実験の領域では |f_sig| ≲ 5 でガードは不発火であり、既存の全テスト・E1/M6 の記録結果に影響しない(全テスト green で確認)。
- 理由: lam_bar(§6.1)・病的メンバーの重みゼロ化(#0011)と同種の「力学を歪めない数値安全弁」。σ_s → 0 は SPEC §5 の注記「log 座標のおかげで L < 0 でも σ_s > 0 が保たれる」の想定内の極限であり、ガードは厳密解が滑らかに到達する床を離散化が飛び越えないようにするだけである。
- 関連: §5 式(11) / §10 / DECISIONS #0011 / コミット(本エントリと同時)

## [0033] 週次カウント尤度のテンパリング(情報量のモデルイベント換算)

- 日付: 2026-07-08
- 論点: タイのスモークランで ESS が 1 まで崩壊(45年で再抽選43回)。ν ≈ 772(#0031-1 のモーメント初期値)のポアソン尤度 Poisson(N_w | ν·Λ) は「観測マイクロイベント1件 = 独立な情報1件」として扱うため、週次カウント数件でもメンバー間の log 重み差が数十に達し、粒子フィルタが退化する。実際の ACLED カウントは1つの政治的大変動が多数の関連イベント(デモ・衝突の連鎖)として報道される**クラスタ構造**をもち、独立ポアソンより大幅に過分散である。
- 採用: カウント尤度の log 重み増分を 1/ν 倍にテンパリングする: log w += (1/ν)·[N_w·log(ν·Λ) − ν·Λ]。これは「クラスタサイズ ν の複合ポアソン」の近似で、観測週次カウントの実効情報量を「モデルイベント数」単位に換算するもの。run_assimilation に count_temper 引数を追加(既定 1.0 = 従来動作。E1 経路は不変)。
- 理由: ν → 1 で通常のポアソン尤度に連続的に一致する最小の過分散補正であり、追加パラメータを導入しない。NegBin 等のより精密な分散モデルは M9(walk-forward)で再評価する。
- 関連: §9.3 / DECISIONS #0031 / #0011

## [0034] M8 検証ラン設定の凍結(#0030-7 の履行)

- 日付: 2026-07-10
- 論点: L2 較正(EKI、experiments/M8_calibrate.jl)完了を受け、検証期間ランの前に全設定値を凍結する。**本エントリとコミット以後、検証結果を見た後の変更は禁止**(E1 の acceptance_thresholds.toml と同じ流儀)。
- 凍結値(experiments/M8_frozen_config.toml、コミット済み):
  1. **較正済みパラメータ**(EKI J=24・iters=4・N=100、較正窓のみ使用。ν はポアソン最尤プロファイル): JPN {eta_g 0.423, delta_sig 0.121, lam0 0.0195, mu_gbar −0.880, mu_p −1.138, c_v0 4.767, ν 1.0}、THA {eta_g 1.223, delta_sig 0.0362, lam0 0.237, mu_gbar 0.080, mu_p 0.651, c_v0 4.657, ν 4.59}。他の L2 は §8.2 仮値。ミスフィット: JPN 2.16→1.1(収束)、THA 〜20 で振動(年別カウントパターンの限界。収束改善は M9 課題として持ち越し)。
  2. **ラン設定**: N=100、seed=20260708、初期スプレッド 0.3、RTPS α=0.7、EnKS lag=5年、t1=35(2024年末)。
  3. **観測ノイズ sd**(保持座標): P 0.005 / w 0.01 / y 0.02 / g_swiid max(0.05, 2×SWIID se デルタ法) / T_proxy 0.15 / phi 0.05 / v 0.05 / tau 0.15 / p max(0.10, V-Dem sd デルタ法)。
  4. **イベント**: 深南部除外・q=0.98・count_scale=0.1・min_size=5(#0030-3)、テンパリング 1/ν(#0033)。
  5. **外生入力**: netgrowth・wbar は較正窓データからのフィット(決定的手続き)。
- 較正窓サニティ(凍結前確認、検証窓は未参照): JPN 被覆 0.948・RMSE 比 9/9 < 1。THA 被覆 0.846・8/9 < 1(g 1.65 のみ超過 — 強制ジャンプの g 衝撃と較正済み自由ランの強さ。#0030-6 の「過半」基準には抵触しない)。
- 判定基準: #0030-6 の4項目(検証窓被覆 0.80–0.98 / 1年先予測 RMSE 過半改善 / THA カウント予測尤度改善 / テスト green)。実装解釈(検証前に確定):
  - 基準1の「事前アンサンブル」= 逐次同化の解析前状態(観測グリッド1ステップ前の状態で近似、O(dt)=3.65日)。
  - 基準2の「1年先予測」= 検証窓の各観測時刻 s について、同化ランの s−1 年時点の状態から**純予測(内生 Hawkes・同化なし)で1年間発射**したアンサンブルによる H の予測。ベースラインは同一較正パラメータの自由ラン(同化なしなので自由ランの s 時点の状態がそのまま1年先予測)。
  - 基準3のカウント予測尤度 = Σ_w log Poisson(N_w | ν·Λ_w)(検証窓のカバレッジ窓、テンパリングなしの素の尤度で両者を同一比較。Λ_w は各ランの状態から積分)。
- 関連: §0.5.8 / DECISIONS #0030 / #0031 / #0033 / experiments/M8_frozen_config.toml

## [0035] M8 検証ランの公式判定: 不合格(基準1両国・基準3日本)と診断

- 日付: 2026-07-10
- 結果(凍結設定 #0034、コミット c74b67b で実行): **基準1(検証窓被覆 0.80–0.98)= FAIL**(JPN 0.485 n=66 / THA 0.389 n=54。較正窓では 0.948/0.846 だった)。**基準2(1年先予測 RMSE 過半改善)= PASS**(JPN 5/9、THA 6/9)。**基準3(カウント予測尤度)= JPN FAIL**(−93.9 vs 自由 −86.8)**/ THA PASS**(−483.5 vs −1187.0)。基準4(テスト green)= PASS。総合 = **不合格**。#0030-6 の規定に従い、しきい値・手法・較正値は変更せず本エントリで診断を記録しユーザーの指示を待つ。
- 診断:
  1. **被覆率の崩壊(主因)**: 較正窓 0.95/0.85 → 検証窓 0.49/0.39 の急落は、逐次事前アンサンブルの検証期バイアス+過小分散。1年先 RMSE の内訳が原因を指す — JPN tau 10.6倍・g 7.4倍、THA g 11.8倍・tau 1.3倍と、**制度ブロックの隠れ変数(tauA)経由の汚染**が顕著。tau の1年先誤差 2.2〜4.9(logit)は κ_tau(ξ_tauA − ξ_tau) のドリフトとして ξ_tauA − ξ_tau ≈ O(10) を要求し、**同化された tauA が非物理的な遠方に漂流**していることを示す。tauA は無拘束(観測なし・力学は遅い追従のみ)で、EnKF の擬似相関と EnKS 更新(smoother_vars に tauA を含む)の蓄積によるドリフト — E1 で記録した k・tauA の弱観測方向問題(#0018)の実データ版。
  2. **THA g の1年先悪化(11.8倍)**: 純予測発射中の内生 Hawkes ジャンプ(較正値 lam0 = 0.237/年は高い)が c_g·m の衝撃を毎回与える。自由ランは(mu_gbar が較正済みのため)ジャンプがあっても平均回帰で真値近傍に留まり、ベースラインとして強い。
  3. **JPN 基準3(僅差 FAIL)**: ν = 1・観測カウントほぼ0の領域で、同化が σ_s をやや高く推定し λ が過大(logL 差 7.1)。イベントがほぼ無い国での λ 水準の較正情報が乏しい(較正窓にカウントデータなし — ACLED カバレッジ 2018〜)ことの帰結。
  4. 基準2は両国 PASS であり、観測変数の追跡・短期予測の情報量という中核機能は機能している。破綻は「隠れ変数の長期無拘束ドリフト」と「予報分布の較正」に局在する。
- 想定される是正方向(いずれも実施はユーザー承認後。手法変更として DECISIONS 記録+再較正→同一凍結基準で再検証): (a) tauA の解析更新の制限(smoother_vars からの除外・変数局所化の強化)or tauA への弱い擬似観測(tau 観測時に tauA ≈ tau の緩い拘束)、(b) 予測発射時のアンサンブルへの RTPS 相当のスプレッド回復、(c) JPN の λ 較正に週次集計 API(リアルタイム側)の利用を検討。
- 関連: §13(不合格時の規定の準用) / DECISIONS #0018 / #0030 / #0034

## [0036] M8 是正 (a): tauA の拘束強化(ユーザー承認、手法変更)

- 日付: 2026-07-10
- 論点: #0035 の主因診断(tauA の無拘束ドリフト)への是正。ユーザーは提示 3 案のうち **(a) のみ**を承認((b) スプレッド回復・(c) 週次集計 API は見送り)。凍結済みの判定基準(#0030-6)と検証ラン設定の解釈(#0034)は一切変更しない。手法変更のため再較正(EKI、#0034-1 と同一の J=24・iters=4・N=100)を行い、新較正値を検証実行前に追加エントリで凍結した上で、同一基準で再検証する。
- 採用(2 点):
  1. **tauA への緩い擬似観測**: tau の観測が届いた解析ステップで、同じ観測値を tauA(IX_TAUA、logit 空間)への擬似観測として batch に追加する(h = xi -> xi[IX_TAUA])。sd は **tau 観測 sd の 3 倍**(#0034-3 の tau sd 0.15 に対し logit 単位 0.45)に凍結。実装は run_assimilation / AssimConfig への後方互換な拡張(#0033 の count_temper と同型、既定オフ = 従来動作)。
  2. **M8 の smoother_vars から tauA を除外**: M8 実験(M8_hindcast.jl / M8_calibrate.jl)で smoother_vars = [IX_G, IX_TAU, IX_SIG, IX_PP] を明示指定する。AssimConfig のデフォルト(#0025、tauA 含む)は変更しない — E1・既存テストの記録結果を保護するため。
- 理由: tau の 1 年先誤差 2.2〜4.9(logit)は ξ_tauA − ξ_tau ≈ O(10) を要求し(#0035-1)、これは κ_tau 追従・EnKF 擬似相関のどちらからも物理的に説明できない解析起因のドリフトである。擬似観測 sd = 3×tau sd は、|ξ_tauA − ξ_tau| を数観測周期で O(1) に有界化する一方、アンカーが観測 tau に遅れて追従する式(9)の力学(遅い緩和)を上書きしない緩さとして選んだ。EnKS 経由の遡及更新(1 の拘束が及ばない経路)は 2 で遮断する。いずれも検証データを新たに参照しない手法変更であり、E1 の #0018(k・tauA の弱観測方向は smoother_vars 局所化で対処)と同一の設計原理の適用である。
- 関連: §9.3 / DECISIONS #0018 / #0025 / #0033 / #0034 / #0035

## [0037] M8 再較正値の凍結(#0036 手法変更後、再検証ラン前)

- 日付: 2026-07-10
- 論点: #0036 の実装(コミット 8294c57)後、#0034-1 と同一の EKI 手続き(J=24・iters=4・N=100、最終反復のアンサンブル平均、ν はポアソン最尤プロファイル、較正窓のみ使用)で再較正した。再検証ラン前に新較正値を凍結する。**本エントリ以後、検証結果を見た後の変更は禁止**(#0030-7 の準用)。
- 凍結値(experiments/M8_frozen_config.toml 更新済み、seed 20260710):
  - JPN {eta_g 0.448, delta_sig 0.122, lam0 0.0211, mu_gbar −0.849, mu_p −1.081, c_v0 4.743, ν 1.0}。ミスフィット 2.22 → 1.15(収束、#0034 とほぼ同水準)。旧値からの変化は小さい(tauA 拘束は JPN 較正窓の適合をほぼ変えない)。
  - THA {eta_g 1.253, delta_sig 0.0429, lam0 0.166, mu_gbar 0.064, mu_p 0.304, c_v0 4.142, ν 4.95}。ミスフィット 21.7 → 35.6 で振動(#0034 で記録済みの THA 振動と同じ現象。最終反復平均を採るのは #0034 と同一の決定的手続きであり、反復の選好は行わない)。lam0 0.237 → 0.166、mu_p 0.651 → 0.304 が主な移動。
- ラン設定・観測ノイズ・イベント設定(#0034-2〜5)と判定基準の実装解釈(#0034 後段)は全て不変。#0036 の追加設定(tauA_pseudo_sd_mult = 3.0、smoother_vars = [IX_G, IX_TAU, IX_SIG, IX_PP])は再較正・再検証の両方に適用される(M8_calibrate.jl / M8_hindcast.jl 両方に実装済み)。
- 関連: §0.5.8 / DECISIONS #0034 / #0036 / experiments/M8_frozen_config.toml

## [0038] M8 再検証ランの公式判定: 不合格(基準1両国)と診断

- 日付: 2026-07-10
- 結果(凍結設定 #0037、コミット 4fcf37a で実行): **基準1(検証窓被覆 0.80–0.98)= FAIL**(JPN 0.470 n=66 / THA 0.519 n=54。前回 0.485/0.389 → THA は改善、JPN は横ばい)。**基準2(1年先予測 RMSE 過半改善)= PASS**(JPN 6/9、THA 6/9)。**基準3(カウント予測尤度)= 両国 PASS**(JPN −79.8 vs 自由 −96.9 — 前回 FAIL から転換。THA −442.0 vs −1291.6)。基準4(テスト green)= PASS(8294c57 で 312 本)。総合 = **不合格**。規定に従い、しきい値・手法・較正値は変更せず本エントリで診断を記録しユーザーの指示を待つ。
- #0036 の効果: 基準3 JPN の FAIL→PASS、THA 被覆 0.389→0.519、THA lam0 の低下(0.237→0.166、#0035-2 の副因を緩和)。一方 JPN の tau 1年先 RMSE 比は 10.6→47.95、g_swiid 比 7.4→50.1 に悪化。
- 診断:
  1. **(a) の拘束は実質不発だった(tau 観測の極端な疎性)**: WVS tau は JPN 5 点(較正窓 4・検証窓 1 = 2019)/ THA 3 点(較正窓 1・検証窓 1)。擬似観測は tau 観測時にしか発火しないため、検証窓では各国 1 回しか tauA を引き戻せない。一方 tauA への解析更新は**毎年の全観測(P, w, y, phi, v, p 等)との擬似相関経由で無制限に起こる**(enkf.jl の現在状態更新は全状態行に K を掛ける。smoother_vars/smooth_rows は過去スナップショットの平滑化のみを局所化し、現在時刻の解析には効かない — #0036-2 はドリフトの主経路を塞いでいなかった)。ξ_tauA のドリフトは継続し、稀な擬似観測の発火時には大きなイノベーションが擬似相関経由で g 等の他変数に注入されて悪化を招いた(JPN g_swiid 比 50.1 の有力経路。drift には tau→g の力学結合はなく、解析起因と特定)。
  2. **基準1の不合格は tau/tauA だけでは説明できない**: tau は被覆分母の 1.5〜1.9%(JPN 1/66、THA 1/54)。ミス質量の大半は年次マクロ系列(P, w, y, phi, v, p 各 7〜9 点)にあり、検証窓での事前アンサンブルの**過小分散/バイアス**が支配的 — #0035 是正案 (b)(スプレッド回復)が対処するはずだった側の問題。変数別被覆の内訳は現行成果物に保存されておらず(プール値のみ)、確定には成果物拡張が要る。
  3. **RMSE 比の指標特性**: g_swiid(SWIID 補間系列)はほぼ単調・平滑で自由ラン RMSE が 0.03〜0.05 と極小のため、同化側の中程度の誤差でも比が 50 倍等に膨張する。50 倍という数字は実害を過大表示している(基準2は「過半」判定のため影響なし。指標の再設計は行わない — 記録のみ)。
- 想定される是正方向(実施はユーザー承認後): (α) **現在時刻の解析更新への変数局所化**(tauA 行の更新を tau/tauA 観測時のみに制限する解析マスク — #0036 の設計原理を主経路に適用するもの)、(β) **検証窓の事前スプレッド不足への対処**(#0035-(b) のスプレッド回復の再提案。基準1の支配的ミス質量への直接対処)、(γ) 変数別被覆の成果物への追加(診断能の改善。判定基準自体は不変)。
- 関連: DECISIONS #0018 / #0035 / #0036 / #0037 / src/enkf.jl / experiments/M8_hindcast.jl

## [0039] 意思決定権限の委任(ユーザー指示、ガバナンス変更)

- 日付: 2026-07-10
- 論点: #0038 の是正方針の選択肢提示に対し、ユーザーは次のとおり指示した(原文趣旨): 「以降、Fable が最適だと思うように意思決定を行ってください。この方針は適切なドキュメントに記載しておいてください。決定内容とその理由が後から参照できる状態であれば、自律的に進め続けることを推奨します。」
- 採用(運用規則の改訂):
  1. **手法・是正方針の選択は Fable が自律決定する**。個別のユーザー承認は不要。ただし全ての決定と理由は従来どおり DECISIONS エントリとして事前に記録する(記録なしの変更は引き続き禁止)。
  2. **不変の境界**: 凍結済みの合格基準(#0030-6)としきい値は自律決定の対象外とする(結果を見た後に基準を動かさない原則は科学的健全性の根幹であり、この変更にはエントリだけでなく明示のユーザー承認を引き続き要求する)。マイルストーンの合格認定・main へのマージ・タグ付与は結果とともに事後報告する。
  3. CLAUDE.md の運用方針にも本委任を反映する。
- 関連: CLAUDE.md / DECISIONS #0030 / #0038

## [0040] M8 是正2回目: 解析局所化 + RTPS 強化 + 診断成果物(#0039 に基づく自律決定)

- 日付: 2026-07-10
- 論点: #0038 の診断への是正。#0039 の委任に基づき Fable が決定(選択肢 α/β/γ の全採用)。凍結基準(#0030-6)と判定解釈(#0034 後段)は不変。手順は従来どおり: 本エントリ → 実装 → 再較正(EKI 同一手続き)→ 新較正値の凍結エントリ → 再検証 → 判定記録。
- 採用(3点):
  1. **(α) 現在時刻解析の変数局所化**: AssimConfig に後方互換の設定(既定 = 従来動作)を追加し、指定した状態行(M8 では IX_TAUA)について、解析バッチが指定観測名(M8 では :tau / :tauA_pseudo)を含まない場合に現在時刻の EnKF 更新(K の該当行)をマスクする。EnKS の smooth_rows(過去平滑化の局所化、#0025)と対になる、現在時刻側の局所化である。
  2. **(β) RTPS α 0.7 → 0.85(M8 のみ)**: 基準1(逐次事前被覆)のミス質量の大半は年次マクロ系列の過小分散(#0038-2)。RTPS は #0013 で採用済みの確立したスプレッド保持機構であり、α 増は解析後スプレッドの事前スプレッドへの回復を強める最小の一手。乗法インフレーションは #0012 で崩壊が記録済みのため採らない。値 0.85 は本エントリで(検証実行前に)確定する。AssimConfig デフォルト(0.7)は不変、M8 実験の設定で指定。
  3. **(γ) 変数別診断の成果物化**: M8_hindcast.jl の検証出力に変数別の被覆内訳(hit/n)と事前アンサンブルの変数別スプレッド・イノベーション統計を JSON サイドカーとして保存する。判定はこれまでどおりプール被覆値で行い、基準・判定手続きは一切変えない(診断能の追加のみ)。
- 理由: (α) は #0036 と同一の設計原理(実情報のない変数への雑音蓄積防止、#0018/#0025 系譜)をドリフトの主経路(現在時刻解析)に適用するもの。tau 観測の疎性(検証窓1点)は変えられないデータ制約であり、拘束を増やすのではなく無拘束方向への漏洩を塞ぐ側を選ぶ。(β) は #0035 是正案 (b) の系統で、当時見送られたが #0038-2 で必要性の証拠(被覆ミスの支配的質量が tau 以外)が得られた。(γ) は今回プール値しか残らず原因変数を確定できなかった反省。いずれも検証窓の観測値・判定指標を新たに参照しない。
- 関連: DECISIONS #0012 / #0013 / #0025 / #0036 / #0038 / #0039

## [0041] M8 再々較正値の凍結(#0040 手法変更後、3回目検証ラン前)

- 日付: 2026-07-11
- 論点: #0040 の実装(コミット 46df7ae)後、#0034-1 と同一の EKI 手続き(J=24・iters=4・N=100・seed 20260710、最終反復平均、ν ポアソン最尤プロファイル、較正窓のみ)で再較正した。3回目検証ラン前に凍結する。**本エントリ以後、検証結果を見た後の変更は禁止**(#0030-7 の準用)。
- 凍結値(experiments/M8_frozen_config.toml 更新済み):
  - JPN {eta_g 0.454, delta_sig 0.122, lam0 0.0202, mu_gbar −0.866, mu_p −1.049, c_v0 4.719, ν 1.0}。ミスフィット 1.99 → 0.96(収束。#0037 の 1.15 よりさらに改善)。
  - THA {eta_g 1.444, delta_sig 0.0632, lam0 0.106, mu_gbar −0.028, mu_p −0.596, c_v0 4.157, ν 4.83}。ミスフィット振動継続(iter4 = 32.1、既知 #0034/#0037)。lam0 は 0.237(#0034)→ 0.166(#0037)→ 0.106 とさらに低下、mu_p は符号反転(0.304 → −0.596。tauA 局所化と RTPS 0.85 により較正窓の適合構造が変わった帰結)。
- 透明性の記録(プロトコル逸脱の申告): #0040 実装時にサブエージェントが動作確認として JPN の非 smoke 検証ラン(--calib)を1回実行し、プール被覆 0.652(旧 #0037 較正値のまま)を一時観察した(成果物は削除済み)。これは正式判定ではなく、本凍結値(再較正後)による正式ランとは較正値が異なる。凍結基準・判定手続きへの影響はないが、検証窓への非公式アクセスとして記録する。今後のサブエージェント指示には「非 smoke 検証ランの実行禁止」を明記する。
- 関連: §0.5.8 / DECISIONS #0037 / #0040 / experiments/M8_frozen_config.toml

## [0042] M8 3回目検証ランの公式判定: 不合格(基準1両国)と診断の確定

- 日付: 2026-07-11
- 結果(凍結設定 #0041、コミット 774494e で実行): **基準1 = FAIL**(JPN 0.545 / THA 0.444。推移: 0.485/0.389 → 0.470/0.519 → 0.545/0.444)。**基準2 = PASS**(JPN 6/9、THA 7/9)。**基準3 = 両国 PASS**(JPN −92.7 vs −96.9、THA −794.1 vs −1256.8)。基準4 = PASS(323 本)。総合 = **不合格**。
- #0040 の効果検証: (α) は明確に有効 — JPN tau 1年先 RMSE 比 47.95 → 6.66(絶対 8.57 → 1.15)、g_swiid 比 50.1 → 1.53。tauA ドリフト汚染は解消。(β) RTPS 0.85 は被覆をほぼ動かせず(理由は下記)。(γ) により原因変数が初めて確定した。
- 診断(γ の変数別内訳で確定。innov = |観測 − 事前平均|、spread = 事前 sd):
  1. **被覆崩壊の実体はバイアス優位で、機構はアンサンブル崩壊によるゲイン消失**。JPN: v 0/8(innov/spread 3.5)・T_proxy 0/6(9.2)・phi 2/9(5.9)。THA: T_proxy 0/4(**120.3**)・P 0/7(17.7)・phi 0/7(4.8)・y 1/7(6.6)。例: THA T_proxy は事前 spread 0.0056 vs 観測 sd 0.15 → カルマンゲイン ≈ 0.001 で、**同化が T 観測を事実上無視**し、トレンド誤差が検証窓 7 年間無修正で蓄積する。RTPS は解析後スプレッドを「事前スプレッド比」で回復する機構のため、事前自体が崩壊した方向には構造的に無力(β が効かなかった理由)。
  2. **崩壊変数はいずれも「平衡構造がない or 駆動パラメータが較正対象外」**: T は平衡なしの単調成長で成長率(a_T, c_T0, eta_Ty)は §8.2 固定値。phi は減衰項のない駆動式で r_phi 等は全て固定。P は較正窓 netgrowth 定数(始点終点勾配)の外挿のみ。v は目標 T + c_v0 経由で T の誤差を継承。y は合成診断量として全てを継承。EKI 較正対象 7 個がこれらの成長率/駆動パラメータをほぼ含まないことが、被覆崩壊の変数パターンと符合する。
  3. 較正窓で被覆が良かった(0.95/0.85)のは、初期スプレッド 0.3 が残存しているうちに較正窓が終わるため。検証窓では観測を吸えない方向のスプレッドが枯渇する。
- 関連: DECISIONS #0040 / #0041 / experiments/output/M8_verify_diag_{JPN,THA}.json

## [0043] M8 是正3回目: 観測座標の事前スプレッド下限(#0039 に基づく自律決定)

- 日付: 2026-07-11
- 論点: #0042 診断への是正。#0039 の委任に基づき Fable が決定。凍結基準(#0030-6)・判定解釈(#0034)は不変。
- 採用: **観測座標の加法的スプレッド床**。各解析ステップで、バッチ内の各観測の座標 h(x) について事前アンサンブルの sd が **floor = 0.5 × 観測 sd** を下回る場合、当該観測座標方向の状態行(h が単一状態行の恒等写像である実データ観測に限る)へ独立ガウス摂動を加えて sd を床まで回復してから解析を行う。実装は AssimConfig への後方互換 kwarg(既定オフ)。floor 係数 0.5 は本エントリで検証前に確定する。
- 理由: ゲイン消失(#0042-1)への標準的対処は事前スプレッドの維持であり、加法ノイズは (i) K = spread²/(spread²+sd²) を床で最低 0.2 に回復して逐次解析にトレンド追従能力を返し、(ii) 被覆区間(spread と観測ノイズの合成、Hamill #0017)を広げる — バイアスと過小分散の両方に働く最小の一手。乗法インフレーション(#0012 で崩壊記録)ではなく加法とし、RTPS(#0013)とは独立に併用する。駆動パラメータの L3 状態拡大(a_T, netgrowth 等を状態化して同化で学習する)がより根治的だが、現行 L3 機構は theta_sig 専用ハードコードで一般化は中規模リファクタとなるため M9 に送る。**本ラウンドの歯止め**: 検証診断を参照した手法反復はこれで3回目であり、検証窓への実質的な適応(参照バイアス)が累積するリスクを認識する。本是正で不合格の場合、Fable は反復を停止し、構造的限界(モデルの自由度不足)としてユーザーに報告・相談する。
- 手順: 本エントリ → 実装(テスト green)→ EKI 再較正(同一手続き)→ 凍結エントリ → 再検証 → 判定記録。
- 関連: DECISIONS #0012 / #0013 / #0017 / #0039 / #0042

## [0044] M8 較正値の凍結(#0043 手法変更後、4回目検証ラン前)

- 日付: 2026-07-11
- 論点: #0043 の実装(コミット fd341d7)後、同一 EKI 手続き(J=24・iters=4・N=100・seed 20260710)で再較正。4回目検証ラン前に凍結する。**本エントリ以後、検証結果を見た後の変更は禁止**(#0030-7 の準用)。
- 凍結値(experiments/M8_frozen_config.toml 更新済み):
  - JPN {eta_g 0.446, delta_sig 0.122, lam0 0.0206, mu_gbar −0.873, mu_p −1.051, c_v0 4.752, ν 1.0}。ミスフィット 1.94 → 1.04(収束、安定)。#0041 からの移動はごく小さい。
  - THA {eta_g 1.840, delta_sig 0.0500, lam0 0.223, mu_gbar 0.291, mu_p 0.510, c_v0 3.741, ν 4.56}。振動継続(iter4 = 25.5)。スプレッド床導入で較正窓の適合構造が再度変わり、lam0 が 0.106 → 0.223 に戻り mu_p も再度符号反転(THA 較正の同定不良は既知 #0034/#0037/#0041 — EKI ミスフィット振動下では較正値がラウンド間で大きく動く)。
  - 参考(較正窓サニティ、#0043 実装時の smoke: 旧 #0041 較正値でのもの): JPN 被覆 0.837 / THA 0.631。
- 関連: §0.5.8 / DECISIONS #0041 / #0043 / experiments/M8_frozen_config.toml

## [0045] M8 4回目検証ランの公式判定: 不合格 — 是正反復の停止と構造的診断(ユーザー相談へ)

- 日付: 2026-07-11
- 結果(凍結設定 #0044、コミット dba549e で実行): **基準1 = FAIL**(JPN 0.545 / THA 0.241)。基準2 = PASS(5/9, 5/9)。基準3 = 両国 PASS(JPN −63.1 vs −96.9、THA −803.4 vs −1178.1)。基準4 = PASS(330 本)。総合 = **不合格**。#0043 で事前に確定した歯止めに従い、**是正反復をここで停止**する(4ラウンドの推移: 基準1 JPN 0.485→0.470→0.545→0.545 / THA 0.389→0.519→0.444→0.241)。
- #0043(スプレッド床)の効果: 部分的に有効(JPN P 被覆 0.56→1.00、JPN プール値は横ばい)だが、(i) **床は観測 sd に比例するため、バイアスが観測 sd の数倍〜数十倍の変数(THA v innov 0.375 vs sd 0.05 等)には原理的に届かない**、(ii) THA は較正値の大振れ(eta_g 1.44→1.84、lam0 0.106→0.223、mu_gbar 符号反転 — EKI ミスフィット振動下の同定不良 #0044)で全体が悪化、(iii) JPN の tau/g 1年先比が再悪化(6.66→29.6 / 1.53→28.0。tau 行への床ノイズが疎観測変数の予測分散を通じ相互作用した可能性。確定診断は保留)。
- 構造的結論(4ラウンドの総括):
  1. **安定して PASS するもの**: 基準2(同化の短期予測情報量)と基準3(カウント予測尤度)は全ラウンド PASS(基準3 JPN は #0036 以降)。同化系の中核機能は実データで機能している。
  2. **基準1(逐次事前被覆 0.80–0.98)は、現行モデルの自由度では到達不能と判断する**。被覆を壊す変数(T_proxy, phi, v, P, THA の g/y)は「平衡構造がなく成長率・駆動パラメータ(a_T, c_T0, eta_Ty, r_phi, kappa_phiv, kappa_phitau, netgrowth 定数外挿)が全て較正対象外の固定値」であり(#0042-2)、検証窓の分布シフトに追従する自由度が EKI 較正(7 パラメータ)にも同化(13 状態 + theta_sig)にも存在しない。DA 側の対症療法(#0036 擬似観測・#0040 局所化/RTPS・#0043 床)では埋まらないことを 3 ラウンドで確認した。
  3. **THA は較正自体の同定不良が併発**(ミスフィット 20〜35 で振動、較正値がラウンド間で大きく移動)。年別カウントパターンの限界として #0034 から記録されてきたもので、単国・単窓の EKI では解けない。
- 是正の選択肢(実施判断はユーザー相談。マイルストーン範囲と凍結基準に関わるため #0039 の自律決定範囲外):
  - **(A) 駆動パラメータの L3 状態拡大**(netgrowth, T 成長率, r_phi 等を遅いランダムウォーク状態として同化で学習)。根治的で SPEC の L3 概念とも整合するが、現行機構(theta_sig 専用ハードコード)の一般化は中規模リファクタ。M8 の枠内で続けるか、M9 に併合するかの判断が必要。
  - **(B) 基準1の適用範囲の再設計**(例: 被覆対象を同化で拘束可能な変数に限定、または走行中再較正を認める walk-forward 形式へ)— **凍結基準の変更に該当し、明示のユーザー承認が必須**。
  - **(C) M8 を「基準2・3 達成、基準1 未達」の部分達成として記録し、被覆問題を M9(walk-forward)の設計に持ち込む** — 完了条件の変更に該当し、同じく明示承認が必須。
- 関連: DECISIONS #0030 / #0039 / #0042 / #0043 / #0044 / experiments/output/M8_verify_diag_{JPN,THA}.json

## [0046] M8 是正4回目: 駆動パラメータの L3 状態拡大(#0045 選択肢 A、ユーザー承認)

- 日付: 2026-07-11
- 論点: #0045 の選択肢からユーザーは **(A) L3 状態拡大で M8 継続**を選択(凍結基準 #0030-6 は不変のまま再挑戦)。#0043 の反復停止はユーザーの方向性決定により解除。設計は #0039 に基づき Fable が確定する。
- 採用(設計):
  1. **拡大パラメータ集合(4個)**: netgrowth(恒等リンク。負値可)、a_T(log リンク。T = T_proxy の成長率スケール)、r_phi(log リンク。phi の駆動係数)、mu_gbar(恒等。g の平衡目標)。選定根拠は #0042-2/#0045-2 — 被覆崩壊変数(P, T_proxy, phi, v, THA g/y)の駆動自由度をちょうど覆う最小集合(v は T 経由、y は P/T 等の合成で自動的に恩恵を受ける)。
  2. **力学**: 各拡大状態はリンク座標上の純粋ランダムウォーク(drift 0 + 微小拡散)。theta_sig の既存機構(#0010 系)の一般化として実装し、augmented ブール + 14 行決め打ちを「拡大パラメータ記述子のリスト(名前・リンク・初期値・RW sd)」に置き換える。既存の theta_sig 経路は記述子1個の特例として後方互換に保つ(E1/既存テストの記録結果を保護)。
  3. **同化との相互作用**: 拡大行は EnKF 解析の更新対象(観測から学習させるのが目的。#0040 の tauA マスキングとは独立)。smoother_vars(過去平滑化)には拡大行を含めない。スプレッド床(#0043)は観測座標のみが対象で拡大行に直接は触れない。
  4. **較正との分担**: EKI は従来どおり 7 パラメータの窓定数センターを較正(手続き不変)。拡大状態は較正センター(netgrowth は fit_exogenous 値)を初期値とし、初期スプレッドと RW sd で走行中の適応幅を規定する。
  5. **数値(RW sd・初期スプレッド)の確定手続き**: 較正窓 smoke(検証窓不参照)のみで感度確認し、検証ラン前の凍結エントリで確定する(#0030-7 の流儀)。出発点の目安: netgrowth RW sd 0.002/√年・初期 sd 0.003、log a_T / log r_phi RW sd 0.05/√年・初期 sd 0.1、mu_gbar RW sd 0.05/√年・初期 sd 0.1。
- 理由: 被覆崩壊の根因は「検証窓の分布シフトに追従する自由度の欠如」(#0045-2)であり、遅い RW による状態化は (i) 逐次同化に過去観測のみからトレンドを学習させる(検証窓データの事前参照なし)、(ii) 拡大行の事後不確実性が事前スプレッドに自然に伝播し被覆区間を拡げる、の両方を原理的に備える唯一の選択肢。SPEC の L3 概念(theta_sig 状態拡大)の自然な拡張であり、判定基準には一切触れない。
- 関連: §8.2 / DECISIONS #0010 / #0039 / #0042 / #0045

## [0047] M8 較正値と拡大設定の凍結(#0046 実装後、5回目検証ラン前)

- 日付: 2026-07-11
- 論点: #0046 の実装(コミット 0fe3665、テスト 393 本 green)後、同一 EKI 手続き(J=24・iters=4・N=100・seed 20260710、拡大力学込みの評価ラン)で再較正した。5回目検証ラン前に較正値と拡大設定を凍結する。**本エントリ以後、検証結果を見た後の変更は禁止**(#0030-7 の準用)。
- 凍結値(experiments/M8_frozen_config.toml 更新済み):
  - JPN {eta_g 0.467, delta_sig 0.121, lam0 0.0200, mu_gbar −0.869, mu_p −1.018, c_v0 4.746, ν 1.0}。ミスフィット 1.75 → 1.10(収束)。
  - THA {eta_g 1.117, delta_sig 0.0299, lam0 0.194, mu_gbar −0.540, mu_p 0.557, c_v0 4.670, ν 4.62}。ミスフィット 16.8〜21.3 — 従来の振動幅(20〜35)より縮小。拡大力学が較正窓適合を改善。
  - **拡大設定(M8_AUG_SETTINGS、#0046-5 の出発値をそのまま凍結)**: theta_sig(log、init_sd 0.5・rw_sd 0.01)、netgrowth(identity、init = fit_exogenous・init_sd 0.003・rw_sd 0.002)、a_T(log、init = §8.2・init_sd 0.1・rw_sd 0.05)、r_phi(log、init = §8.2・init_sd 0.1・rw_sd 0.05)、mu_gbar(identity、init = EKI 較正値・init_sd 0.1・rw_sd 0.05)。
- 較正窓サニティ(smoke、検証窓未参照): JPN 被覆 0.89・RMSE 9/9 PASS。THA 被覆 0.80・8/9・基準3 PASS。RW sd の追加チューニングは不要と判断(#0046-5 の感度確認手続きの完了)。
- 来歴の注記: (1) コミット規約是正(filter-branch、7687f83)により、THA 較正 JSON サイドカーの commit フィールドは書き換え前 SHA(41b1d00)を記録している。書き換え後の等価コミットは 0fe3665(ツリー同一、メッセージのみ変更)。JPN は 7687f83(docs のみの差でコード同一)。(2) 4回目までの M8 ランは #0030-5 の「L3 状態拡大 = theta_sig」を実装しておらず(augmented 未使用)、#0046 の統合で初めて設計どおりになったことを記録する。
- 関連: §0.5.8 / DECISIONS #0030 / #0044 / #0046 / experiments/M8_frozen_config.toml

## [0048] M8 5回目検証ランの公式判定: 不合格(基準1両国・基準3日本)— 反復の是非をユーザー相談へ

- 日付: 2026-07-11
- 結果(凍結設定 #0047、コミット 17a3c90 で実行): **基準1 = FAIL**(JPN 0.682 / THA 0.685。5ラウンド推移: JPN 0.485→0.470→0.545→0.545→0.682、THA 0.389→0.519→0.444→0.241→0.685)。**基準2 = PASS**(6/9, 7/9)。**基準3 = JPN FAIL**(−156.6 vs 自由 −96.9。#0044 ランでは PASS だった)**/ THA PASS**(−399.2 vs −1197.4)。基準4 = PASS(393 本)。総合 = **不合格**。
- #0046(L3 拡大)の効果検証: **方向として明確に有効** — THA T_proxy 被覆 0/4→4/4、JPN P 0.56→1.00・phi 0.22→0.56・g/y 全数ヒット、両国のプール被覆が過去最高。a_T・netgrowth・mu_gbar の事後平均は物理的に妥当な範囲で学習されている。
- 診断(残存 FAIL の局在):
  1. **JPN v(0/8、全5ラウンド不変)**: innov ≈ 0.23 が一度も改善していない唯一の変数。v の平衡目標 T + c_v0(式8)は T の成長とともに増え続けるが、観測(モバイル契約)は検証窓で飽和 — c_v0 が窓定数(EKI 較正値で凍結)のため追従不能。#0046 の拡大集合に c_v0 を含めなかったことの帰結(選定時は v を「T 経由で自動回復」と見込んだが、実際は c_v0 自体の窓間シフトが支配的)。
  2. **JPN T_proxy(0/6)**: a_T(成長率)は学習された(事後 0.0055)が、較正窓で蓄積したレベルオフセットが残存。T の解析ゲインが小さく(spread 0.070 vs sd 0.15)、7年では抜けない。
  3. **JPN 基準3の転落**: theta_sig の RW がイベント情報のほぼ無い国(較正窓カウントなし、検証窓もほぼ0)で無拘束に漂流し(事後 0.95 vs init 3.0)、λ が過大化。#0035-3 と同根で、theta_sig 拡大を JPN に適用したことの副作用(#0047 で両国一律に凍結した)。
  4. THA は tau(1点)・phi(0.29)・P(0.43)に残るが、プール 0.685 は基準まで +0.115。
- **反復継続への懸念(記録)**: 検証窓の診断を参照した手法反復は本ラウンドで5回目であり、凍結基準の証拠能力(検証窓への非適応性)は反復のたびに毀損される。改善軌道は単調でなく(whack-a-mole 的な副作用の発生)、これ以上の反復は「検証窓への手法の過適合」と区別が付きにくくなる。継続の是非は科学的完全性に関わるため、#0039 の自律決定範囲でなくユーザーに諮る。
- 選択肢(ユーザー判断): (i) 最後の1ラウンド — c_v0 の拡大追加 + JPN の theta_sig 拡大除外(いずれも #0046 と同じ設計原理の局所修正。THA は到達可能性が高いが JPN の T_proxy レベルバイアスは不確実)。(ii) #0045-(C) 部分達成として M8 を閉じ、被覆問題と反復の教訓を M9(walk-forward、走行中再較正)の設計に持ち込む。(iii) #0045-(B) 基準1の再設計(凍結基準変更、明示承認必須)。
- 関連: DECISIONS #0030 / #0043 / #0045 / #0046 / #0047 / experiments/output/M8_verify_diag_{JPN,THA}.json

## [0049] M8 最終是正ラウンド: c_v0 拡大追加 + JPN の theta_sig 拡大除外(#0048 選択肢 i、ユーザー承認)

- 日付: 2026-07-11
- 論点: #0048 の選択肢からユーザーは **(i) 最後の1ラウンドのみ実施**を選択(不合格なら (ii) 部分達成で M8 を閉じることも同時に確定)。設計は #0039 に基づき Fable が確定する。
- 採用(#0046 と同じ設計原理の局所修正2点):
  1. **c_v0 を拡大集合に追加**(identity リンク、init = EKI 較正値、init_sd 0.1・rw_sd 0.05 — mu_gbar と同格)。根拠: JPN v の被覆 0/8 と innov ≈ 0.23 は全5ラウンド不変(#0048-1)で、v の平衡目標 T + c_v0 の窓間シフト(モバイル契約の飽和)に c_v0 が窓定数のため追従できないことが原因。
  2. **JPN のみ theta_sig を拡大集合から除外**(THA は維持)。根拠: イベント情報のほぼ無い国では theta_sig の RW が無拘束漂流し λ を過大化(#0048-3、基準3の転落)。拡大は「観測から学習できる自由度」にのみ与えるという #0046-3 の原理の適用。
- 手順: 実装(テスト green)→ EKI 再較正(同一手続き)→ 較正窓 smoke サニティ → 凍結エントリ → 6回目検証 → 判定記録。**本ラウンドで不合格の場合、M8 は #0045-(C) の部分達成として閉じ、M9 設計へ移行する(ユーザー確定済み)。**
- 関連: DECISIONS #0039 / #0046 / #0048

## [0050] M8 較正値の凍結(#0049 実装後、6回目 = 最終検証ラン前)

- 日付: 2026-07-11
- 論点: #0049 の実装(コミット e954395、テスト 393 本 green)後、同一 EKI 手続き(J=24・iters=4・N=100・seed 20260710)で再較正。最終検証ラン前に凍結する。**本エントリ以後、検証結果を見た後の変更は禁止**(#0030-7 の準用)。本ランで不合格の場合、M8 は #0045-(C) の部分達成として閉じる(#0049 でユーザー確定済み)。
- 凍結値(experiments/M8_frozen_config.toml 更新済み、calib_commit e954395):
  - JPN {eta_g 0.568, delta_sig 0.123, lam0 0.0187, mu_gbar −0.741, mu_p −1.032, c_v0 4.444, ν 1.0}。ミスフィット 1.34 → 1.06(収束)。
  - THA {eta_g 0.692, delta_sig 0.0217, lam0 0.0974, mu_gbar −0.931, mu_p 0.119, c_v0 5.300, ν 4.22}。ミスフィット 15.8〜21.7(振動幅は #0044 の 20〜35 → #0047 の 16.8〜21.3 → 本ラウンドでさらに縮小傾向)。
  - 拡大設定: #0047 の凍結値に c_v0(identity、init = EKI 較正値・init_sd 0.1・rw_sd 0.05)を追加、JPN は theta_sig 除外(#0049)。他は不変。
- 較正窓サニティ(smoke、検証窓未参照): JPN 被覆 0.919・RMSE 9/9・（JPN はイベントなしのため基準3対象外)。THA 被覆 0.862・9/9・基準3 PASS。
- 関連: §0.5.8 / DECISIONS #0047 / #0049 / experiments/M8_frozen_config.toml

## [0051] M8 6回目(最終)検証ランの公式判定と M8 の部分達成クローズ

- 日付: 2026-07-11
- 結果(凍結設定 #0050、コミット 4fe456c で実行): **JPN = 全4基準 PASS(初)** — 基準1 被覆 0.894(0.80–0.98 内)/ 基準2 6/9 / 基準3 −62.7 vs 自由 −86.0 / 基準4 393 本。**THA = 基準1 FAIL(0.611)**、基準2 6/9 PASS・基準3 PASS(−398.8 vs −1079.7)。基準1 は「両国」要求のため総合 = **不合格**。6ラウンド推移: JPN 0.485→0.470→0.545→0.545→0.682→**0.894**、THA 0.389→0.519→0.444→0.241→0.685→0.611。
- 判定: #0049 で事前確定したとおり、**M8 を #0045-(C) の部分達成(基準2・3・4 は両国安定 PASS、基準1 は JPN のみ PASS)として閉じる**。これ以上の手法反復は行わない(検証窓参照 6 回に達し、過適合と区別不能になる懸念 #0048 のとおり)。
- THA 残存不合格の帰因: EKI 較正の同定不良(#0034 から一貫)。本ラウンドでも較正値が大きく移動し(mu_gbar −0.541→−0.931 等)、拡大状態が走行中に大距離を学習し直す過渡バイアスが g_swiid(1/7、innov 0.41)に集中した。単国・単窓の年別カウント EKI では解けない構造で、走行中再較正(walk-forward)を前提とする M9 の主題に持ち込む。
- M8 の成果(部分達成として記録): (1) 実データ2国の完全な同化・検証パイプライン(データ来歴・凍結手続き込み)。(2) 同化の情報量(基準2)とイベント尤度(基準3)は全6ラウンド安定 PASS — 中核機能は実証済み。(3) L3 状態拡大の汎用機構(#0046)と JPN での全基準 PASS — 被覆課題が「駆動自由度の欠如」であることの実証と処方。(4) 診断成果物(変数別被覆 γ)。(5) 教訓: 検証窓診断を見た手法反復の回数管理(#0048)、EKI 同定不良の指標(ミスフィット振動)。
- 後続(M9 への引き継ぎ): walk-forward 再較正、THA counts の同定改善(尤度設計)、tau の疎観測設計、theta_sig 拡大のイベント情報量条件。
- ブランチ処理: feature/M8-hindcast → develop へ `--no-ff` マージ(部分達成の記録として。テスト 393 本 green)。**main マージ・vM8 タグは行わない**(完了条件未達のため)。
- 関連: DECISIONS #0030 / #0045 / #0048 / #0049 / #0050 / experiments/output/M8_verify_diag_{JPN,THA}.json

## [0052] M9 設計の凍結(walk-forward × JPN/THA)

- 日付: 2026-07-11
- 論点: M8 部分達成クローズ(#0051)の引き継ぎ事項に基づく M9 の設計。#0039 により手法詳細は Fable が確定。判定基準・対象国スコープ・反復上限はユーザー承認済み(本日: **JPN+THA のみ** / **基準1 は両国要求** / **是正反復上限2回**)。第3国拡張は M9 完了後に別途設計。
- 検証形式:
  1. **Expanding-window walk-forward・年次オリジン**。JPN: オリジン t=26..33(2016〜2023、8本)、THA: t=28..33(2018〜2023、6本)。各オリジンで1年先予報を発行し翌年 [t_k, t_k+1] の観測で評価。T1=35(2024年末、ACLED エンバーゴ内)は不変。
  2. 各オリジン k で (a) 窓開始(JPN t=5 / THA t=20)〜t_k のデータ**のみ**で EKI 再較正(expanding)。初回オリジンは M8 凍結値(#0050)を prior 中心に warm-start、以後は前オリジン較正値を prior 中心に warm-start(prior sd は凍結値に復元)。(b) t_k まで同化 → (c) 1年先予報を発行。評価区間の観測は較正・同化とも未参照(疑似リアルタイム)。
  3. ヴィンテージは Phase 2.0(最新確定系列、#0023 の段階化に従う)。Phase 2.5(当時版データ)は M9 スコープ外。
  4. rolling でなく expanding を採る理由: THA の残存問題は単窓の情報量不足による EKI 同定不良(#0034〜#0050 で一貫)であり、分布シフトへの追従は L3 状態拡大(#0046)が既に担う。較正側は情報を蓄積する方が処方に合う。
- 判定基準(本エントリで凍結。変更はユーザー明示承認 + DECISIONS 必須):
  1. 全オリジンをプールした1年先予報の観測較正被覆(事前90%区間、Hamill 定義 #0017)が **0.80〜0.98(両国)**。THA の再挑戦を含む(walk-forward が THA 不合格への処方そのものであるため)。
  2. 1年先予報 RMSE が同一較正値の自由ラン比で観測変数の**過半で改善**(国別)。
  3. THA 週次カウントの1年先予測尤度が自由ラン(内生 Hawkes)比で改善。
  4. 既存テスト green。
  5. **是正反復上限 = 2回**: 検証結果を参照する手法反復は初回検証後最大2回(計3ラン)。上限到達時は結果のまま判定を確定し、以降の是正は次マイルストーン送り(#0048 の教訓の明文化)。
- サブテーマのプロトコル:
  - **カウント尤度の分散モデル再評価(#0033 の宿題)**: NegBin 等の候補比較は**較正窓データのみ**で実施し、検証前の設定凍結エントリで採否を確定する(検証窓未参照の位置に置き、是正反復カウントを消費しない)。
  - **theta_sig 拡大の適用条件**: 「初回オリジン時点の較正窓のフィルタ後イベント情報量」による事前規則とし、しきい値の数値は設定凍結エントリで確定(現状の帰結 = THA 適用・JPN 除外 #0049 の規則化)。結果を見て国別に付け外ししない。
  - **tau 疎観測**: WVS 点は該当オリジン以前なら同化に使用、評価区間内の点は評価のみ。系列取得はユーザー TODO 継続。
- 手順(#0030 と同じ流儀): 本設計凍結 → 実装(テスト green、動作確認は smoke まで)→ 較正窓サニティ → 設定凍結エントリ → 検証実行 → 判定記録。
- 関連: DECISIONS #0023 / #0030 / #0033 / #0039 / #0046 / #0048 / #0050 / #0051 / docs/PHASE2_DESIGN.md §1・§7

## [0053] M9 予報アンサンブルにおける拡大パラメータの扱い(実装レビューでの是正)

- 日付: 2026-07-11
- 論点: M9 ハーネス初期実装(489ce5b)は、1年先予報の初期アンサンブルにオリジン t_k の解析後「状態」のみを渡し、L3 拡大パラメータ(#0046)の事後値を捨てて較正定数で前進していた(M8 forecast_rmse の扱いの踏襲)。しかし M8 の基準1被覆は同化サイクルの事前 — 拡大状態を保持したまま前進したアンサンブル — で測っており、拡大が担う分布シフト追従(M8 被覆是正の決定打 #0046)が M9 予報に反映されないのは生成則の不整合。疑似リアルタイムの立場でも、t_k 時点の拡大事後は当時入手可能な最良推定であり予報に使うのが整合的。
- 決定: **予報はメンバーごとの拡大パラメータ事後値を初期値として持ち込み、予報区間中も同一 rw_sd の RW 過程を継続する**(同化サイクルの事前生成と同一の生成則。リード時間が解析間隔から1年に伸びるだけ)。基準2・3 の自由ラン基準は従来どおり較正定数・拡大なし(比較対象の定義は不変)。
- 補足: 本是正は初回検証ラン前(検証窓未参照)の実装レビューによるもので、#0052-基準5 の是正反復カウントは消費しない。
- 関連: DECISIONS #0046 / #0052

## [0054] カウント尤度の分散モデル評価プロトコル(#0033 の宿題、較正窓のみ)

- 日付: 2026-07-11
- 論点: #0033 で M9 送りとした NegBin 等の分散モデル再評価。#0052 のとおり較正窓データのみで採否を決め、是正反復カウントを消費しない位置で行う。選定基準を実行前に登録する(#0039)。
- 候補: (a) 現行 = 複合ポアソン近似 + 1/ν テンパリング(ν* = ΣN/ΣΛ プロファイル、ベースライン)。(b) 負の二項(NegBin)尤度 — 分散パラメータは ν と同様に較正窓プロファイル最尤で決め、EKI 較正はしない。
- 評価: THA 較正窓(t=20〜28)のみ(JPN は較正窓カウント0件で対象外)。M8 凍結設定(N=100)の較正窓同化ランで、(i) カウント観測の逐次1ステップ先予測 log スコア、(ii) カウント更新時の ESS(最小・中央値)を比較。
- 採否規則(事前凍結): **NegBin を採用するのは (i) の log スコアが改善し、かつ (ii) の ESS 中央値がベースラインの 0.8 倍以上の場合のみ**。それ以外(改善が曖昧・ESS 劣化)はベースライン維持(確立済みの単純な方を優先)。判定結果は設定凍結エントリに記録する。
- 関連: DECISIONS #0033 / #0039 / #0052

## [0055] M9 設定凍結(NegBin 採用・theta_sig 規則・ラン設定)

- 日付: 2026-07-12
- 論点: #0052 手順の設定凍結。**本エントリ以後、検証結果を見た後の設定変更は禁止**(#0030-7 の準用)。是正は #0052-基準5 の上限2回の枠内でのみ行い、各回 DECISIONS に事前記録する。
- **カウント尤度 = NegBin 採用(#0054 の事前規則により確定、蒸し返さない)**: THA 較正窓評価(t=20〜28、M8 凍結設定 N=100)で (i) カウント観測の逐次1ステップ先予測 log スコア −794 → −678(+116 改善)、(ii) ESS 中央値比 0.856 ≥ 0.8 — 採否規則の両条件を満たし採用。留意事項(記録): ESS 最小は 17.5 → 13.0 に低下、再抽選回数は 15 → 23 回に増加(中央値は健全だが裾の粒子劣化はやや悪化)。較正窓プロファイル r̂ = 0.577。評価成果物: experiments/output/M9_negbin_eval_THA.json。
- **walk-forward への統合**(コミット 346f762): 各オリジンで予備同化ラン(現行ポアソン + 1/ν)の週次 Λ̄ から ν* = ΣN/ΣΛ(下限1、#0031)→ r̂(src/weights.jl negbin_profile_r、log r 空間黄金分割)の順に**窓内データのみ**で再プロファイルし、本同化ランを NegBin(素の pmf、テンパリングなし、同一シード)で再実行する。基準3のスコアリングも完全 log pmf の NegBin に統一し、予報ラン・自由ランの両方に当該オリジンの同一 (ν*, r̂) を共通適用。較正窓カウントゼロの国(JPN)は予備ランが本ランを兼ね r = Inf(実質ポアソン)にフォールバック。smoke 実測プロファイル例: THA t=28 で ν*=4.17・r̂=0.472、t=29 で ν*=2.51・r̂=1.10(縮小設定での動作確認値)。
- **theta_sig 適用規則(#0052 サブテーマのしきい値確定)**: 初回オリジン較正窓のフィルタ後 ΣN ≥ 50 のとき theta_sig を拡大集合に含め、**全オリジンで固定**(結果を見た付け外しをしない)。実測: JPN ΣN=0 → 除外、THA ΣN=618 → 適用 — #0049 の帰結をデータ規則として再現。
- **ラン設定(凍結)**: N=100、EKI J=24・iters=4・N_eki=100、ベースシード 20260711(オリジン別 seed = base + t_k)。warm-start: 初回オリジン = M8 凍結値 #0050 を prior 中心、以後 = 前オリジン較正値を prior 中心(prior sd は凍結値に復元)。オリジン JPN t=26..33(8本)/ THA t=28..33(6本)、窓開始 JPN t=5 / THA t=20、T1=35、1年先予報。
- **smoke 使用履歴の透明化**: 開発中の動作確認 smoke(各国先頭2オリジン・N=40・J=12・iters=2)は先頭2オリジンの検証区間(予報評価)に触れている。これは縮小設定での完走・ログ・成果物形式の確認であり、判定材料・設定選択の根拠としない。#0053 の是正は smoke 実行前に生成則の整合性(原理)から決定したもので smoke 結果を参照していない。NegBin 採否(#0054)は較正窓のみの評価で確定しており検証窓未参照。
- テスト 463 本 green(コミット 346f762 時点)。
- 関連: DECISIONS #0031 / #0033 / #0049 / #0050 / #0052 / #0053 / #0054

## [0056] M9 初回検証ランの公式判定(不合格)と診断

- 日付: 2026-07-12
- 結果(凍結設定 #0055、コミット 03b7d53 で実行。成果物 experiments/output/M9_walkforward_{JPN,THA}.json):
  - **JPN = 基準1〜3 PASS**: 被覆 0.885(54/61、0.80〜0.98 内)/ RMSE 改善 7/9 / カウント予測 logL 予報 −71.7 vs 自由 −123.4。M8 で6ラウンドを要した合格水準が walk-forward の8オリジンで維持された。
  - **THA = 基準1 FAIL(被覆 0.660 = 31/47)**、基準2 PASS(7/9)、基準3 PASS(−479.8 vs −483.7、僅差)。M8 最終の 0.611 から +0.05。
  - 基準4 PASS(463 本 green)。基準1 の「両国」要求により総合 = **不合格**。是正反復は #0052-基準5 の上限2回、本エントリ時点で残り2回。
- 運用上の記録: 初回起動は r̂=Inf の JSON シリアライズ欠陥(03b7d53 で修正、手法変更なし)で JPN 完走後にクラッシュし再実行した。再実行 JPN の判定数値はクラッシュ前ランと同一(シード固定の再現性確認を兼ねる)。
- 診断(THA 基準1 の局在):
  1. **後半オリジン集中**: オリジン別被覆 t=28..33 で 0.67 / 1.00 / 0.75 / 0.50 / 0.43 / 0.57。前半2本は良好で、劣化はオリジンを重ねるほど進行。t=32 では自由ラン(0.71)が予報(0.43)を上回る逆転。
  2. **較正ドリフト**: lam0 0.129→1.439(約11倍、単調増)、eta_g 1.22→2.90、mu_gbar は t=33 で符号反転(−0.59→+0.43)。EKI 平均ミスフィットは 17〜42 で単調収束せず(#0034 以来の THA 同定不良が walk-forward でも残存)。ドリフト加速の時期が被覆悪化(t=31..33)と符合。
  3. **粒子劣化の進行**: 再抽選 20→30 回(オリジン単調増)、ESS 最小 14〜19(前半)。JPN(再抽選 7〜18)より一貫して悪い。
  4. 同化は機能している: プール予報被覆 0.660 は自由ラン 0.553 を上回る(+0.11)。ただし改善幅は JPN(+0.26)の半分未満。
  5. **診断ギャップ**: 変数別の予報被覆 hit/n・アンサンブル区間幅が M9 成果物に未保存で、FAIL の変数帰属(M8 #0051 では g_swiid/tau/phi に集中)が今回データから断定できない。
- **決定(是正前の診断計装ラン)**: 是正1回目を設計する前に、変数別被覆 hit/n・オリジン別の変数別アンサンブル区間幅を JSON に保存する計装を追加し、**THA のみ同一設定・同一シード(#0055 不変)で再ラン**して診断粒度を確保する。手法・設定・乱数の変更が一切なく判定数値は同一に再現される(プール数値の一致で検証)ため、#0052-基準5 の是正反復カウントは**消費しない**(ログ再解析と同格の情報抽出)。是正1回目の設計は計装ラン の変数別診断を見て #0057 として事前記録する。
- 関連: DECISIONS #0034 / #0051 / #0052 / #0055 / experiments/output/M9_walkforward_run.log

## [0057] M9 是正1回目: 再抽選後の Liu-West 若返り(スプレッド崩壊の是正)

- 日付: 2026-07-12
- 位置づけ: #0052-基準5 の**是正反復1回目**(残り1回)。#0039 により Fable が設計を確定し実装前に記録する。凍結基準・#0055 の凍結値(シード・N・EKI 設定・NegBin・theta_sig 規則)は変更しない。
- 診断計装ラン(#0056 の決定。THA のみ・同一シード、プール判定数値 0.6595/−479.8 が初回と完全一致 = 計装の無害性確認)による変数帰属:
  - **P 3/6**(後半3オリジン連続ミス): 予報90%区間幅 0.020〜0.036 に対し観測ノイズのみの幅 ≈ 0.016(sd 0.005)— **アンサンブルスプレッドがほぼ消滅**。バイアス自体は小(|0.01〜0.04|)。自由ランは P 6/6(広い区間)。
  - g_swiid 2/6: 全オリジン一方向の正バイアス(+0.25〜+0.66、M8 #0051 から残存する構造バイアス)。区間幅は 0.4〜2.2 でバイアスと拮抗。
  - phi 3/6・v 4/6・w 5/6・y 4/6: 幅とバイアスが拮抗する境界的ミスが後半に増加。tau 0/1(疎観測、バイアス +3.45)。
- 機構: src/assimilation.jl の系統再抽選(カウント窓 ESS 低下時)は複製粒子への摂動(若返り)を行わない。拡散の小さい遅い変数(P 等)は複製後に再多様化せず、THA の再抽選 20〜30 回/オリジンでスプレッドが単調に崩壊する。後半オリジンほど窓が長く再抽選回数が累積するため、被覆悪化の後半集中(0.50/0.43/0.57)と整合。JPN が PASS するのは再抽選頻度が低いため。
- **決定(是正内容)**: 系統再抽選の直後に **Liu-West 縮小核による若返り**を追加する: 再抽選前の重み付きアンサンブル平均 x̄ と座標別標準偏差 σ を用い、再抽選後の各粒子を x′ = a·x + (1−a)·x̄ + √(1−a²)·σ·ε(ε〜N(0,I)、保持座標・全行 = 状態+拡大)で置換する(1次・2次モーメント保存)。EnKS ラグ窓スナップショットは現行どおりインデックス対応のみ(過去断面への遡及ジッタはしない)。
- **縮小係数 a の事前選定プロトコル(検証窓未参照、#0054 と同格)**: 候補 a ∈ {0.90, 0.95, 0.99}。THA 初回オリジン較正窓(t=20..28)で M8 凍結較正値(#0050)を用いた同化ラン(N=100、NegBin ν*・r̂ は窓内プロファイル)を各候補で1本ずつ実行し、**窓内の事前90%被覆(#0050 サニティと同定義)が名目 0.90 に最も近い a を採用**。同点は大きい a(介入の小さい方)。選定値は実装コミットに記録し、以後変更しない。JPN は同一機構を適用するが選定は THA 窓のみで行う(JPN は再抽選が希少で感度なし)。
- 期待とリスク(記録): P の3ミス回復 + 境界的ミスの過半回復で被覆 0.66 → 0.79〜0.85 と見込む(g_swiid の構造バイアスは本是正の対象外 — 残存すれば是正2回目の主題)。基準3(現状 +3.8 の僅差 PASS)は同化経路の変更で変動しうる — 悪化して FAIL に転じた場合もそのまま記録する(基準は動かさない)。
- 手順: 実装(テスト green)→ a 選定ラン(較正窓のみ)→ smoke → 検証ラン2回目(JPN+THA、#0055 のシード・設定)→ 公式判定。
- 関連: DECISIONS #0011 / #0050 / #0052 / #0054 / #0055 / #0056

## [0058] M9 検証ラン2回目(是正1回目適用後)の公式判定(不合格、1ヒット差)

- 日付: 2026-07-13
- 結果(#0055 凍結設定 + #0057 若返り a=0.95、コミット bf67242 で実行):
  - JPN: 基準1 被覆 0.836(51/61)PASS / 基準2 7/9 PASS / 基準3 −86.6 vs 自由 −169.7 PASS。若返りで 0.885→0.836 と帯中央側へ移動(上限 0.98 への上振れ懸念は不発)。
  - THA: **基準1 被覆 0.787(37/47)FAIL** — 合格下限 0.80 に**1ヒット差**(38/47 = 0.808 で PASS だった)。基準2 7/9 PASS / 基準3 −494.2 vs −499.0 PASS。
  - 基準4 PASS(480 本 green、bf67242)。総合 = **不合格**。是正反復は残り1回。
- 是正1回目(#0057)の効果検証: **機構仮説どおり有効** — THA 被覆 0.660→0.787(+6ヒット)。スプレッド崩壊系のミスはほぼ解消(P 3/6→5/6、phi 3/6→5/6、v 4/6→6/6、w 5/6→6/6。P の残る1ミスは t=33 の境界的1件)。基準3 も PASS 維持(+4.8)。
- 残存 FAIL の帰属(変数別診断): ミス10件中 **g_swiid が4件**(2/6。バイアス +0.61〜+0.69 が全オリジンでほぼ一定の一方向、区間幅 0.5〜0.9 で吸収不能)。他は tau 1件(疎観測1点、バイアス +3.7)、初回オリジン t=28 の T_proxy/y 各1件、phi/P 各1件(境界的)。**g_swiid の正バイアス(モデルの g がSWIID 観測水準を系統的に下回る)は M8 #0051(innov 0.41、1/7)から一貫する THA 固有の構造バイアス**で、mu_gbar の EKI 較正・L3 拡大でも吸収されていない。
- 関連: DECISIONS #0051 / #0055 / #0056 / #0057

## [0059] M9 是正2回目(最終): g_swiid 観測バイアス状態の L3 拡大

- 日付: 2026-07-13
- 位置づけ: #0052-基準5 の**是正反復2回目 = 最後の1回**。本ランの結果で M9 の判定を確定する(以降の是正は次マイルストーン送り)。#0039 により Fable が設計を確定し実装前に記録する。凍結基準・#0055 凍結値は変更しない。
- 診断(#0058): 残存 FAIL は g_swiid の全オリジン一定の正バイアス(+0.6)に集約。同化は毎解析で innovation を部分補正するが、予報区間中に力学が g をモデル平衡水準へ引き戻すため1年先で再現する。JPN の g_swiid は 6/6(バイアスなし)であり、THA の SWIID 系列水準とモデル g 状態の対応にのみ系統オフセットがある。
- **決定**: 観測演算子側の加法バイアス状態 **b_g(logit 座標、h_g(x) = logit(g) + b_g)を L3 拡大集合に追加**する(バイアス考慮同化の標準形)。init = 0、init_sd = 0.1、rw_sd = 0.05(mu_gbar と同格、#0049 の慣例)。両国一律に適用(g_swiid が観測される国でのみ学習可能な自由度であり、バイアスの無い JPN では 0 近傍に留まると期待される — #0046-3「観測から学習できる自由度にのみ与える」と整合)。予報は #0053 どおりメンバーごとの b_g 事後を持ち込み RW 継続。基準2・3 の自由ラン基準は従来どおり拡大なし(比較対象の定義不変)。
- 設計理由: (i) 力学側(mu_gbar)の拡大・較正では2ラウンドとも吸収されなかった水準対応の問題であり、観測写像の自由度として学習するのが直接的。(ii) EKI 較正集合は変更しない(同定不良 #0034 を悪化させない)。(iii) 期待効果は g_swiid 4ミスの過半回復(+2 で合格域 38/47 に到達)。
- リスク(記録): b_g が実信号(g の真の変動)を吸収して g 状態の解釈性が下がる可能性 — M9 では被覆・RMSE・尤度の凍結基準で判定し、b_g 事後の大きさと安定性を診断として記録する。基準3・JPN 被覆(0.836)への影響は小さいと見込むが、悪化してもそのまま記録する。
- 手順: 実装(テスト green)→ THA smoke サニティ → 検証ラン3回目(最終、#0055 のシード・設定)→ 公式判定の確定。
- 関連: DECISIONS #0034 / #0046 / #0049 / #0053 / #0057 / #0058

## [0060] M9 検証ラン3回目(最終)の公式判定 — 是正上限到達により判定確定

- 日付: 2026-07-13
- 結果(#0055 凍結設定 + #0057 若返り + #0059 b_g、コミット b6fa8bc で実行):
  - JPN: 基準1 被覆 0.869(53/61)PASS / 基準2 7/9 PASS。(参考: JPN カウント logL は 予報 −106.8 vs 自由 −105.0 — 基準3 は #0052 で **THA 固有**のため判定対象外。JPN のカウントは疎で本指標はラン実現ごとに大きく振れる: 3ラン推移 −71.7/−123.4 → −86.6/−169.7 → −106.8/−105.0)
  - THA: **基準1 被覆 0.702(33/47)FAIL** / 基準2 7/9 PASS / 基準3 −486.7 vs −493.9 PASS。
  - 基準4 PASS(486 本 green、b6fa8bc)。
  - **総合 = 不合格。#0052-基準5 の是正上限(2回)到達により本判定で M9 の検証結果を確定する。以降の是正は次マイルストーン送り。**
- 是正2回目(#0059 b_g)の効果検証: **THA では不発、むしろ悪化**。THA 被覆は 0.787(run2)→ 0.702。b_g 事後は −0.95〜+0.56 とオリジン間で不安定に迷走し、診断した一定バイアス +0.6 を学習しなかった(g_swiid 2/6 のまま)。機構解釈: THA では週次カウント再重み付け・再抽選(+若返りジッタ)がアンサンブルを支配し、年1回の g_swiid 解析でしか情報を得られない b_g が偽相関で流される。smoke(短窓・縮小設定)では +0.72 を学習しており、窓が長くカウント更新が多いほど b_g の同定が壊れる。一方 **JPN では設計どおり機能**(g_swiid 4/6→6/6、b_g は 0 近傍で安定、被覆 0.836→0.869)。#0046-3 の原理の裏返しとして「観測から学習できる自由度」の判定はノイズ環境依存であることの実証。なお run2→run3 の THA 悪化分の一部は拡大行追加による乱数実現の変化を含む(p/w/y/T_proxy の境界的ミスの入れ替わり)。
- 3ラン総括(THA 被覆): 0.660 → 0.787(#0057 若返り、+6ヒット、機構仮説どおり)→ 0.702(#0059、不発)。**最良構成は run2(#0057 まで、b_g なし)の 0.787** で、合格下限まで1ヒット差。
- 記録(運用): run3 の JPN 所要時間 25191 秒(run2 の 7074 秒から約3.6倍)— b_g 追加後の JPN で再抽選頻度が増えた可能性。診断は M10 送り。
- 未決(ユーザー判断待ち): M9 のクローズ形態(部分達成の扱い)、#0059 実装(b6fa8bc)を revert して最良構成 run2 をブランチ最終状態とするか、develop マージの要否。
- 関連: DECISIONS #0052 / #0055 / #0057 / #0058 / #0059 / experiments/output/M9_walkforward_{JPN,THA}.json(run3)・*_run{1,2}.json(退避)

## [0061] M9 の部分達成クローズ(ユーザー承認、2026-07-13)

- 日付: 2026-07-13
- 論点: #0060 で確定した不合格(THA 基準1のみ未達、是正上限到達)を受けたクローズ形態。ユーザーは選択肢から **「#0059 実装を revert して部分達成クローズ」** を選択。
- 決定:
  1. b6fa8bc(b_g 観測バイアス拡大)を revert し、**実証済み最良構成 = run2(#0055 凍結設定 + #0057 Liu-West 若返り a=0.95、THA 被覆 0.787・JPN 全基準 PASS)**をブランチ最終状態とする。判定は #0060 の不合格のまま動かさない。b_g の知見(JPN で有効・カウント支配環境では同定不能)は #0060 が一次資料。
  2. feature/M9-walkforward → develop へ `--no-ff` マージ(部分達成の記録として)。**main マージ・vM9 タグは行わない**(完了条件未達)。
- M9 の成果(部分達成として記録): (1) expanding-window walk-forward 検証基盤(オリジン別 EKI warm-start・NegBin 窓内プロファイル・変数別被覆診断)。(2) **Liu-West 若返り(#0057)** — スプレッド崩壊の機構診断から処方まで一貫し、THA 被覆 0.660→0.787(+6ヒット)・JPN 全基準 PASS 維持。(3) NegBin カウント尤度(#0054、較正窓プロトコルで採用)。(4) 検証窓未参照の診断計装プロトコル(#0056、同一シード再ランによる無害性実証)。(5) 教訓: 拡大自由度の同定可能性は観測頻度とノイズ環境に依存(#0060)。
- M10 への引き継ぎ候補: THA g_swiid 構造バイアス(残存 +0.6、力学側 or 前処理側の対処)、tau 疎観測設計(WVS 系列はユーザー TODO 継続)、run3 で観測された JPN 実行時間 3.6 倍化の診断、EKI 同定不良(lam0/eta_g ドリフト)の正則化。
- 関連: DECISIONS #0051 / #0052 / #0057 / #0059 / #0060

## [0062] M10 設計の凍結(g_swiid アンカリング prior × EKI 正則化)

- 日付: 2026-07-13
- 論点: M9 引き継ぎ(#0061)からの M10 設計。スコープはユーザー確認済み(本日): **THA g_swiid 構造バイアス是正 + EKI 同定不良の正則化**の2点。tau/WVS はユーザー TODO 継続で スコープ外、run3 実行時間 3.6 倍化(#0060)は smoke で観察し必要時のみ診断(独立作業項目としない)。手法詳細は #0039 により Fable が確定し本エントリで事前記録する。
- 診断の統合(設計の根拠): 2つの残存問題は同根と診断する。g の力学には国別平衡項が既に較正パラメータ `mu_gbar` として存在するが、EKI の目的関数が週次カウント尤度に支配されるため mu_gbar が誤同定され(t=33 で符号反転 −0.59→+0.43、#0056 診断2)、モデル g が観測水準を一定 +0.6(logit)下回り続ける(#0058)。また lam0 のオリジン間単調 11 倍ドリフトは「前オリジン事後を次 prior 中心にする warm-start 連鎖」がドリフト蓄積チャネルになっている(#0055 の設定)。フィルタ内自由度(b_g)での是正はカウント再重み付け支配下で同定不能と実証済み(#0060)— 較正 prior は解析サイクルの再重み付けに流されないため、この機構を回避できる。
- 決定(手法):
  1. **mu_gbar のデータ情報 prior(アンカリング)**: 各オリジンの EKI で mu_gbar の prior 中心を「当該オリジン較正窓内の g_swiid 観測の logit 平均」に係留する(warm-start の対象から除外)。力学式・観測演算子は不変。両国一律の規則として適用(バイアスの無い JPN では凍結較正値と近い中心になり中立と期待)。
  2. **EKI prior 中心の係留(warm-start 連鎖の廃止)**: 全オリジンで prior 中心 = M8 凍結値 #0050(mu_gbar のみ規則1)とし、前オリジン事後への連鎖 warm-start を廃止する。ドリフト蓄積チャネルを構造的に除去し、情報蓄積は expanding window(#0052-4 の採用理由)が担う。
  3. **prior sd の数値は設定凍結エントリで確定**: mu_gbar のアンカリング prior sd は候補 {0.1, 0.2, 0.3} を **THA 初回オリジン較正窓のみ**(t=20..28、検証窓未参照)の事前選定プロトコル(#0054/#0057 と同格)で選ぶ。選定指標: 較正後同化ランの窓内 g_swiid 事前平均バイアス(絶対値最小)、同点は窓内事前90%被覆が名目 0.90 に近い方、なお同点は大きい sd(介入の小さい方)。他パラメータの prior sd は現行 0.5 を維持(変更しない)。
- 検証形式・判定基準: **#0052 と同一**(expanding-window walk-forward、オリジン JPN t=26..33 / THA t=28..33、Phase 2.0、基準1〜5 の定義・数値・「基準1 両国要求」・是正反復上限2回を含め全て #0052 のユーザー承認済み基準を M10 に再凍結)。基準の新設・変更はない。M9 実証済み構成(#0055 NegBin・theta_sig 規則・#0057 若返り a=0.95、#0053 予報生成則)は不変の土台として引き継ぐ。
- 手順(#0030/#0052 と同じ流儀): 本設計凍結 → 実装(テスト green、動作確認は smoke まで)→ prior sd 事前選定 + 較正窓サニティ → 設定凍結エントリ → 検証実行 → 判定記録。
- 関連: DECISIONS #0039 / #0050 / #0052 / #0055 / #0056 / #0057 / #0058 / #0060 / #0061

## [0063] M10 設定凍結(mu_gbar prior sd = 0.3・ラン設定)

- 日付: 2026-07-13
- 論点: #0062 手順の設定凍結。**本エントリ以後、検証結果を見た後の設定変更は禁止**(#0030-7 準用)。是正は #0052-基準5(M10 に再凍結)の上限2回の枠内でのみ行い、各回事前記録する。
- **mu_gbar アンカリング prior sd = 0.3(#0062 の事前選定プロトコルにより確定、蒸し返さない)**: THA 初回オリジン(t=28)較正窓 t=20..28 のみのフル設定評価(N=100・J=24・iters=4、experiments/output/M10_prior_select.json、3候補 46分)。窓内 g_swiid(n=8)の事前平均バイアス: sd=0.1 → −0.597、sd=0.2 → −0.515、**sd=0.3 → −0.421(絶対値最小)**。同点なしのため第2規則(被覆)以降は不使用。参考記録: 選定構成の窓内事前90%被覆 0.500、mu_gbar 事後 −0.398(アンカー −0.427 近傍で安定、M8 凍結値 −0.931 から観測水準側へ移動)、lam0 事後 0.366。アンカリングにより窓内バイアスは M9 診断の ≈0.6 から 0.42 に縮小(部分是正。残余は検証で判定)。
- **ラン設定(凍結)**: #0055 と同一 — N=100、EKI J=24・iters=4・N_eki=100、ベースシード 20260711(オリジン別 seed = base + t_k)、NegBin 窓内プロファイル、theta_sig ΣN≥50 規則、若返り a=0.95(#0057)、予報生成則 #0053。オリジン JPN t=26..33 / THA t=28..33、窓開始 JPN t=5 / THA t=20、T1=35、1年先予報。シードを M9 と同一に保つのは prior 規則(#0062)の効果を M9 run2 と同一乱数系で分離比較するため。
- **prior 規則(#0062 の実装確定)**: 全オリジンで prior 中心 = M8 凍結値 #0050(warm-start 連鎖なし)、mu_gbar のみ prior 中心 = 当該オリジン較正窓 g_swiid の logit 平均(sd 0.3)、他パラメータの prior sd = 0.5。実装 = experiments/M10_walkforward.jl(コミット a2c49fc)。
- **smoke 使用履歴の透明化**(#0055 と同格): 動作確認 smoke(THA 先頭2オリジン・N=40・J=12・iters=2・sd=0.2)は先頭2オリジンの検証区間に触れているが、完走・出力形式の確認のみで判定材料・設定選択の根拠としない。sd 選定は較正窓のみのフル設定評価(上記)で確定しており検証窓未参照。
- 関連: DECISIONS #0030 / #0050 / #0053 / #0055 / #0057 / #0062

## [0064] M10 検証ラン1回目クラッシュの診断と是正(prior 妥当性リサンプリング・シード準拠)

- 日付: 2026-07-13
- 論点: M10 検証ラン1回目(2026-07-13 13:12 起動)が JPN オリジン t=28 の EKI iter 1 で「有効ラン 8/24 < しきい値 12」(M8_calibrate.jl の既存ガード)により停止。**判定数値(プール被覆等)は未産出であり公式判定は存在しない**。
- 診断(再現スクリプトで24本全数調査、失敗16本を完全再現):
  1. 失敗は全て `ArgumentError: stable regime requires De < 1`(src/parameters.jl の**静的**パラメータ検証。SPEC §8.4)。forward 実行前に落ちており、同化・観測・カウント尤度は無関係。ACLED JPN カバレッジ開始(t=28)との一致は偶然で関与なしを確認(t1=28.0 では有効カウント窓 0 のまま)。
  2. 分離変数は **delta_sig(De の分母)**: 失敗16本は全て delta_sig ≤ 0.084、成功8本は全て ≥ 0.115。log 空間 sd 0.5・中心 0.123(M8 凍結値)の下方ドローが De>1 域に落ちる確率が構造的に 3〜4 割ある。M9 では warm-start 連鎖が delta_sig 中心を 30〜50% 上方(0.16〜0.19)へ動かし偶然隠蔽していた — #0062 の warm-start 廃止で顕在化。オリジン間の 19→16→8 の単調悪化はシード差による二項ばらつきで、t=28 は閾値を割った抽選。
  3. 付随バグ: M10_walkforward.jl のベースシード既定値が 20260713 になっており、#0063 凍結値 **20260711** と不一致(M9 は 311 行で 20260711)。ラン1回目はこの誤ったシードで走っていた。
- 決定(是正2点、#0039 により Fable 確定・事前記録):
  1. **prior 初期ドローの静的妥当性リサンプリング**: EKI の初期アンサンブル H の各列について、パラメータ構築の静的チェック(fit_exogenous → ModelParameters、forward 実行なしで安価)が通るまで同一 prior から再ドロー(上限 200 回/列、超過はエラー)。統計的正当化: 既存実装は無効列を棄却して残りで更新する = 事前は事実上 De<1 許容領域への**切断分布**として機能していた。本是正は同じ切断を J 本維持のまま初期化時に行うだけで、凍結済みの sd・prior 中心・J・iters の**値は一切変えない**(#0063 不変)。EKI 更新後(iter 2 以降)の無効列の扱い(棄却+補充)は従来どおり。M8/M9 経路にも安全な一般化だが、既定は M10 経路のみ有効化し後方互換を保つ。
  2. **シード既定値の修正**: M10_walkforward.jl の既定を 20260711 に修正(#0063 への準拠バグ修正)。
- **是正反復カウント(#0052-基準5)は消費しない**: ランは判定数値を産出せずクラッシュしており、本是正はクラッシュ機構の除去と凍結値準拠のみで、予報性能の観測結果に基づく手法変更を含まない(#0053 の検証前実装是正と同格)。透明化: クラッシュログには JPN t=26/27 のオリジン別数値(被覆 0.875/0.875 等)が含まれ Fable はこれを目にしたが、是正内容の選択には使用していない。
- 手順: 実装(テスト green)→ JPN 窓 (5,28) J=24 で全列有効の静的確認(フル EKI 不要)→ 検証ラン再起動(正シード)。
- 関連: DECISIONS #0039 / #0053 / #0055 / #0062 / #0063 / SPEC §8.4

## [0065] M10 初回検証ラン(run2)の公式判定(不合格)と診断

- 日付: 2026-07-13
- 結果(コミット cb370a3、シード 20260711、成果物 experiments/output/M10_walkforward_{JPN,THA}.json):
  - **JPN: 全基準 PASS** — 基準1 被覆 0.885(54/61)、基準2 RMSE 改善 7/9、基準3 logL −78.9 vs 自由 −93.0。**g_swiid 6/6**(M9 run2 の 4/6 から改善 — アンカリング prior が設計どおり機能)。
  - **THA: 基準1 FAIL** — 被覆 0.766(36/47、合格下限 0.80)。基準2 PASS(7/9)、基準3 PASS(−497.9 vs −504.3)。
  - 基準4 PASS(480 本 green)。基準1 の「両国」要求により総合 = **不合格**。是正反復は残り2回(#0064 は非消費)。
- 診断:
  1. **EKI 正則化(#0062 規則2)は目的達成**: THA の mu_gbar 事後はアンカー近傍で安定(−0.41〜−0.72、M9 の符号反転消滅)、lam0 は 0.09〜0.24 に留まりオリジン間単調ドリフト(M9 で 11 倍)が消滅。
  2. **THA g_swiid は 1/6 に悪化**(M9 run2 は 2/6)。オリジン別の予報中心バイアス +0.41〜+0.92(全オリジン一方向)、区間幅 0.51〜0.94 で吸収不能。予報 RMSE 0.64 に対し**自由ラン 0.27** — 平衡(アンカー済み mu_gbar)近傍に留まる自由ランの方が観測に近い。
  3. 機構仮説: バイアスは平衡パラメータでなく**同化窓中にフィルタ状態へ蓄積**する。予報区間には週次更新が無いので、予報バイアスはオリジン時点の解析状態の低バイアスをほぼそのまま反映。週次カウント再重み付けが g→応力→強度の結合(eta_g 載荷)経由で低 g 粒子を系統的に選好し、年1回の g_swiid 解析では是正しきれない(#0060 で実証した「カウント更新支配」の状態版)。カウント情報が僅少な JPN が 6/6 で無縁なことは対照群として仮説と整合。
- **決定(是正前の診断計装ラン、#0056 と同格・是正カウント非消費)**: THA のみ同一設定・同一シードで、同化窓内のアンサンブル g(平均・sd)を各解析ステップ前後で時系列記録する計装を追加し再ラン。判別したい二択: (a) 年次 g_swiid 解析は g を観測近傍まで是正するが週次更新で再蓄積する(→ 是正方向: 週次再重み付けからの g 保護/解析頻度側)、(b) 年次解析自体が弱く未是正(スプレッド不足 or R 過大 → 是正方向: 解析時の g スプレッド膨張/R 設計)。手法・設定・乱数の変更なしで判定数値は同一に再現される(プール数値一致で無害性検証)。是正1回目の設計は計装結果を見て次エントリとして事前記録する。
- 関連: DECISIONS #0052 / #0056 / #0060 / #0062 / #0063 / #0064

## [0066] g 計装ランの結果 — 予報窓バイアス仮説への転換と第2計装(非消費)

- 日付: 2026-07-13
- 計装ラン結果(コミット 0fef057、THA 同一設定・同一シード、プール数値は #0065 と完全一致 = 無害性検証済み。成果物 experiments/output/M10_instrument_g_THA_t{28..33}.json):
  1. **#0065 診断3の前提(解析状態の低バイアス持ち越し)は棄却**: 窓末尾 t_k のフィルタ g 平均と観測の差は平均 +0.035・符号不定で、予報バイアス +0.41〜+0.92 を説明しない(説明率は平均 −10.7%)。
  2. 窓内は「週次カウント再重み付けで急落(絶対値合計 25.7、2010 ACLED 開始直後の1区間だけで −9.2)→ 年次 g_swiid 解析が乖離の約87%を是正(移動合計 +20.5)」の拮抗で、正味持ち越しはほぼゼロ。年次解析の強度は設計どおり(実測ゲイン 0.593 ≈ 理論 0.575)、g スプレッドも枯渇していない(#0065 の機構 (b)(c) は主犯性なし)。
  3. 帰結: **予報バイアスは予報窓(1年間の自由伝播、観測更新なし)の中で発生する**。自由ラン(較正定数・拡大なし、g_swiid RMSE 0.27・4/6)は g を引き下げないのに、ほぼ無バイアスの解析状態から出発する予報アンサンブル(RMSE 0.64・1/6)だけが下がる — 両者の力学の違いは**メンバー別 L3 拡大パラメータ事後 + RW 継続(#0053)のみ**。よって週次カウント再重み付けが拡大パラメータ(mu_gbar が第一容疑 — #0060 で b_g に実証した「カウント支配下の年次自由度の迷走」と同型。ほか a_T・netgrowth・r_phi・c_v0・theta_sig)の事後を歪め、予報中の力学(平均回帰先・載荷)を下方に引くと推定。
- **決定(第2計装、#0056/#0065 と同格・是正カウント非消費)**: 同化窓内と**予報窓内**の両方で、各拡大パラメータのメンバー平均・sd の時系列と予報窓の g 平均軌道を記録する計装を追加し、THA のみ同一設定・同一シードで再ラン。プール数値一致で無害性検証。主犯パラメータの確定後、是正1回目を次エントリとして事前記録する(有力候補: 当該拡大自由度の THA での扱い変更 — #0060 の教訓「拡大自由度の同定可能性は観測頻度・ノイズ環境依存」の適用)。
- 関連: DECISIONS #0046 / #0053 / #0056 / #0060 / #0065

## [0067] 第2計装ランの結果 — L3 拡大パラメータ仮説の棄却とジャンプバースト仮説・第3計装(非消費)

- 日付: 2026-07-14
- 第2計装ラン結果(コミット ad786d5、THA 同一設定・同一シード、プール判定 0.766 (36/47)・基準2 7/9・基準3 −497.9 で #0065 と一致 = 無害性検証済み。成果物 experiments/output/M10_instrument_g_THA_t{28..33}.json の g_diag_forecast / aug):
  1. **#0066 の主犯仮説(L3 拡大パラメータ事後の歪みが予報力学を下方に引く)は棄却**: 予報窓の g バイアスは緩慢な単調ドリフトではなく、**予報開始後 8〜16 週の「初期バースト」で年間バイアスの 87〜139% が発生**し、残り10ヶ月余はほぼ平坦(一部は逆に回復)。mu_gbar 事後の含意する平衡方向と実際のドリフト方向の符号整合は 3/6(偶然水準)。t=28 は mu_gbar 事後(+0.264)が現在 g より上方なのに g が低下。kappa_g = 0.1/yr(非拡大・固定)では 8 週で 0.2〜0.9 logit の移動は力学的に不可能。予報窓中の拡大パラメータ RW 分散膨張も僅少(mu_gbar sd 0.12→0.14)で寄与しない。
  2. mu_gbar 事後のオリジン間振れ(−0.68〜+0.26、アンカー −0.427・M8 凍結値 −0.931 の周囲)は同定不良の**症状**であり、バーストの原因ではない(mu_gbar 事後とバースト量の相関 r=0.96 は、紛争の重いオリジンほど (a) 拡大事後が極端に引かれ (b) 窓末尾の λ_e が高い、という共通原因による交絡と解釈)。
  3. **新仮説(ジャンプバースト)**: 同化窓(実紛争データ + 強制ジャンプ 15 週。強制ジャンプも apply_jump! 経由で λ_e を励起 — src/assimilation.jl:546)で高まった Hawkes 自己励起状態 **λ_e(IX_LAME)が予報アンサンブルへそのまま持ち越され**(#0053 の全状態継続)、予報開始直後に内生ジャンプのバーストを発火、各ジャンプが g −= c_g·m(c_g = 0.5、src/jumps.jl:65)で g を圧縮する。λ_e の減衰率 β = 5/yr(時定数 ≈ 10 週)は観測されたバースト完了時期と一致。g_sd のバースト同時刻での離散的増加(t=28: 0.274→0.319/2週)も連続拡散でなくジャンプ事象と整合。#0035-2(M8 期)で記録済みの機構の再来 — 当時は点推定 mu_gbar の平均回帰が自由ランを支えたが、予報アンサンブルは λ_e をデータ由来の高水準から開始する点が自由ランと異なる(自由ラン RMSE 0.27〜0.38 vs 予報 0.49〜0.64 の逆転も整合)。
- **決定(第3計装、#0056/#0065/#0066 と同格・是正カウント非消費)**: --instrument-g を拡張し、オリジン別に (i) 同化窓末尾と予報窓中(週次)の λ_e アンサンブル統計(平均・sd・分位)、(ii) 予報窓中のメンバー別内生ジャンプ発生数と累積 c_g·m(g 変化のジャンプ寄与 vs ドリフト寄与の分解)、(iii) 同化窓内の強制ジャンプ時刻(t_k との近接)を記録し、THA のみ同一設定・同一シードで再ラン。プール数値一致で無害性検証。ジャンプ寄与がバーストの主因と確認された場合、是正1回目(λ_e 持ち越し/ジャンプ→g 結合の予報時の扱い)を次エントリとして事前記録する。
- 関連: DECISIONS #0035 / #0052 / #0053 / #0056 / #0065 / #0066

## [0068] M10 是正1回目: 予報窓の内生 Γ ジャンプへの超過確率シンニング(是正カウント消費、残り1回)

- 日付: 2026-07-14
- 第3計装ラン結果(コミット a0f9f43、THA 同一設定・同一シード、プール 0.766 (36/47)・7/9・−497.9 で #0065 と一致 = 無害性検証済み):
  1. **ジャンプ寄与が予報 g バイアスを定量的に完全説明**: オリジン別の年間 Δg(logit)に対しメンバー平均 Σc_g·m の説明率 109〜175%(平均 123%)。残差は全オリジンで正 = ドリフト項(kappa_g の mu_gbar への緩和)はむしろ押し戻し方向。ジャンプ→g 圧縮が唯一の実質的下押し機序と確定。
  2. **ただし「λ_e 持ち越し」は主因でない(#0067 仮説の修正)**: λ_e(t_k) は t28 のみ 55.4(直前 0.067y に強制ジャンプ)、t29〜t33 は実質ゼロ(≤0.33)なのにバーストは t28 より大きい。λ_e は予報窓内で自己生成される(開始2週間で数百規模へ)。
  3. **真の機構は強度キャップ張り付き**: 同化窓でカウント尤度(Λ = ∫intensity、lam_b = lam0·exp(theta_sig·(σ_s−1)+…)— src/jumps.jl:23)が σ_s を載荷し、予報開始と同時に内生 Hawkes が lam ≈ lam_bar = 50/yr で発火(実測 n_jumps 48〜50/年/メンバー、ほぼ全メンバー一様 = 中心シフト)。各発火が全量の Γ(apply_jump!)を適用し、蓄積された σ_s を数週間で g へ放出(σ_s 枯渇後は m→0 でプラトー)。
  4. **不整合の根源**: 同化窓では Γ ジャンプは規模閾値(較正窓 98% 分位、s_w = 死者+0.1×件数 ≥ 7.3)超えの週のみ強制発火(THA t33 窓で 15 週 ≈ 1.2回/年)なのに、予報窓は全ての内生発火(≈50回/年 — カウント再現には必要な水準)を Γ 級破局として扱う。約40倍の頻度不整合。
- **決定(是正1回目、手法変更・凍結基準 #0052 は不変更)**: 予報アンサンブル(forecast_ensemble → simulate_sde_augmented)の内生ジャンプについて、**Γ 適用(apply_jump!)を超過確率 p_ex でシンニング**する。
  - p_ex = (同化窓内の強制ジャンプ週数)/(同化窓内のカウントデータ週数)。オリジン・国ごとにデータから決定論的に算出(自由パラメータなし)。THA t33 なら ≈ 15/678 ≈ 0.022 → 予報 Γ 率 ≈ 0.022 × 50 ≈ 1.1回/年 = 同化窓の強制ジャンプ気候値と自己整合。
  - カウントデータ週が 0 の場合(JPN の t_k = 28 等)は p_ex = 1(現行動作、当該国の lam_b は微小で実質差なし)。
  - 実装は thinning の採択確率への乗算(乱数消費列を変えない形)。既定 p_ex = 1 で現行と bitwise 同一(後方互換)。
  - **自由ラン(比較基線)・同化力学・EKI・判定基準は一切変更しない**。カウント予測 logL(基準3)の Λ は intensity の積分のままで、σ_s 非枯渇 + λ_e 非励起でもキャップ近傍のため影響は小さい見込み(結果で確認)。
  - 論拠: §6.4 の内生/外生二重モードの整合性回復。同化側の「Γ = 閾値超え規模の週のみ」という運用(#0026/#0030 のイベント抽出規則)を予報側にも適用するもので、モデル定数(c_g、lam_bar、β 等)・較正値には触れない。
- 検証: 実装後テスト green + 凍結設定(#0063、シード 20260711、mu_gbar sd 0.3)で JPN+THA フル検証ラン → #0052 基準で判定。これで**是正反復1回を消費(残り1回)**。
- 関連: DECISIONS #0026 / #0030 / #0052 / #0053 / #0062 / #0063 / #0065 / #0066 / #0067

## [0069] M10 検証ラン(run3)の公式判定 — 合格

- 日付: 2026-07-14
- 結果(コミット 1a6ca51、シード 20260711、mu_gbar prior sd 0.3、成果物 experiments/output/M10_walkforward_{JPN,THA}.json、ログ experiments/output/M10_run3_verify.log):
  - **JPN: 全基準 PASS** — 基準1 被覆 0.885(54/61)、基準2 RMSE 改善 7/9、基準3 カウント logL 予報 −48.3 vs 自由 −93.0。g_swiid 6/6。
  - **THA: 全基準 PASS** — 基準1 被覆 **0.851**(40/47、run2 の 0.766 から +0.085、凍結バンド [0.8, 0.98] 内)、基準2 8/9、基準3 −434.2 vs −504.3。**g_swiid 1/6 → 6/6**(#0065 で特定した予報 g バイアスが解消)。
  - フルテスト 2510/2510 green(実装コミット 1a6ca51 時点 + 本判定時再実行)。
  - **総合 = 合格**。是正反復は #0068 の1回のみ消費(残り1回は未使用のままクローズ)。凍結基準 #0052 は不変更のまま判定。
- 所見:
  1. p_ex 実測: THA 0.0215〜0.0275(6オリジン、強制ジャンプ週 11〜15 / カウント週 400〜650)。JPN は t26〜28 が 1.0(カウント週 0 → 設計どおり no-op)、t29〜33 が 0.0(カウント週あり・強制ジャンプ週 0 → 予報 Γ 全抑制)。
  2. **#0068 で警戒した基準3の悪化(smoke で logL −174→−331)はプール判定では顕在化せず**、むしろ改善: THA run2 −497.9 → run3 −434.2、JPN −78.9 → −48.3。予報 Γ 抑制は λ_e 自己励起による Λ 過大分を除く方向に働いた(smoke の劣化は較正窓 2 オリジンのみの局所評価による偏り)。
  3. THA 最終オリジンの予報 g RMSE 0.134 vs 自由 0.381 — #0065 の逆転(予報 0.64 > 自由 0.27)が解消し、予報が自由ランを明確に上回る本来の序列に復帰。
  4. 被覆の副作用(g 区間幅縮小による他変数ヒット減、smoke で懸念)もプールでは非顕在: THA は v 5/6・phi 5/6・y 4/6 を維持し、FAIL は tau 0/1・T_proxy 2/4 の少数点変数のみ。
- 手続き: #0039 に基づき、合格認定・develop / main マージ・vM10 タグ付与は本エントリ記録後に実施し、ユーザーへ事後報告する。
- 関連: DECISIONS #0039 / #0052 / #0062 / #0063 / #0065 / #0068

## [0070] Phase 3 スコープ凍結: M11〜M13(予報の運用化と可視化)+ Issue ベース開発体制

- 日付: 2026-07-14(ユーザー承認同日)
- 経緯: M10 合格クローズ(#0069)を受け、展望提案書 docs/PHASE3_PROPOSAL.md を作成・提示。ユーザー方針「課題の幅広い積み上げでなく、横展開可能な全体像(データ→較正→検証→予報→可視化)の完成を優先。定型作業は委譲し Fable は設計・判定に専念」を反映し、ユーザーが M11〜M13 の設計を承認。
- 決定(スコープ凍結。詳細プロトコルは各マイルストーン着手時に別途凍結 — #0045/#0052/#0062 の流儀):
  1. **M11 予報パイプラインの一般化 + 真の将来予報**: 国コード引数の単一パイプライン(データ取得→較正→walk-forward 自己検証→将来予報 JSON、ホライズン 30 年・来歴サイドカー付き)。JPN/THA 再実行 walk-forward が M10 凍結基準(#0052)の要求帯内であることを回帰条件とする(基準変更なし)。
  2. **M12 予報円ダッシュボード**: 自己完結静的 HTML(外部依存なし、データ・来歴埋め込み、Julia で決定的再生成)+ GitHub Pages 公開。観測照合済み 9 系列 + カウント予報の予報円。**検証済みホライズン(6 年)と外挿領域を視覚的に区別**し、外挿は不確実性の広がり + 無次元診断(De レジーム等)として提示する — 検証範囲外を「予報」と呼ばない誠実さの線引き。
  3. **M13 横展開**: 候補国調査(ACLED×V-Dem×SWIID×世銀交差)→ 国固有前処理の診断(THA 深南部 #0026 相当の判断は DECISIONS 記録)→ パイプライン実行 → M10 基準帯内で合格 → ダッシュボード追加。是正上限 2 回の流儀を踏襲。
  4. **開発体制を Issue ベースへ転換**(ユーザー提案採用): Issue = 自己完結委譲プロンプト(目的・対象ファイル・完了条件・報告形式)。ラベル = マイルストーン × 必要能力(fable-required / sonnet-ok / good-first-issue)。外部協力者はフォーク + PR。受入判定は Fable がローカル再検証。マージ規約不変。
  5. 深化課題(tau EnKS、y 四半期化、GDP ヴィンテージ、theta_p #0010、EKI 正則化 #0061、GDELT、k・tauA #0019)はバックログとし、必要時に個別マイルストーン化。
- 事実訂正の記録: 「tau=WVS はユーザーTODO」(#0052/#0061 以来の記載)は**完了済み**(2026-07-10 ユーザー転記、experiments/data/raw/{JPN,THA}_tau.csv + 来歴メタ、同化使用中)。残課題はデータ取得でなく疎観測(約 5 年間隔)の同化設計(EnKS)。
- 関連: DECISIONS #0010 / #0019 / #0026 / #0039 / #0051 / #0052 / #0061 / #0062 / #0069。設計本文 docs/PHASE3_DESIGN.md、提案の経緯 docs/PHASE3_PROPOSAL.md
## [0071] M11 将来予報モードの p_ex 規則 — JPN λ_e カスケードのアーティファクト判定と 0/0 フォールバックの定義

- 日付: 2026-07-16(Issue #4 実装前の事前凍結。ユーザー承認: 2026-07-16)
- 経緯: M11 30年ホライズンスモーク(Issue #2 / PR #7)で、JPN の30年予報に Hawkes 自己励起カスケードを観測(内生ジャンプ 100メンバー合計 53,124 発、λ_e の 5-95% 分位幅が自然座標 0→437、tame ガード発火 58,704 回。THA は 626 発で良性)。数値破綻はなし(NaN/Inf ゼロ、ガード #0032 は設計どおり)。
- 機構の事実確認(コード根拠付き。サブエージェント調査 2026-07-16):
  1. **p_ex シンニングはジャンプ発生そのものを間引く**: src/integrator.jl の Ogata thinning 採択確率に gamma_thinning_p が乗算され、棄却候補は apply_jump! を呼ばない。apply_jump!(src/jumps.jl)は Γ 適用と λ_e 自己励起加算を同一関数内で不可分に実行するため、間引かれたジャンプは自己励起にも寄与しない。**#0068 の決定文言「Γ 適用をシンニング」は実装挙動(発生ごと間引き)と表現上の差異があるが、M10 合格判定(#0069)は実装挙動に対して行われたため実装を規範とする**(文言の明確化であり実装・判定の変更ではない)。
  2. **λ_e 状態には上限がない**: intensity() は min(lam_bar, λ_b + λ_e) で採択レートのみキャップ(50/yr)。λ_e 自体は無キャップ加算 + 指数減衰のみ。JPN の λ_e=437 はキャップと矛盾しない設計どおりの挙動で、発火は実質飽和レートで継続していた。
  3. **JPN の p_ex=1.0 の由来**: スモークの同化窓 t=0〜26(1990〜2016)は ACLED JPN カバレッジ(2018〜)より前でカウント週ゼロ → #0068 のフォールバック p_ex=1.0(間引きなし)が発動。#0068 がフォールバックを正当化した前提「(1年先予報では)当該国の lam_b は微小で実質差なし」は、自己励起が複利成長する30年ホライズンでは成立しない。
- 判定(Fable): **JPN の λ_e カスケードは力学的に支持されないフォールバック・アーティファクト**。根拠: (i) 観測記録上 JPN はいかなる窓でもジャンプ候補週ゼロ(2018〜2021 で17イベント、閾値 5.0 超なし)であり、30年で5万発の政治的騒乱ジャンプは経験的裏付けを欠く。(ii) フォールバックの成立前提が長期ホライズンで破綻(上記 3)。(iii) #0068 の分子(強制ジャンプ週数)がゼロの国は、式そのものが p_ex=0 を与える設計であり、0/0 のみ 1.0 に跳ぶのは規則内部の不整合。
- 決定(将来予報モード = Issue #4 の凍結規則):
  1. p_ex は #0068 の式(強制ジャンプ週数/カウント週数)を**その予報ランの同化窓**で計算する。真の将来予報では同化窓が現在まで延びるため、JPN も ACLED 2018〜のカウント週を持ち、式が自然に評価される(強制ジャンプ週ゼロなら p_ex=0 — #0069 所見1の t29〜33 と同挙動)。
  2. **カウント週ゼロ(0/0)のフォールバックは 1.0 でなく 0.0** とする(ジャンプ証拠皆無の国で予報 Γ ジャンプを発生させない)。発動時は予報 JSON の来歴に `p_ex_fallback: "no_count_data"` を必ず記録し、ダッシュボード(M12)で「ジャンプリスク評価不能」と区別表示できるようにする。
  3. **#0068 自体(M10 凍結)は不変更**: 回帰 walk-forward(Issue #6)は従来どおり #0068 のフォールバック 1.0 のまま実行する(凍結基準・凍結規則は動かさない。実害も M10 検証の 6 年ホライズンでは #0069 所見1のとおり顕在化しない)。
  4. 本規則は将来予報モードという新規スコープの設計だが、フォールバック値の相違という形式上の分岐を含むため、実装前にユーザーの明示承認を得た(本エントリの承認日付が記録)。
- 関連: DECISIONS #0032 / #0039 / #0053 / #0068 / #0069 / #0070。実装先 Issue #4、成果物 experiments/output/M11_horizon_smoke_{JPN,THA}.json(PR #7)

## [0072] M11 回帰 walk-forward(Issue #6)の公式判定 — 合格、M11 クローズ

- 日付: 2026-07-17
- 位置づけ: Phase 3 スコープ(#0070)の M11 クローズ条件 = パイプライン再編(Issue #3〜#5)後の JPN/THA フル walk-forward が M10 凍結基準(#0052、#0062 で M10 に再凍結)の要求帯内であることの回帰確認。基準の変更・解釈の緩和はない。
- 実行: ユーザー合意済み分担(2026-07-16)に基づく委譲実行(友人側 Fable)。develop 先端 2d671a5・凍結設定(#0063、シード 20260711、mu_gbar prior sd 0.3)・forecast_pipeline.jl(--from obs --to walkforward、-t 8)。数値報告は 2026-07-17 にユーザー経由で転記。成果物 + 来歴サイドカーは実行者側環境に保存(判定数値は下記のとおり決定的に再現されるため、公式成果物は #0069 のものを維持)。
- 結果(比較基準 = #0069 公式値、コミット 1a6ca51):
  - **JPN: 全基準 PASS** — 基準1 被覆 0.885(54/61)、基準2 RMSE 改善 7/9。(参考: カウント logL 予報 −48.28 vs 自由 −92.99 — 基準3 は #0052 で THA 固有)。#0069 と小数点以下まで一致。
  - **THA: 全基準 PASS** — 基準1 被覆 0.851(40/47)、基準2 8/9、基準3 logL 予報 −434.23 vs 自由 −504.33。#0069 と小数点以下まで一致。
  - 変数別・オリジン別の内訳も #0069 所見と一致(THA: g_swiid 6/6、FAIL は tau 0/1・T_proxy 2/4 の少数点変数のみ)。
  - 基準4: フルテスト 2510/2510 green(実行者側 2d671a5 + オーナー側本判定時再実行)。
  - **総合 = 合格。パイプライン再編は M10 合格構成を保存しており、M11 をクローズする**(#0039 に基づき Fable 判定・事後報告)。
- 判定根拠に関する決定(#0039、本判定の前提として記録):
  1. **オーナー側での再ランは実施しない**: (a) 凍結シードによる決定的再現が両国とも小数点以下まで成立、(b) PR #10 レビュー時にオーナー側で JPN 通し再現済み(#0069 完全一致、再生成 JSON は来歴除き完全一致)、(c) (a)(b) により判定数値の環境非依存性が二重に確認済み。追加ラン(2〜2.5 h/国)は情報を増やさない。
  2. 実行者ローカルに残っていた 7/16 産 THA walk-forward ファイル(被覆 39/47=0.830、logL −411.7 — いずれも合格帯内だが #0069 と不一致)は、Issue #5 検証中の開発途中作業ツリーによる非標準条件ランの産物と判定する。本ランがクリーンな develop 先端で #0069 を正確に再現したため、凍結シードの決定的再現性は保たれている。公式比較基準は #0069 のまま(実行者側で experiments/output/issue6_baseline_backup/ に退避済み)。
- 手続き: 本エントリ記録後、Issue #6 クローズ → develop→main --no-ff マージ → vM11 タグ → ユーザーへ事後報告(#0039)。
- 関連: DECISIONS #0039 / #0052 / #0062 / #0063 / #0069 / #0070 / #0071。Issue #6・PR #7〜#10、docs/PHASE3_DESIGN.md §2

## [0073] M12 予報円ダッシュボード設計凍結

- 日付: 2026-07-18(実装着手前の事前凍結。#0039 に基づく Fable 自律決定 — 凍結基準・しきい値の変更を含まない)
- 経緯: M11 クローズ(#0072)を受け、Phase 3 スコープ(#0070)の M12 に着手。#0070 の凍結骨子(自己完結静的 HTML、Pages 公開、検証済み/外挿の視覚区別)を実装可能な仕様に詳細化した。設計本文 = **docs/M12_DASHBOARD_DESIGN.md**(本エントリと同時コミット。以後の実装 Issue の規範)。
- ユーザー確認事項(2026-07-18、公開面に関わるため個別確認): (i) UI 言語 = 日本語主体 + 英語変数名、(ii) 公開方式 = main ブランチ /docs 配下(`docs/dashboard/index.html`、Pages ソース = main /docs。docs/ の md も同時閲覧可能になる点を含めて承認)。
- 決定(方式選定は Fable 判断):
  1. **生成器 = experiments/M12_dashboard.jl 1本**。テンプレート(HTML/CSS/JS)をスクリプト内に保持し、入力 JSON(experiments/output/M11_forecast_{ISO3}.json)をバイト列のまま埋め込む。描画はクライアント側 vanilla JS の SVG とする(Julia 側事前描画は不採用 — 国切替・ツールチップに JS が必要な以上、描画ロジックを 1 箇所に集約する方が保守と決定性検証が単純)。
  2. **決定性規約**: 同一入力 → バイト同一 HTML。生成器内の now()・乱数・環境依存値の出力を禁止し、日時等はすべて入力 JSON の provenance から転記。埋め込みは再シリアライズせずバイト忠実(キー順・浮動小数表現の揺れを排除)。生成 HTML は git コミット(来歴内蔵)。
  3. **義務要件の実装形**: (a) 検証済み/外挿は境界縦線 + 外挿領域の背景ウォッシュ + 帯不透明度 0.55 倍の 3 チャネル冗長で区別し、外挿を「予報」と表記しない(#0070)。(b) `p_ex_fallback == "no_count_data"` は count_forecast パネルに警告バッジ(⚠ アイコン + テキスト併記、色のみ禁止)— 現行 JPN/THA では非発動だが実装・テスト必須(#0071)。
  4. **チャート・色**: 全パネル単一色相(青 #2a78d6/#3987e5)のファンチャート(q05–q95 / q25–q75 / q50)、検証済みパレット準拠、ライト/ダーク両対応、各パネルに表ビュー(JS 無効時の代替)。
  5. **テスト**: 縮約フィクスチャ JSON(fallback 発動国を含む)による生成成功・決定性(2 回生成バイト一致)・義務要件マーカー(バッジがデータ駆動であることを含む)・埋め込み忠実性の 4 系統を Julia テストに追加。
  6. **Issue 分解**: A = 生成器骨格 + 埋め込み + 来歴パネル + フィクスチャテスト(sonnet-ok)、B = ファンチャート描画 + 義務要件 (a)(b) + 国切替(sonnet-ok、A に依存)、C = Pages 設定 + 凍結完了条件(#0070 の 3 条件)に対する受入判定(fable-required)。
  7. スコープ外: 観測実績の重ね描き、国別比較ビュー、スキーマ変更(必要が生じたら別エントリ)。
- 関連: DECISIONS #0039 / #0070 / #0071 / #0072。設計本文 docs/M12_DASHBOARD_DESIGN.md、スキーマ契約 docs/FORECAST_JSON.md、上位設計 docs/PHASE3_DESIGN.md §3

## [0074] M12 設計の自己レビュー是正(#0073 の修正)

- 日付: 2026-07-18(ユーザー指示による自己レビュー → 修正方針の明示承認同日)
- 経緯: #0073 凍結直後にユーザー指示で設計の自己レビューを実施。SPEC §3・src/coordinates.jl・fetch_data.jl との突合(サブエージェント検証)で事実誤り 1 系統、設計内部の不整合 3 件、プロセス上の懸念 1 件を検出した。義務要件 (a)(b) の実装形・決定性規約・バイト忠実埋め込み・色仕様・スキーマ整合(count_forecast の配列長、p_ex_fallback の格納場所)・ユーザー確認事項の記録は問題なしを確認済み。
- 是正内容(docs/M12_DASHBOARD_DESIGN.md を同時改訂):
  1. **変数ラベルの是正(事実誤り)**: §3-3 の日本語ラベルのうち w(誤: 労働分配率相当 → 正: **生産年齢人口比率**)、phi(誤: 政治参加 → 正: **技術普及率**)、p(誤: 政治的自由 → 正: **政治的分極度**)の 3 件を訂正。tau は制度信頼(社会的信頼でなく)、v は情報伝播速度(SPEC 正典用語)に精密化。「9 実系列」の呼称も「9 系列 = 8 状態変数 + y(合成観測)」に是正。ラベルは SPEC §3 の正典用語に固定し、実装時の独自言い換えを禁止。
  2. **責務分割の明確化(内部矛盾の解消)**: 「生成器は描画ロジックを持たない」を改め、パネル見出し・警告バッジ・表ビュー・来歴・免責は**生成器がデータ駆動の静的 HTML として出力**、クライアント JS は SVG チャート描画とインタラクションのみ。これにより義務要件マーカーのテスト(バッジが非発動国の HTML に存在しないこと)と JS 無効環境の可読性が同時に成立する。
  3. **テスト配置の是正(既存流儀との整合)**: メインスイート追加をやめ、experiments 依存テストの確立済みパターン(test_m8_augmentation.jl / test_m10_prior.jl)に従い **experiments/test_m12_dashboard.jl のスタンドアロン**とする。フィクスチャは experiments/test_fixtures/、国コードは架空 ISO3。受入判定にスタンドアロンテスト green を追加。
  4. **テスタビリティの明記**: 生成コアを「入力 JSON パス配列 → HTML 文字列」の純関数として公開し CLI は薄いラッパ(テストがフィクスチャパスで直接呼べる)。
  5. **成果物プロセスの変更(Issue A/B)**: 実データ HTML(docs/dashboard/index.html)の生成・コミットは実装者でなく**オーナーが PR レビュー時に正典 JSON(2026-07-17 生成)から実施**。実装者はフィクスチャで完結(provenance の commit SHA・generated_at の環境揺れ防止、約 30 分/国の再生成負担の排除)。
  6. 軽微: Pages 設定時に docs/.nojekyll を配置(Issue C 作業メモ)。
- 不変事項: #0070 の凍結完了条件・義務要件、#0073 の決定 1〜4(生成器 1 本・決定性・埋め込み・色)・ユーザー確認事項(UI 言語・Pages 方式)は変更なし。凍結基準・しきい値には触れない。
- 手続き: 本エントリと設計書改訂を同時コミット後、Issue #11/#12/#13 の本文を gh api で更新。
- 関連: DECISIONS #0039 / #0070 / #0071 / #0073。Issue #11・#12・#13、docs/SPEC.md §3、src/coordinates.jl

## [0075] M12 受入判定 — 合格、M12 クローズ(Issue #13)

- 日付: 2026-07-18(#0039 に基づく Fable 判定・事後報告)
- 位置づけ: Phase 3 スコープ(#0070)の M12 クローズ条件 = 凍結完了条件 3 項目 + 義務要件 2 件(設計 #0073/#0074、規範 docs/M12_DASHBOARD_DESIGN.md §7)に対する受入判定。基準の変更・緩和はない。
- 実装の来歴: Issue #11(生成器骨格、Sonnet 委譲 4c980e3 + レビュー fixup 3c593c7)、Issue #12(ファンチャート描画、Sonnet 委譲 5d5b42f)、Issue #13(公開資材 ee1b32a、Fable 実施)。公開 HTML は正典 JSON(experiments/output/M11_forecast_JPN.json = 2026-07-17 産 commit d289ce7 / M11_forecast_THA.json = 2026-07-16 産 commit cdef313、いずれも seed 20260711)から生成(#0074 決定 5)。
- 判定結果(§7 の 5 項目、全 PASS):
  1. **凍結完了条件(#0070)**: (1) JPN/THA の予報円が国切替つきで表示(データスクリプト 2 本・タブ・チャート JS を DOM 検査 + フィクスチャ実表示のユーザー確認 2026-07-18)、(2) 来歴が UI で確認できる(seed 20260711・commit・generated_at 等 14 項目の来歴パネル)、(3) **GitHub Pages URL で閲覧可**: https://aray-99.github.io/mirai-yohou/dashboard/ (HTTP 200、配信バイト列 = コミット済み index.html と完全一致を curl + cmp で確認)
  2. **義務要件**: (a) 検証済み(〜2031)/外挿の視覚区別 — data-until-calendar-year="2031" ×2、境界マーカー・clipPath 分割・不透明度 0.55 倍・「検証済み | 外挿」ラベル(テストでデータ駆動性を検査)。(b) p_ex_fallback バッジ — 実データ両国は null で非表示(正常系)、発動系はフィクスチャ BBB で表示・非発動系で文字列ごと不在をテスト検証
  3. **決定性**: 正典 JSON から 2 回生成しバイト一致(公開前 + 配信後照合)
  4. **テスト**: フル 2510/2510 green + スタンドアロン 17/17 green
  5. **目視**: フィクスチャ版 Artifact プレビューをユーザーが確認(2026-07-18)。指摘 3 件は Issue #14(言語トグル)/#15(軸文字可読性)/#16(デザイン刷新方針)として起票済み — ユーザー方針「デザイン改善は完成後に漸進で可」により**非ブロッキング**と判定(完了条件・義務要件はデザイン品質と独立)
- 手続きの記録: Pages 配信には main 反映が必要なため、公開資材の main マージ(030117f)→ Pages 有効化(ソース main /docs)→ URL 検証 → 本エントリ記録 → 最終 develop→main マージ + vM12 タグ、の 2 段マージとした(いずれも M12 完了処理の一部)。
- フォローアップ(M12 スコープ外、起票済み): Issue #14/#15(sonnet-ok、友人委譲可)、#16(fable-required、デザインシステム導入 or デザイン特化エージェント刷新の比較検討)。#14/#15 マージ後の公開 HTML 再生成・再コミットはオーナーが実施(#0074 決定 5)。
- 関連: DECISIONS #0039 / #0070 / #0071 / #0073 / #0074。Issue #11〜#16、docs/M12_DASHBOARD_DESIGN.md

## [0076] 友人委譲の自動化 — GitHub Actions + @claude メンション体制

- 日付: 2026-07-18(ユーザー承認済み計画に基づく事前記録、#0039)
- 経緯: これまで友人(GitHub: Rodan-n、Claude Pro/Max 契約)への実装委譲は「ユーザーがメッセージアプリでプロンプトを転送し、友人が手動で自分の Claude Code に投入する」方式だった(M11 の PR #7〜#10 実績)。人間の中継が毎回必要で非効率のため、ユーザーの発案によりオーケストレーター(Fable)が GitHub 経由で友人側 Claude リソースへ直接タスクを割り振る体制へ移行する。
- 決定内容:
  1. **方式**: 公式 claude-code-action@v1 を採用。友人が `claude setup-token` で発行した OAuth トークン(Pro/Max サブスクリプション対応は公式 docs/setup.md で確認済み)をリポジトリ secrets `CLAUDE_CODE_OAUTH_TOKEN` に登録し、Issue/PR コメントへの `@claude` メンションで**友人のクォータで動く Claude が GitHub Actions 上で自動起動**し、ブランチ作成 → PR 作成まで行う。トークンは友人の同意に基づき提供され、友人はいつでも失効(再発行・revoke)できる。
  2. **起動条件の制限(クォータ保護)**: 公開リポジトリのため、workflow の `if:` 条件で起動を `aray-99` / `Rodan-n` の発話に限定し、第三者コメントによる友人クォータの消費を防ぐ。加えて timeout-minutes と concurrency 制限を設定。secrets はフォーク由来 PR には渡らない(GitHub 標準挙動)。
  3. **main 反映の例外**: `issue_comment` 系トリガーはデフォルトブランチ(main)上の workflow 定義を実行する GitHub 仕様のため、`.github/workflows/claude.yml` を含む本コミット群はマイルストーン完了時以外だが develop→main マージを行う(現時点で develop = main = 605d542 であり、差分はインフラ・ドキュメントのみ。実験成果物・凍結基準に非干渉)。#0039 に基づき事後報告。
  4. **定常フロー**: ユーザー → Fable に依頼 → Fable が自己完結プロンプトの Issue を起票し `@claude` メンション → 友人クォータの Action が実装・PR 作成 → ユーザーが GitHub 通知で検知 → Fable が確立済み手順(checkout → diff 精査 → テスト自走 → --no-ff マージ)でレビュー。**PR の自動監視ループは回さない**(ユーザー決定、Pro クォータ節約)。
  5. **初回実地テスト**: Issue #15(軸文字サイズ、sonnet-ok)を用いる。従来方式で友人に依頼済み想定のため、着手状況をユーザー経由で確認の上で実施。
- 不変事項: レビュー・マージ権限はオーナー側のまま(凍結ファイル即差し戻し、公開 HTML 再生成はオーナー実施 = #0074 決定 5)。凍結基準・しきい値には触れない。Issue 本文を自己完結プロンプトとする起票流儀は維持。
- 関連: DECISIONS #0039 / #0070 / #0075。Issue #14 / #15、.github/workflows/claude.yml、CLAUDE.md 運用節

## [0077] M13 プロトコル凍結 — 対象国 KOR + TUR、横展開サイクル

- 日付: 2026-07-18(候補国調査の結果に基づく選定、ユーザー確認済み)
- 経緯: PHASE3_DESIGN §4 の手順 1(候補国調査)を Sonnet 委譲で実施(10 カ国: KOR/IDN/PHL/MYS/IND/BRA/MEX/TUR/ZAF/EGY。ACLED カバレッジ開始年は公式表で全国確認、WVS Wave 6/7 参加・SWIID・V-Dem・WB 系列・慢性地域紛争構造を調査)。Fable 推奨 = KOR + TUR をユーザーが選択(2026-07-18)。
- 選定理由:
  - **KOR**: JPN 型(低カウント安定民主)の複製で「安定国はそのまま通る」ことを最小コストで実証。WVS Wave 6/7 両参加、地域紛争なし = exclude_admin1 不要、ACLED 2018〜(JPN と同条件)。
  - **TUR**: PKK 紛争が南東部県に地理的に限定され、タイ深南部(#0026/#0030)と最も構造が近い exclude_admin1 適用例。「除外設計と較正の枠組みが新レジーム(競争的権威主義)へ一般化する」ことを実証。WVS 両波参加、ACLED 2016〜。
  - 見送り: IND(紛争が複数系統で除外設計が最難)、BRA/MEX(全国拡散型の都市犯罪・カルテル暴力で県除外の枠組み不適合。MEX は ACLED 自身がアクター特定困難を明記)、ZAF(WVS Wave 7 参加未確認 = tau 欠測リスク)、EGY(有力だが 3 カ国目はパイプラインが 2 カ国で通ってからの追加が安全 — 計算・レビューコスト考慮)、IDN/PHL/MYS(上位候補への優位性なし)。
- プロトコル(§4 のサイクルを KOR/TUR に具体化。凍結):
  1. **国設定 + データ整備**: countries/KOR.toml・TUR.toml を作成し、確立済みスクリプト(fetch_data.jl / extract_vdem_p.jl / extract_swiid_g.jl)でデータ取得・来歴付きコミット。ACLED 認証が必要なためローカル実行のみ。tau(WVS E069_11)は確立済み形式(<ISO3>_tau.csv、year,value)で転記 — 唯一の手作業。
  2. **紛争構造診断(TUR)**: ACLED 生データで南東部県の死者シェアを一次集計し、exclude_admin1 と regime 分類を Fable が診断 → DECISIONS 記録 + ユーザー報告(THA 85% 分析と同じ流儀)。KOR は事前調査で除外不要見込みだが、同じ集計で確認する。
  3. **較正 + 検証**: 凍結済みハイパーパラメータの流儀(#0063: シード 20260711、mu_gbar prior sd=0.3 等の M10 凍結構成)で国別 EKI 較正 → walk-forward 検証。
  4. **判定**: M10 凍結基準の要求帯(#0052 基準セット)で国別に合否判定。不合格時の是正上限 2 回、上限到達時は部分達成クローズ判断をユーザーへ(#0070 凍結済み)。基準・しきい値の変更はしない。
  5. **ダッシュボード追加**: 合格国を M12 生成器で追加(正典 JSON 生成・コミットはオーナー、#0074 決定 5)。#14/#15(友人委譲中)とはファイル衝突しない順序で実施。
- 完了条件(#0070 から不変): 新国の walk-forward が M10 凍結基準の要求帯内 + ダッシュボード反映。
- 未確定事項(プロトコル外、診断ステップで決める): TUR の exclude_admin1 県リスト、各国の calib_from/to(データ交差を見て設定、手順 1 で提案 → 手順 2 の DECISIONS で確定)。
- 関連: DECISIONS #0026 / #0030 / #0052 / #0063 / #0070。docs/PHASE3_DESIGN.md §4、候補国調査(2026-07-18 Sonnet 委譲、セッション記録)

## [0078] KOR/TUR 紛争構造診断 — exclude_admin1・regime・較正窓の確定(Issue #19)

- 日付: 2026-07-18(#0077 手順 2、Fable 診断・#0039 に基づく決定と事後報告)
- データ: Issue #18 でコミットした ACLED 生データ(KOR 42,710 events / TUR 45,015 events、来歴サイドカー付き)の admin1 一次集計(全期間 + 較正窓 ≤2021)。
- **TUR の診断**:
  - 南東部・東部のクルド紛争(PKK)県に死者が極端に集中: 上位は Şırnak 24.8% / Hakkari 19.9% / Diyarbakır 11.1% / Mardin 8.2%。地理的に連続する 12 県(Sirnak, Hakkari, Diyarbakir, Mardin, Tunceli, Van, Siirt, Bitlis, Bingol, Agri, Batman, Mus — ACLED ASCII 表記)で**全期間死者の 85.7% をイベントシェア 31.1% で占める** — THA 深南部 4 県(85%、#0026/#0030)とほぼ同一の構造。
  - 年次推移: 除外セット内は 2016=3,755 → 2017=2,399 → 逓減しつつ 2023 年まで持続(慢性)。除外後の残り(国政レベル)は 2016=734(クーデター未遂・大都市テロ = 正当な国政ジャンプ候補)→ 以降は年 2 桁 — 国政分析に適した系列になる。
  - **決定**: exclude_admin1 = 上記 12 県。regime = volatile。較正窓 = 2016-01-01〜2021-12-31(制約要因: ACLED 開始年と WB 特許出願終了年 2021)。
  - 感度の記録: 境界候補の Elazig(58)/ Kars(76)/ Erzurum(56)を加えてもシェアは +1.5% 未満で判定に影響しない。Hatay/Gaziantep/Sanliurfa(計 ~3%)はシリア紛争の越境波及でありメカニズムが異なるため除外しない(国政系列に残す)。
- **KOR の診断**: 全期間死者 0(42,710 events 全て)。慢性地域紛争なし。イベントは Seoul 40.6% を筆頭とする抗議活動中心 = 国政そのもので除外不要。**決定**: exclude_admin1 = []。regime = stable。較正窓 = 2018-01-01〜2021-12-31(制約要因: ACLED 開始年と WB Gini/特許出願終了年 2021)。
- 機構メモ: exclude_admin1 は fetch でなく下流(prepare_events / hindcast のロード時フィルタ)で適用されるため、コミット済み生 CSV は全国データのまま(来歴主義に整合)で、TOML 設定のみで有効化される。再取得は不要。
- 関連: DECISIONS #0026 / #0030 / #0077。Issue #18 / #19、experiments/data/countries/{KOR,TUR}.toml

## [0079] M13-C 実行プロトコル凍結 — KOR/TUR のヒンドキャストウィンドウ・オリジン・EKI 設定(Issue #20)

- 日付: 2026-07-19(ラン起動前の事前凍結、#0039/#0030-7 の流儀)
- 入力: tau 転記(ユーザー、d487c2f)+ パイプライン仕様調査(Explore 委譲)。ブロッカー 3 点([hindcast] 未確定 / M9_ORIGINS 未登録 / M8_frozen_config 未凍結)を本エントリで解消する。
- **ヒンドキャストウィンドウ(科学的判断)**:
  - **KOR**: calib_t = [5.0, 26.0](1995〜2016)、verif_t = [26.0, 35.0]、オリジン 26:33(8 個)— **JPN の完全ミラー**。根拠: 同型レジーム(stable・低烈度)、ACLED 開始 2018 も同一(較正窓カバレッジゼロ、検証窓中盤から)。acled_from = 2018-01-01。
  - **TUR**: calib_t = [26.0, 30.0](2016〜2020)、verif_t = [30.0, 35.0]、オリジン 30:33(4 個)— **THA 型(較正窓を ACLED カバレッジに整合)**。根拠: volatile 国はカウント尤度が較正の主情報源(THA 前例)。較正窓に 2016-17 PKK ピークと 2016 クーデター未遂(除外後国政系列のジャンプ)を含めジャンプカタログを成立させる。検証オリジン 4 個は THA(6)より少ない — ACLED 実カバー 9 年の制約として記録し、プール判定の n 減少を承知の上で #0052 帯を不変更で適用。acled_from = 2016-01-01。
- **EKI 較正**: M8_calibrate.jl を公式設定 **J=24 / iters=4 / N=100**(#0069 と同一)で国別に明示実行(pipeline の calibrate 段は既定 J16 のため使わない)。シードはソース既定 20260710(JPN/THA 公式較正と同一)。結果 JSON をオーナーが検分し M8_frozen_config.toml に凍結(#0050 流儀)。
- **theta_sig の較正時扱い(#0055 の ΣN≥50 規則に整合)**: 較正窓のフィルタ後週次カウント ΣN で判定 — **KOR = 除外**(較正窓 [5,26) は ACLED カバレッジ外で ΣN=0。JPN と同条件)、**TUR = 含める**(較正窓 2016-2020 の除外後イベント数千件 ≥50。THA と同条件)。M8_calibrate.jl の CLI 既定(country != "JPN")は JPN 固有ハックのため、後方互換の `--no-theta-sig` フラグを追加して KOR 較正時に明示指定する(関数既定・JPN/THA 経路は不変更)。
- **walk-forward**: M10_walkforward.jl(#0062 prior 規則、--mu-gbar-sd 0.3、シード既定 20260711、オリジン別再較正 J24/iters4/N100)。合格判定は **#0052 基準セット不変更**(基準1 プール被覆 0.80–0.98 / 基準2 RMSE 過半 / 基準3 NegBin logL / 基準4 テスト green、是正上限 2 回 #0070)。
- **実行順と ETA**(実測: M10 で 15〜18 分/オリジン、較正 30〜60 分/国): ① KOR/TUR EKI 較正(逐次、計 1〜2h)→ ② オーナー検分・凍結 → ③ M10 walk-forward(KOR ≈2〜2.5h、TUR ≈1〜1.2h)→ ④ 判定(#0039 事後報告)。>30 分ランは全て setsid + 完了マーカーで切り離し([[mirai-yohou-long-run-protocol]])。
- 付随コード変更(本エントリと同時コミット): KOR/TUR TOML への [hindcast]/acled_from 追記、M9_ORIGINS への KOR=>26:33 / TUR=>30:33 追加、M8_calibrate.jl --no-theta-sig フラグ、test_m8_hindcast_country_cfg.jl の期待値更新(KOR/TUR が確定国になるため)。
- 関連: DECISIONS #0052 / #0055 / #0062 / #0063 / #0069 / #0077 / #0078。Issue #20

## [0080] KOR/TUR EKI 較正値の凍結(Issue #20、#0079 手順②)

- 日付: 2026-07-19(walk-forward 起動前に凍結、#0030-7 の流儀。結果を見た後の変更禁止)
- 較正ラン: M8_calibrate.jl、J=24/iters=4/N=100、seed 20260710、コード 36f5675(#0079 凍結後)。KOR は --no-theta-sig(ΣN=0)、TUR は theta_sig 込み(#0079)。両国 exit=0。
- 凍結値(M8_frozen_config.toml に追記):
  - **KOR**(窓 [5,26]): eta_g 0.619 / delta_sig 0.126 / lam0 0.0191 / mu_gbar −0.712 / mu_p −0.863 / c_v0 3.22 / ν*=1.0(較正窓カウントなし)
  - **TUR**(窓 [26,30]): eta_g 1.572 / delta_sig 0.0517 / lam0 0.260 / mu_gbar 0.136 / mu_p 0.312 / c_v0 2.97 / ν*=12.16
- 検分所見: KOR は JPN 凍結値と高整合(eta_g 0.62/0.57、mu_gbar −0.71/−0.74、delta_sig 0.126/0.123、lam0 0.019/0.019)— JPN ミラー設計(#0079)の妥当性を支持。TUR は THA 比で高強度(eta_g 2.3 倍、lam0 2.7 倍、ν 2.9 倍)— PKK 除外後も高いイベント密度・過分散をもつ系列として整合的。
- 関連: DECISIONS #0050 / #0079。experiments/output/M8_calib_{KOR,TUR}.json(来歴付き)

## [0081] ダッシュボード視覚デザイン刷新方針 — DADS トークン正典 + エージェント委譲のハイブリッド(Issue #16)

- 日付: 2026-07-19(ユーザー選択: ハイブリッド案。視覚デザインは公開面 = ユーザー判断事項)
- 経緯: #0075 クローズ時のユーザーフィードバック「文書仕様 → 汎用エージェント実装の方式は機能要件に有効だが視覚品質は上がりにくい」を受けた抜本策検討(Issue #16)。2 案(デザイン強エージェント委譲 / 既存デザインシステム導入)を調査・比較した結果、両案は排他でなく合成可能と判断。
- 調査結果:
  - **Japan Dashboard 系の提供物は 2 系統**: ①デジタル庁「ダッシュボードデザインの実践ガイドブック + デザインテンプレート」はガイドブック PDF(7 色パレット・グリッド・チャート様式)+ Power BI 用 pbit テンプレートで、自己完結 HTML には形式非互換(設計指針の参照のみ可)。②**デジタル庁デザインシステム(DADS)の design-tokens(github.com/digital-go-jp/design-tokens、MIT ライセンス)**が tokens.css / tokens-simple.css(色・タイポ・エレベーション)として配布されており、必要部分の CSS インライン写経で CDN 依存ゼロのまま採用可能 = #0070 自己完結制約と完全両立。
  - **現行実装は視覚面のみの差し替えが可能な構造**(Explore 調査): DASHBOARD_CSS 定数と renderFanChart 等 SVG 描画の視覚表現部が対象で、データ検証・データ駆動 HTML レンダリング・JA/EN 文字列定数(VARIABLE_LABELS / STRINGS)とは責務分離済み(#0074)。
- **決定**:
  1. **DADS design-tokens(MIT)を視覚正典として採用**し、必要トークンを DASHBOARD_CSS にインライン写経する(出典コミット SHA を生成器コメントに記録)。ガイドブックのチャート様式は参照指針とする。
  2. **実装はエージェント委譲**(@claude 友人委譲を第一候補)。Issue 本文に不変条件を明記: 義務要件 a/b(#0070/#0071)、決定性(バイト同一再生成)、自己完結(外部リクエストゼロ・システムフォントスタック維持 — DADS 標準の Noto Sans JP は Web フォント読込不可のため不採用)、JA/EN 対訳表一字一句厳守、単一色相原則(§4.2)は DADS Blue 系トークンへの写像として保持、#14/#15 の挙動(言語切替時のチャートキャッシュ破棄・URL ハッシュ復元・formatAxisValue)保存、スキーマ・入力 JSON 不変。
  3. **着手順序: Issue #21(KOR/TUR ダッシュボード追加)完了後**。両者は M12_dashboard.jl を編集するため並行させない。
- 理由: 案 2 単独では品質改善がトークン置換に限定され、案 1 単独では品質がエージェントのセンス依存でレビュー負荷が高い。DADS を正典に固定することで品質の下限と将来の国追加時の再現性を機械的に保証し、その制約下でエージェントの実装力を使うのが両取りになる。政府統計ダッシュボードで実証済みの体系であり、社会統計を扱う本プロダクトの題材とも整合する。
- 関連: DECISIONS #0070 / #0071 / #0073 / #0074 / #0075。Issue #16 / #21、docs/M12_DASHBOARD_DESIGN.md
- 追記(2026-07-19、ユーザー指示): 決定 3 の着手順序を変更し、Issue #24 を即時起動する(#21 より先行)。#21 は M13 walk-forward 完了待ちで着手が数時間先のため、実際の編集期間は重ならない見込み。万一 PR が並んだ場合は先にマージした側に対しオーナーがレビュー時にリベース・衝突解消する(両者の変更領域は視覚表現部と国メタデータで分離)。

## [0082] M13-C walk-forward 判定 — KOR/TUR 両国合格(#0052 基準、是正 0 回)

- 日付: 2026-07-19(#0079 手順④、#0039 に基づく判定と事後報告)
- ラン: M10_walkforward.jl、--mu-gbar-sd 0.3、シード 20260711、コード 8771269(#0080 凍結後)。KOR 8 オリジン 7990.6 秒 / TUR 4 オリジン 4700.4 秒、両国 exit=0。出力 = experiments/output/M10_walkforward_{KOR,TUR}.json(来歴付き)。
- **判定(#0052 基準セット不変更・是正 0 回で一発合格)**:
  - **KOR: 合格** — 基準1 プール被覆 0.937(n=63、要求帯 0.80–0.98 内)/ 基準2 RMSE 改善 7/9 過半 / 基準3 カウント logL 予報 −468.9 > 自由 −1250.2 / 基準4 フルテスト 2510/2510 green。
  - **TUR: 合格** — 基準1 プール被覆 0.900(n=30)/ 基準2 RMSE 改善 8/8 / 基準3 logL 予報 −517.6 > 自由 −518.4 / 基準4 同上。
- 所見: KOR は JPN 同様の安定国パターン(強制ジャンプ 0 週、被覆・logL とも余裕のある合格)。TUR は基準3 の差が僅少(0.8)だが、これは自由ランでも捉わる程度に除外後系列の分散が大きいことの帰結で、基準の定義(予報 ≥ 自由)は明確に満たす。オリジン数 4 の制約(#0079 で事前記録)込みで n=30 の被覆 0.900 は帯中央に近い。
- 残作業(M13 完了条件の後半): 両国の M11_forecast JSON 生成(オーナー、シード 20260711)→ #21 ダッシュボード追加 → M13 クローズ判定。
- 関連: DECISIONS #0052 / #0079 / #0080。experiments/output/M13_wf_{KOR,TUR}.log

## [0083] フラッグシップ可視化のデザイン正典選定 — 自前デュアルテーマ・トークン(ライト = DADS 写像 / ダーク = Operations)+ 実装ガイダンス skill 併用

- 日付: 2026-07-19(ユーザーから選定を委任。プロトタイプ方向性はユーザー承認済み: 「グローブ + ドリルダウン」案、v1 に対し「方向性は非常によい」)
- 経緯: #0081 のトークン刷新に対しユーザーから抜本フィードバック(「政策決定機関ツールの迫力」、地図/3D、実装基盤再検討、Apple 式デザイン skills の活用示唆)。実データ(M11_forecast 正典 JSON)埋込・完全自己完結(238KB、外部リクエストゼロ)のプロトタイプ v1/v2 を制作し、静的 HTML が天井でないこと(天井は #0070 の「スクリプト 1 本」著述モデル)を実証した。
- **選定(デザイン正典)**:
  1. **自前のデュアルテーマ・トークンセットを正典として凍結する**(実装フェーズでリポジトリに文書化):
     - **ライト(Briefing)テーマ** = DADS design-tokens の写像(#0081 の継続。blue-700 系列色 + neutral gray、MIT、出典 SHA 記録済み)— 行政・公開向けの基調。
     - **ダーク(Operations)テーマ** = プロトタイプで検証した独自パレット(bg #0b0e14 / 系列 blue-400 #7096f8 / 警告 amber)— コマンドセンター基調。DADS はダーク運用体系を提供しないため独自定義とし、dataviz 検証(コントラスト・CVD)を通す。
  2. **実装ガイダンスは公開デザイン skills を併用**: 第一候補 = Anthropic 公式 frontend-design skill(2026-02 更新版)。Apple HIG 系 skills(apple-hig-designer 等)はプラットフォーム特化のため正典採用せず参照に留める。採用時にライセンス・自己完結性(CDN 参照を吐かないか)を審査する。
  3. **機能要件(プロトタイプ v2 で確定)**: 3D グローブ(回転・国選択)+ 国リスト併置 / 予報タイムライン再生(年スクラブ・期待イベント数連動のマーカー発光・チャートカーソル同期)/ 全チャートの拡大モーダル(大判ファンチャート + 分位テーブル)/ ライト・ダーク切替 / 検証済み・外挿の視覚区別と正典文言の維持 / 来歴(seed・commit)常時表示。
  4. **シンクタンク型レポート機能(将来 Issue 化)**: 正典予報 JSON → LLM 起草(数値は JSON から機械挿入し捏造防止)→ 人手レビュー → 版管理して同梱 → UI から閲覧。今回は実装せず UI にスタブのみ。
- 未決(ユーザー承認待ち・凍結条項のため本エントリでは決定しない): #0070 の「Julia スクリプト 1 本の文字列リテラル」条項の改定(vendored ライブラリ + ビルドステップの静的サイト化)。正典ダッシュボードとフラッグシップの関係(置換か併存か)も同判断に含める。
- 関連: DECISIONS #0070 / #0074 / #0081。プロトタイプ = セッション成果物(scratchpad、正典外)

## [0084] #0070 決定 2 の実装様式条項の改定 — フラッグシップ可視化の実装基盤(ユーザー明示承認)

- 日付: 2026-07-19(凍結条項の改定につきユーザー明示承認を取得: 「#0070 を承認します」。改定対象と原則維持の説明を提示済みの上での承認)
- **改定内容**: #0070 決定 2 の「自己完結静的 HTML(…)Julia で決定的再生成」のうち、**「Julia スクリプト 1 本の文字列リテラルで生成する」という著述形式の限定を解除**する。公開ダッシュボードは、ライブラリ同梱(vendoring)+ ビルドステップを持つ専用フロントエンドの静的サイトとして実装してよい。
- **不変の原則**(改定されない): 自己完結(実行時外部リクエストゼロ — CDN・Web フォント・fetch 禁止)/ 決定的ビルド(同一入力 → 同一成果物バイト列)/ GitHub Pages 配信(main /docs)/ 来歴の埋め込み(seed・commit・生成日時)/ 義務要件 a(検証済み・外挿の視覚区別と正典文言)・b(p_ex_fallback 警告)/ JA/EN 対訳表の厳守 / 正典入力 = M11 予報 JSON(スキーマ不変)。
- **正典との関係(#0039 に基づく Fable 決定): 段階的置換**。フラッグシップが M12 義務要件のパリティ + 受入判定を満たした時点で公開 UI の正典に昇格し、M12 単一ファイル生成器は凍結保守(機能追加なし、フォールバック・回帰照合用)へ移行する。並存期間中の公開 URL は現行を維持し、切替時に DECISIONS 記録。
- 実装の正典デザイン = #0083(デュアルテーマ・トークン、機能要件はプロトタイプ v5 で確定)。vendored ライブラリは採用時にライセンス・自己完結性を個別審査(MathJax 含む)。
- プロトタイプ経緯: v1(グローブ + ドリルダウン)→ v2(タイムライン・モーダル・ライトテーマ)→ v3(自転方向・2D 地図・スパン・観測重畳・注釈・スケール群化・ランキング)→ v4(国比較・被り修正)→ v5(4 カ国化・モーダル蓄積バグ修正)。全て scratchpad 成果物(正典外)、ユーザーレビュー済み。
- 関連: DECISIONS #0070 / #0074 / #0081 / #0083。Issue #26 / #27 / #28 / #29 / #30

## [0085] M13 合格クローズ — 横展開サイクルの完成(KOR/TUR、vM13)

- 日付: 2026-07-19(#0039 に基づく合格認定と事後報告)
- 完了条件(#0070/#0077 凍結)の充足:
  1. **walk-forward が M10 凍結基準帯内**: KOR 被覆 0.937(n=63)・RMSE 7/9・logL PASS / TUR 被覆 0.900(n=30)・RMSE 8/8・logL PASS(#0082、是正 0 回・基準不変更)。
  2. **ダッシュボード反映**: 4 カ国 HTML を再生成し main a74265d で公開、Pages 配信バイト列一致確認済み(PR #31 / Issue #21)。
- サイクル実績(#0077 手順 1〜5): 国設定 + データ整備(#18)→ 紛争構造診断(#19/#0078: TUR 12 県除外・THA 同型)→ プロトコル凍結(#0079)→ EKI 較正凍結(#0080)→ walk-forward 判定(#0082)→ ダッシュボード追加(#21)。**確立済みパイプラインが新レジーム(競争的権威主義 TUR)と安定国複製(KOR)の両方に一発合格で一般化**した — Phase 3 の中心仮説を実証。
- 委譲実績: @claude 友人委譲 3 件(#22/#24/#21)全て成功、オーナーレビュー体制(テスト自走・決定性・凍結不可侵)で品質維持。
- タグ: vM13(main a74265d)。Phase 3(M11〜M13)全マイルストーン完了。
- 次: フラッグシップ可視化(#0083/#0084、Issue #32)と Phase 4 提案(#28 相互作用 / #29 高頻度観測 / #30 wbar 時変化 / #26 レポート / #27 V&V 文書)の優先順位づけをユーザーと相談。
- 関連: DECISIONS #0070 / #0077 / #0078 / #0079 / #0080 / #0082
- 追記(2026-07-19、#0083 デザイン正典の精緻化 — ユーザー要望「比較国の色分け」「国旗表示」を受けた Fable 決定): **色は実体(国)に従う**。単一国ビュー内は従来どおり単一色相(§4.2 — 同一アンサンブルの分位のため)を維持し、**国横断の文脈(比較オーバーレイ・ランキング・地図マーカー)では国別固定カテゴリカル色**を用いる: JPN 青(#7096f8/#264af4)・THA 青緑(#2fbf9a/#197a3e)・KOR 紫(#c792ea/#6f42c1)・TUR 橙(#f2a33c/#b25000、ダーク/ライト)。dataviz 検証: ライト全 PASS、ダークは CVD・コントラスト床 PASS(明度帯逸脱は国旗 + 国名直接ラベルの二次符号化で担保)。**国旗は簡略インライン SVG**(自己完結、絵文字国旗は Windows 非描画のため不採用)。国追加時は色スロットを本表に追記し再検証する。

## [0086] 国拡大トラック バッチ 2 プロトコル凍結 — 対象国 EGY(Issue #33)

- 日付: 2026-07-19(#0039 に基づく Fable 自律決定の事前記録。着手はユーザー指示)
- 経緯: Issue #33(常設トラック: 登録国の逐次拡大)の初回バッチ。#0077 の候補調査で「有力だが 3 カ国目はパイプラインが 2 カ国通ってから」と保留した **EGY** を、M13 合格クローズ(#0085)によりその条件が充足されたため次候補として確定する(Issue #33 の候補順 1 位どおり)。
- バッチ構成: **EGY 単独の 1 国バッチ**。理由: volatile 型 + 未知の紛争構造(北シナイ反乱・2011 革命期)で診断・是正コストが読めないため、KOR/TUR のような 2 国並行にせず単独で通す。IDN/PHL/MYS は次バッチ候補(#35 で tau を先行取得中)。
- プロトコル(#0077 手順 1〜5 の反復を EGY に具体化。凍結):
  1. **国設定 + データ整備**: countries/EGY.toml を作成し、確立済みスクリプト(fetch_data.jl / extract_vdem_p.jl / extract_swiid_g.jl)でデータ取得・来歴付きコミット(ACLED 認証のためローカル実行)。tau(WVS E069_11)は Issue #35(cowork)でユーザーが転記中 — KOR/TUR 前例(データ整備 086cb66 → tau 転記 d487c2f)どおり、データ整備・診断は tau と独立に先行してよい。
  2. **紛争構造診断**: ACLED 生データの admin1 別死者シェアを一次集計し、exclude_admin1(想定: 北シナイ反乱の地理的局在性の確認)・regime 分類・較正窓を Fable が診断 → DECISIONS 記録 + ユーザー報告(THA 85% 分析 / #0078 と同じ流儀)。2011 革命・2013 政変期は国政レベル事象としてジャンプカタログ側で扱う想定(診断で確定)。
  3. **実行プロトコル凍結(#0079 相当)**: tau 転記(#35)完了を待って、ヒンドキャストウィンドウ・オリジン・EKI 設定(J=24 / iters=4 / N=100、シード 20260710)・theta_sig の扱い(#0055 ΣN≥50 規則)を別エントリで凍結してから較正に着手する。
  4. **較正 + 判定**: EKI 較正値凍結(#0050 流儀)→ M10_walkforward(#0062 prior 規則、シード 20260711)→ **#0052 基準セット不変更**で合否判定。是正上限 2 回(#0070)、上限到達時は部分達成クローズ判断をユーザーへ。
  5. **ダッシュボード追加**: 合格時に M12 生成器の既定 iso3_list へ EGY を追加し再生成・コミットはオーナー(#0074 決定 5)。フラッグシップ色表(#0085 追記)への EGY 色スロット追記 + パレット再検証も国追加時作業として実施。
- 完了条件(#0070 から不変): EGY の walk-forward が M10 凍結基準の要求帯内 + ダッシュボード反映。
- 未確定事項(手順 2 の診断エントリで確定): exclude_admin1 県リスト、regime 分類(事前想定 volatile)、calib_from/to・ヒンドキャスト窓(ACLED カバレッジ 1997〜の利点をどう使うかを含む)。EGY の WVS Wave 7 参加有無は #35 の確認結果に従う(欠測なら ZAF 前例 #0077 に照らして扱いを判断)。
- 関連: DECISIONS #0039 / #0052 / #0055 / #0070 / #0077 / #0079 / #0085。Issue #33 / #35

## [0087] EGY 紛争構造診断 — exclude_admin1 = North Sinai 単独、regime = volatile、較正窓 2011〜2021(#0086 手順 2)

- 日付: 2026-07-20(#0039 に基づく Fable 診断。ACLED 生データ 1997-01-03〜2025-07-17、11,971 件 / 死者 15,410 の admin1 一次集計に基づく)
- **exclude_admin1 = ["North Sinai"] に確定**:
  - シナイ反乱(2013〜、ISIS 系 Wilayat Sinai)は北シナイ県に地理的に凝集: 死者シェアは 2014-2021 で **84.5%**(8,867/10,493)、2022 以降 70.5%、全期間 63.3%。THA 深南部 4 県(85%、#0026/#0030)・TUR 南東部 12 県(85.7%、#0078)と同型の慢性地域紛争で、EGY は**単一県**で同水準 — 最も凝集した除外例。
  - 分離の質: 2011-2013 革命期の北シナイシェアは 14.7% に留まり、除外後の国政系列にも 2011 革命(除外後 約779 人、Cairo 中心)・2013 政変/ラバア広場(除外後 約1,100 人)の国政レベルジャンプが残る。慢性地域反乱の除去と内生ジャンプの保持が単一県除外で両立する。
  - South Sinai の 2004-2006 スパイク(観光地爆破テロ、計 140 人)は単発型で慢性反乱でなく、較正窓外のため除外しない。
- **regime = "volatile" に確定**: 除外後系列にも大規模内生ジャンプ(2011/2013)が明確。イベント構成も Battles 47.9% / Explosions 19.4% が死者の主因(THA/TUR 型)。
- **較正窓 calib_from = 2011-01-01 / calib_to = 2021-12-31 に確定**: ACLED カバレッジは 1997〜 と長いが、ムバラク期(〜2010、年間死者 0〜171 の低烈度散発)は構造の異なる体制期であり、2011 革命を自然な断点とする。11 年窓はジャンプクラスタ(2011-2013)と鎮圧期の減衰(2014-2021: 除外後 429→9 人/年)の両方を含み、ジャンプカタログと 1/ν プロファイル(#0033)の情報源として成立する。calib_to は WB 系列終端 2021(KOR/TUR #0078 と同じ制約要因)。
- ヒンドキャストウィンドウ([hindcast] calib_t/verif_t)・オリジン・EKI 設定は本エントリで**決めない** — tau 転記(#35)完了後の実行プロトコル凍結エントリ(#0086 手順 3、#0079 流儀)で確定する。
- 一次集計の来歴: raw/EGY_events.csv(コミット 74e02ae)から期間別 admin1 死者シェア・年別系列・event_type 内訳を集計(セッション記録。集計定義は #0078 と同一)。
- 関連: DECISIONS #0026 / #0030 / #0033 / #0078 / #0086。Issue #33
